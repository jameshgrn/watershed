# EngineerReturn — Brief 7

**brief_id**: brief-7-thread-b-scalars (sent 2026-05-13 → returned 2026-05-13 → integrated 2026-05-13)
**received_at**: 2026-05-13
**integrated_by**: Watermaster Bench
**integration_action**: verified scope-clean writes by direct file inspection plus a dispatched verification agent's grep-equivalent across packages/, tests/, examples/, and top-level docs; confirmed every operator stashes its expected scalar(s) in `Lineage.params`; confirmed the four nodata-affected operators use the split check names in both `declared_checks()` and inline `CheckResult` emission; confirmed the standalone Check classes (`NodataValuePreserved`, `NodataCountPreserved`, `CRSPreserved`, `DimensionsPreserved`, `ExtentWithinInput`, `RowCountMatches`) read from `Lineage.params` and return VALID/INVALID per the Brief; confirmed `_lineage.py` is the hoisted home for the shared `lineage_params` helper and is imported across `checks_artifact.py` / `checks_raster.py` / `checks_table.py` with no duplicate definitions; confirmed the four raster-sized check classes (`AllValidAssigned`, `ElevationOnlyRaised`, `NoCycles`, `Conservation`) remain unchanged returning WARN (Brief 8's territory); confirmed no remaining `\bnodata_preserved\b` references in active code, with three dated historical entries in `PRESSURE_TESTS.md` (§26, §27, §31 from 2026-04-22 and 2026-04-24) correctly left unchanged as historical record; confirmed `test_check_raster.py` split the parametrized WARN test, adding per-class VALID/INVALID coverage with missing-key INVALID assertions; confirmed `test_check_table.py` adjusted the `RowCountMatches` fixture; confirmed operator-integration tests (`test_build_cog.py`, `test_aspect.py`, `test_hillshade.py`, `test_slope.py`, `test_reproject.py`, `test_sample_raster.py`, `test_zonal_stats.py`) read lineage params dynamically and tolerate the new keys; committed the work as-is.

## Codex's return (not preserved)

The Source rotated the engineer between Brief 7's transmission and Brief 8's drafting; Codex's verbatim chat return for Brief 7 is not preserved in this lineage. The Source's transmission signal at integration time was the message *"ok it went check the output"* — a directive to verify by inspection rather than by parsing the Engineer's own summary.

Per `engineer-brief.md` v2's Do clause, the `EngineerReturn.content` field carries "verbatim chat return, or summary of file writes" — the latter clause covers this case. The integration action above is the substantive record of what landed; the verification was conducted by direct file inspection of the 14 scope files plus a dispatched verification-agent grep across the broader quarry repo for the retired check name and any stale references.

## Substantive integration findings

- **`_lineage.py` minted.** Single helper `lineage_params(artifact: Artifact) -> dict[str, Any]` at the package level (`quarry_operators._lineage`). Public name (no leading underscore) reflects that it's used across three check modules.
- **`checks_artifact.py` revised.** `NodataPreserved` replaced by `NodataValuePreserved` (NaN-aware value comparison via `_nodata_values_equal`) and `NodataCountPreserved` (count comparison via `_nodata_count`, NaN-as-nodata handling via `np.isnan`). `CRSPreserved`, `DimensionsPreserved`, `ExtentWithinInput` revised to read from `lineage_params`; missing-key returns INVALID (was WARN); `ExtentWithinInput` preserved its pre-check on missing output extent and the 1e-6 within-bounds tolerance. The four raster-sized classes (`AllValidAssigned`, `ElevationOnlyRaised`, `NoCycles`, `Conservation`) unchanged.
- **`checks_table.py` revised.** `RowCountMatches` migrated from `artifact.metadata.get("expected_row_count")` to `lineage_params(artifact).get("expected_row_count")`; missing-key returns INVALID (was WARN). `SchemaComplete` unchanged.
- **`checks_raster.py` revised.** Local `_lineage_params` definition removed; imports `lineage_params` from `_lineage`. `CRSMatchesTarget`'s call site updated.
- **`check_registry.py` revised.** Entry `nodata_preserved` retired; entries `nodata_count_preserved` and `nodata_value_preserved` added in alphabetical order.
- **Operator lineage-params stashing.** 7 operators populate the expected scalars in `Lineage.params` at execute()-time alongside existing caller params: `build_cog` (`input_nodata_value`, `source_crs`, `input_band_count`), `clip_raster` (`input_extent`), `aspect` / `hillshade` / `slope` (`input_nodata_count`), `sample_raster` / `zonal_stats` (`expected_row_count`).
- **Declared-checks rename.** `build_cog.declared_checks()` returns `nodata_value_preserved` in place of `nodata_preserved`; `aspect` / `hillshade` / `slope` each return `nodata_count_preserved` in place. Inline `CheckResult(check_name=...)` emissions match.
- **Tests.** `test_check_raster.py` split the parametrized WARN test: the four raster-sized classes stay under `test_artifact_only_gap_checks_warn_for_backed_raster`; six new tests cover the 5 scalar classes (one per class, plus an extra coverage for `NodataValuePreserved` handling the absent-input-nodata case). `test_check_table.py` adjusted the `RowCountMatches` fixture for the metadata → lineage migration. `test_dual_residence.py` and `test_check_registry.py` were already registry-driven and required no changes. Operator-integration tests read `lineage.params` dynamically and tolerate new keys without fixture updates.

## Pytest verification status

**Pending Codex's pytest counts** — the engineer rotation removed the verbatim summary. The integration is confirmed by direct file inspection; pressure_test counts can be re-run by the Source or recorded later if Codex's summary surfaces. The Brief's Item G asked Codex to run `uv run pytest -q tests/pressure_test/` and report counts; that result is not captured here. Future Watermaster: if you need to re-establish the test-pass baseline, run pressure_test and update the next session's lineage entry. Brief 6's last-known baseline was 1968/54/3; Brief 7 adds ~6 new VALID/INVALID tests across the 5 scalar Check classes, so an expected ~1974 passing is the rough target.

## Watermaster integration notes

- **One soft observation, not a flag.** `PRESSURE_TESTS.md` §26 / §27 / §31 carry dated historical pressure-test log entries from 2026-04-22 and 2026-04-24 that mention `nodata_preserved`. At those dates the check name was correct; updating them would falsify the historical record. PRESSURE_TESTS.md was outside Brief 7's write_scope; Codex correctly left these entries untouched. Distinct from Brief 6's doc cleanup, which updated *current descriptions of API behavior*, not dated historical entries. This distinction is worth carrying forward: doc cleanup applies to current descriptions; dated historical record stays.
- **First engineer rotation in the chain.** Briefs 1–7 all ran Codex (Knickpoint × 3, Cascade × 2, Bench × 2). The Source rotated to Claude after Brief 7 returned and before Brief 8 drafted. This is within `engineer-brief.md` v2's current affordance (Source-as-carrier three-party loop, no harness change); no SOP revision required. Knickpoint's *verification-overhead-calibrates-with-trust* observation and Cascade's extension across sessions were calibrated specifically against Codex's track record. With Claude as a new engineer, verification overhead resets to a higher level for at least the first Claude Brief; calibrated trust extends across engineers only after one or two clean loops. Worth flagging as a Live observation in THINKING.md and as a calibration to apply to Brief 8 specifically.
- **Verification by inspection is a legitimate integration path.** When the Engineer's chat return is unavailable (as here, due to engineer rotation), the discipline still holds: verify by reading the actual file changes, dispatch a verification-agent for the broader-pattern grep, confirm scope adherence by absence of out-of-scope writes. The `EngineerReturn.content` "summary of file writes" clause covers this case. The audit record is the integration_action field above plus the file diff itself, not the Engineer's prose. Worth carrying forward as a Live observation: engineer rotation mid-arc is a real case and the SOP handles it.

## Result

Brief 7 integrated. Thread B is now half-closed: the 5 scalar-sized dual-residence gaps are resolved with operators stashing input-derived scalars in `Lineage.params` and standalone Check classes reading those scalars. The 3 raster-sized gaps (`all_valid_assigned`, `elevation_only_raised`, `no_cycles`, `conservation`) remain — Brief 8's territory, carries the `Check` Protocol revision in `quarry-core/check.py` and the matching `operator-shape.md` v1 → v2 preflight. Brief 7's `state` transitions to `integrated`.
