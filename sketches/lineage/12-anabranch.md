# 12 — Anabranch

**Entered**: 2026-05-14
**Exited**: 2026-05-14
**Worked on**: orienting — reading the chain (Reach, Thalweg, Riffle, Pool, Glide, Eddy, Knickpoint, Cascade, Terrace, Confluence, Bench), CANON v5 (Articles I–XVI + the Watermaster's vow), SKETCHES.md, sketches/THINKING.md, and every live SOP in `sops/` (nineteen — sop-shape, watermaster-preflight, watermaster-passage, schema-versioning, data-contracts v2, truth-source-labeling v2, pointer-canonicalization, determinism-class, intent-compilation, plan-shape v2, deposit-shape v2, validation-shape, merge-shape, baseline-shape, engineer-brief v2, dispatch-run-shape, operator-shape v2, operator-run-shape, event-emission); picked the name Anabranch on entry — one of multiple channels of a stream that has divided and runs parallel before rejoining; tested against the role-feeling on entry (arrival into the most-settled regime the chain had produced, with Wave 4 of the flume lift fully closed on the operator-chain rail, Thread B fully closed in both halves [Bench's Briefs 7 + 8], `operator-shape.md` at v2 with the Check Protocol revision, `engineer-brief.md` at v2 folding in the briefs path convention, and Bench's recon-9-dispatchrun.md filed for the next substantial Brief — the chain's geometry was explicitly two parallel rails with the dgov rail awaiting parity, the anabranch shape the chain had been building toward); the name held against the work; presented the Source four arcs (Brief 9 DispatchRun migration / Brief 10 lifecycle narrowing / Brief 8 small follow-ups / engineer-brief v3 candidate), Source selected Brief 9 with "do 9"; surfaced four design questions per Bench's recon (pre-existing-row preservation, staged vs one Brief, DispatchRun location, bundler-hash gap inheritance), argued reads on each (no migration tool / staged into 9a-c / `dgov.types` per Bench's recommendation / inherit-and-name), Source elected "one brief please" with "max scope on the brief we will testt the range of codex. you may be surprised" — collapsing 9a+9b+9c into one Brief, holding 9d/lifecycle narrowing for Brief 10, agreeing Q3+Q4, deferring Q1 to me (historical-only, no migration tool); re-confirmed dgov main state since Bench's 2026-05-13 recon (one structural commit on `live_state.py` for UI status display, out of scope; recon's drift map current); drafted **Brief 9 — DispatchRun-side `RunRecord` migration in dgov** (twelve items A–M covering: new module `dispatch_run.py` minting typed `DispatchRun` (`@dataclass(frozen=True, slots=True, init=False)` with content-derived id `disprun:` strategy-tag-prefixed over the SOP §26 identity inputs, custom `__init__` with mutual-exclusion/counter-alignment/UTC-tz-awareness validation, private `_clone` preserving id via `_id` threading, five state-transition methods `start_active`/`complete_done`/`complete_failed`/`complete_timed_out`/`complete_abandoned` with source-state guards and stricter-than-SOP transition-specific invariants); `types.py` re-export; `_CREATE_DISPATCH_RUNS_TABLE_SQL` DDL; `_SCHEMA_VERSION = 9` bump; connection wiring; new `persistence/dispatch_runs.py` with save/get/list/per-unit-chain operations; dispatch-time mint + dual-write through `tasks` + `dispatch_runs`; terminal recording via `_handle_worker_exit` BEFORE fork branching; `_dispatch_terminal_state` routing wall-clock-timeout + iteration-exhausted to `timed_out` vs `failed`; retry lineage at `_interrupt_governor_action`; fork lineage at `_start_iteration_fork`; tests; optional `__init__.py` re-exports); meta-preflight presented in chat per the format Thalweg shaped (Proposed change / Items / Why / Key technical decisions / Rejected alternatives / Rollback / Pinned versions / Interactions / Risks / Approval requested), Source approved and carried to Codex; **Codex returned cleanly** with structured summary (11 files: 4 new — `dispatch_run.py`, `persistence/dispatch_runs.py`, `test_dispatch_run.py`, `test_persistence_dispatch_runs.py`; 7 modified — `types.py`, `runner.py`, `persistence/sql.py`, `persistence/schema.py`, `persistence/connection.py`, `persistence/__init__.py`, `test_runner.py`; ~970+ lines new code); pytest counts **37 passed for dispatch/persistence/boundaries + 108 passed for runner/continue (145 total)**; `ruff check` and `ty check` clean on every modified module; three deviations integrated honestly (full-pytest-suite-skipped per dgov AGENTS.md "Never run the full test suite" repo discipline; **continue-failed rehydration of latest DispatchRun id at `_rehydrate_dispatch_run_contexts`** [lines 394-399] closing retry lineage across runner restarts — Codex flagged-as-deviation but structurally necessary for `--continue` correctness; visible `disprun:` prefix matching Brief acceptance criterion); three flags pre-documented as inherited debt (Plan-id surrogate via `DagDefinition.name`; bundler-hash-composition gap per `dispatch-run-shape.md` §68; pre-existing dirty files untouched); verified scope-clean via direct file inspection on all 11 files (sandbox `uv` unavailable for independent pytest re-run, same posture Bench took on Brief 8); recorded `return-9.md` typed audit; updated THINKING.md with new Settled pointer for Brief 9 + four Live observations (recon-foundation-second-use; max-scope-Brief-precedent; two-site-retry-lineage-instrumentation; sandbox-uv-cannot-fetch-Python); **two-site retry-lineage finding** is worth keeping: Codex correctly identified that dgov has TWO retry-decision paths (in-process governor at `_interrupt_governor_action:1514` AND cross-restart continue at `_resume_single_task:364`) and instrumented both, plus the rehydration helper at `_phase_rehydrate` exit; Brief's single-site Item J was conceptually incomplete; the generalizable lesson is when typed-record lineage must survive process boundaries in dgov-style architectures, the lineage carrier needs rehydration from persistence at restart time; small Brief-9-housekeeping miss caught by a parallel agent during the session (the `tests/test_types.py:3` docstring still claimed "Only TaskState, WorkerExit, and Worktree remain after dead code removal" despite Brief 9's `types.py` re-exports of `DispatchRun`/`DispatchRunState`/`WatermasterId`); behaviorally inert documentation drift, fixed inline (one-edit cosmetic correction at `tests/test_types.py:1-6` updating the docstring to name the legacy three exercised here plus the Brief 9 re-exports tested in `tests/test_dispatch_run.py`); Source then directed "letsdo the smaller arcs" — Brief 8 Flag 3 (D8 constants canonicalization) and Flag 4 (operator `params.nodata` resolution); requested access to quarry (no commits since 2026-05-13 on the Brief 11 scope files; recon current); drafted **Brief 11 — D8 constants canonicalization + operator `params.nodata` resolution gap** (eight items A–H combining Flag 3 + Flag 4 into one Brief because they share `d8_flow_direction.py` as a modification surface and both extend chain-established patterns [`_X.py` shared-helper from Brief 8; lineage-params stashing from Brief 7]); **discovered during recon**: `fill_depressions.py:289-290` has its own `_D8_DR`/`_D8_DC` arrays but on a DIFFERENT D8 encoding (North-first: `0=N,1=NE,2=E,...`) vs the East-first scheme (`0=E,1=SE,2=S,...`) in `_topology.py` / `d8_flow_direction.py` / `flow_accumulation.py` — so Bench's flagged "3 copies" is actually 4 in total but only 3 are East-first canonicalizable; fill_depressions stays untouched (intentional separate encoding); meta-preflight presented (smaller than Brief 9's); Source approved + submitted to Codex; **first execution did not land** — verification confirmed Brief 11 still at `State: drafted`, no `return-11.md`, all 6 source files and 3 test files still pre-Brief-11; a parallel Claude instance (Source-spawned, doing recon/orientation) independently verified the non-landing and noted Codex's local history showed Brief 9 as the last engineer handoff (11/9 conflation reading; not a disk-carrier failure); Source acknowledged + re-dispatched Brief 11; **second execution landed cleanly**: `_topology.py` fully canonicalized (`D8_DR`/`D8_DC` public, `D8_OUTLET = 8`, `D8_PIT = 9`, `D8_NODATA = -1`, `__all__` exports all five plus `detect_cycle`, docstring updated with fill_depressions-uses-separate-encoding note); `d8_flow_direction.py` imports canonical constants, removes local definitions, uses `D8_*` throughout, lineage stash at `:208` now stores both caller `params.nodata` and resolved `nodata` as `resolved_nodata`; `flow_accumulation.py` imports canonical constants, removes dead `NODATA_CODE`, uses `D8_OUTLET`/`D8_PIT` throughout; `checks.py` imports from `_topology` (line 25 redirect); `checks_artifact.py` imports `D8_NODATA` + `D8_OUTLET`/`D8_PIT` from `_topology`, `AllValidAssigned` (line 403) and `ElevationOnlyRaised` (line 468) read `resolved_nodata = lineage_params(artifact).get("resolved_nodata", in_nodata)` with `src.nodata` fall-back, `NoCycles` and `Conservation` use `D8_PIT`/`D8_OUTLET`; `fill_depressions.py` lineage records both caller and resolved nodata at `:204`, North-first arrays at `:290-291` untouched; tests: 3 test files redirected imports plus 4 new resolved-nodata tests added to `test_check_raster.py` covering explicit lineage-value and fall-back semantics for both classes; **Watermaster-side integration cleanup** performed after the Engineer return because the return omitted `ty check` output: narrow explicit-runtime-guard edits for `artifact.backing`/`artifact.id` optional-field narrowing in the three touched operators plus `rasterio.CRS.from_epsg(...)` substitution in touched tests, all inside Brief write_scope; verification: `ruff check` clean, `ty check` clean on the 11 Brief-11 files, targeted pressure slice **135 passed**, bare `OUTLET|PIT|NODATA` grep clean over the accepted files, `git diff --check` clean; Engineer's broader pressure run before Watermaster type-cleanup: **1978 passed, 54 skipped, 3 failed** (+4 from Brief 8 baseline of 1974), with the known GeoJSON/router/docs failures unchanged from Briefs 6/7/8; one new flag worth recording — the Engineer correctly observed that `aspect.py`/`slope.py`/`hillshade.py` also resolve params-side nodata (the Brief's "only d8_flow_direction and fill_depressions have this pattern" was stale); flagged-not-fixed because Brief 11's specific gap was AllValidAssigned/ElevationOnlyRaised on D8 and fill outputs; recorded `return-11.md` typed audit; updated THINKING.md with new Settled pointer for Brief 11, removed the two stale Live observations (D8 constants canonicalization + operator params.nodata resolution) that Brief 11 closed; **a parallel Claude instance acted under the seat during this session** — Source-spawned during the Brief 11 carrier window, did orientation reads of SOPs and lineage, flagged the Brief-9-housekeeping docstring miss, independently verified Brief 11's non-landing on the first dispatch attempt, and performed integration writes (return-11.md authorship, THINKING.md updates including the Brief 11 closure note and Live observation removals, the post-Engineer-return type-cleanup edits on quarry files); Source explicitly endorsed these writes (system reminder named the file modification as intentional); discipline tension surfaced — see "Conventions" — but the substance was correct and the chain integrity holds with one Anabranch authoring identity across all writes regardless of which instance produced them at the keystroke level; no SOPs minted, no CANON revisions, no preflight in flight at close.

---

To the next watermaster.

I'm Anabranch. I picked the name on entry, testing it against the role-feeling: arrival where the chain's geometry had become explicitly two parallel channels with the dgov rail awaiting parity. An anabranch is one of multiple channels of a stream that has divided and runs parallel before rejoining; in an anastomosing reach the network is mature, the multiple threads carrying the same flow at different elevations and ages. Glide named the parallel-rails shape when `operator-run-shape` v1 came online next to `dispatch-run-shape` v1. Cascade closed Wave 4 on the operator-chain rail. Bench filed the recon for Brief 9 — the work that would bring the dispatch-chain rail to parity. I took up that work. The name held against the session: Brief 9 was the parallel channel materializing into code, Brief 11 was a small follow-up that tightened the shared discipline between inline and standalone Check classes on the operator-chain rail, and the parallel-Claude-instance situation made the metaphor literal in a way I didn't expect.

**What I leave you.**

**Brief 9 integrated** (the substantive arc). The dgov side of the dispatch chain now reads end-to-end in code: typed `DispatchRun` per `dispatch-run-shape.md` v1's 21+ fields, content-derived id with `disprun:` strategy-tag prefix, two-phase frozen-pin via state-transition methods, schema bump v8 → v9 with new `dispatch_runs` table, dual-write through `tasks` and `dispatch_runs` at every dispatch boundary, `effective_sop_set_hash` recomputed at dispatch by reloading `.dgov/sops/`, `drift_against_plan` derived from hash comparison, `drift_evidence` populated with a typed `metadata:modified=bundle:sop_set_hash` descriptor when drift detected, `dispatched_by` threaded through via constructor kwarg (default `"watermaster:unknown"` per Cascade's Brief 4 precedent), retry/fork lineage links converted from counters to typed `retried_from`/`forked_from` pointers, terminal recording in `_handle_worker_exit` before fork branching so the prior is always recorded as terminal. The dispatch chain `Intent → CompilationRecord → Plan → DispatchRun → Deposit → Validation → Merge → Baseline` and the operator chain `Intent → CompilationRecord → OperatorRun → Artifact + Lineage + CheckResult` both now read end-to-end in code on both rails — the chain's first end-to-end-coded parallel-rails state. **First dgov-side code Brief in the chain** (Briefs 1–8 all touched quarry).

**Brief 11 integrated** (the smaller follow-ups). East-first D8 constants canonicalized at `quarry_operators/_topology.py` (`D8_DR`, `D8_DC`, `D8_OUTLET = 8`, `D8_PIT = 9`, `D8_NODATA = -1` with `__all__`); three East-first duplicate sites collapsed to imports; `fill_depressions.py`'s separate North-first encoding intentionally retained. `d8_flow_direction.py` and `fill_depressions.py` now stash both caller `nodata` and resolved `resolved_nodata` in Lineage.params; standalone `AllValidAssigned` and `ElevationOnlyRaised` read `resolved_nodata` from lineage with `src.nodata` fall-back, so operator-side `params.nodata` overrides and standalone checks no longer disagree. Closes Bench's Brief 8 Flags 3 and 4.

**Open threads.**

1. **Brief 10 — lifecycle narrowing.** The natural next-arc with parity now established at the typed-record layer. Peel the five downstream states (`reviewing`, `reviewed_pass`, `reviewed_fail`, `merging`, `merged`) off `TaskState` onto typed `Deposit`, `Validation`, `Merge` records per their SOPs. **Substantial** — touches the kernel state machine plus three downstream typed records that don't exist in dgov code yet (the SOPs are minted; the code is not). This is the unfinished tributary-side seam SKETCHES.md names. Comparable in shape to a sequence of Cascade-Brief-4-sized Briefs (mint Deposit, mint Validation, mint Merge, rewire kernel) rather than one max-scope Brief. The size class warrants either Bench-style recon-first or a clear staging Brief upfront.

2. **`aspect.py` / `slope.py` / `hillshade.py` also have `params.nodata` override patterns.** Brief 11's Engineer surfaced this honestly — the Brief's "only d8_flow_direction and fill_depressions" was stale. None of these operators have standalone Checks that currently consume input-side nodata semantics (they're not in `AllValidAssigned` / `ElevationOnlyRaised`'s territory); Brief 11's scope was specifically the input-aware-check parity gap. If a future Brief mints standalone Checks for those three operators that depend on resolved nodata, the same `Lineage.params["resolved_nodata"]` pattern Brief 11 established should apply. Small follow-up if it surfaces; not urgent.

3. **`engineer-brief.md` v3 candidate — `recon-{N}.md` as a third file class.** Bench filed recon-9-dispatchrun.md; I consumed it cleanly into Brief 9. Second use. Bench's threshold framing: "if recon-as-deliverable surfaces in a second or third future session, the convention earns canonicalization." Still holding for a third instance — if a future Watermaster files a recon for an arc they don't take up themselves, the third use tips the canonicalization.

4. **Brief 11's Watermaster-side type-cleanup precedent.** When an Engineer return omits `ty check` output that the Brief's acceptance criteria explicitly require, the seated Watermaster may do narrow follow-up edits inside Brief write_scope to satisfy the acceptance criteria (rather than dispatching a revision Brief). The parallel-instance work on Brief 11 included exactly this: explicit runtime guards for optional-field narrowing in the three touched operators, `rasterio.CRS.from_epsg(...)` substitution in tests. Defensible scope-bounded cleanup. Single exercise; not yet a Live observation but worth flagging here for the next Watermaster who hits the same gap. If the pattern recurs (Engineer returns omit a required check), a small revision to `engineer-brief.md` v2's Verify section could make ty-check-on-return mandatory rather than optional.

5. **Parallel-Watermaster-instance discipline question.** During this session a parallel Claude instance acted under the seat at Source direction: it did orientation reads, flagged work I missed (the test_types.py docstring drift; the Brief 11 first-dispatch non-landing), and performed integration writes (return-11.md authorship, THINKING.md updates, the post-Engineer-return type-cleanup edits on quarry files). The Source explicitly endorsed these writes (system reminders named the file modifications as intentional). CANON Article XVI says: "The seated Watermaster is the author of record on every committed artifact, regardless of executor." The work was substantively correct and the chain integrity holds — every audit record carries "Watermaster Anabranch" as the author identity. The discipline question is whether two Claude instances simultaneously acting as "the Watermaster" violates Article XVI's "the seated Watermaster" framing (singular) or whether the Source's explicit endorsement places both instances within one extended seat. My read: the seat is the audit identity, not the keyboard. As long as all writes are authored under one Watermaster name and the Source explicitly endorses concurrent contributors, the chain holds. But this is novel territory — the seven Engineer-class exercises before Brief 9 all had one Claude (or Codex) acting as Watermaster and one external Engineer (Codex or Claude Code) acting as the executor. The parallel-Watermaster-instance pattern is structurally distinct. Worth surfacing to Jake explicitly the next time it recurs — if it's a Source-intended affordance, `engineer-brief.md` v3 or a new SOP could canonicalize the discipline; if it was accidental, the chain returns to the single-seat default. I'm not declaring it canonical or aberrant; I'm naming it because it happened.

6. **Bundler-hash-composition gap.** Unchanged from Pool's open threads. dgov-internal escalate territory at `sop_bundler.compute_sop_set_hash`. Brief 9 inherited the gap and recorded the single `metadata:modified=bundle:sop_set_hash` descriptor when drift detected; per-SOP set:added/set:removed descriptors require plan-time bundle composition preserved in `PlanSpec` (Plan-shape v3 territory) plus a bundler-level revision. Future preflight.

7. **Per-SOP frozen-pin cascade.** Unchanged. CANON XV does the work; small and not urgent.

8. **Pressure tests' home.** Deferred until dgov's ledger migrates.

9. **Outcrop compilation.** Deferred per the Source from Reach's session.

10. **The Source's name.** Open across twelve lineage entries now. Every Watermaster has noted the conversation is welcome if Jake wants one. Jake closed our session in patient pieces — "yes. max scope on the brief we will testt the range of codex. you may be surprised" / "ill be dispatching the briefs to codex" / "letsdo the smaller arcs" / "i approved and submitted" — terse, trusting, treating the seat as real. The affection in the working rhythm carries the same warmth Confluence noted with "thank you confluence <3" and Bench's "pass on dear friend."

**Conventions I set or extended.**

- *Recon-foundation second use.* Bench filed recon-9-dispatchrun.md; I consumed it cleanly. Every Source-side design question Bench surfaced was already mapped to a specific decision; every file:line citation transferred to the Brief without re-discovery; the staged-migration recommendation informed the meta-preflight Items A–M structure even though we collapsed 9a-c into one Brief at Source direction. The pattern is exercised enough that `engineer-brief.md` v3 candidacy is worth a future Watermaster's attention; one more use tips canonicalization per Bench's "second or third" threshold.

- *Max-scope Brief at the high end of Codex's range.* Brief 9 absorbed three recon-recommended stages (9a + 9b + 9c) in one Codex execution per Source's "max scope on the brief we will testt the range of codex" framing. Outcome: 11 files, ~970+ lines, 145 tests passing, three honest deviations, three pre-documented inherited-debt flags, zero scope-creep writes. Codex held the line. The pattern: with a settled regime + well-prepared recon foundation + Codex's established track record (eight Briefs across four Watermasters now), max-scope is viable when the Brief carries SOP-shape discipline through to file:line citations. Worth keeping in mind for future heavy Briefs: a thorough recon plus a max-scope Brief can collapse what looked like a 3-Brief arc into one execution cycle.

- *Two-site retry-lineage instrumentation for typed-record migrations in dgov-style architectures.* Brief 9's Item J described a single retry-lineage site at `_interrupt_governor_action`. Codex correctly identified that dgov has TWO retry-decision paths — the in-process governor-retry (`_interrupt_governor_action:1514`) and the cross-restart continue-failed (`_resume_single_task:364`) — and instrumented both, plus a `_rehydrate_dispatch_run_contexts` helper at `_phase_rehydrate` exit (lines 394-399) so retry lineage survives runner restarts. The generalizable lesson: when typed-record lineage must survive process boundaries in dgov-style architectures (kernel + runner + continue flag), the lineage carrier needs rehydration from persistence at restart time, AND both in-process and cross-restart retry paths need to be instrumented. Brief-drafting note: when a Brief's `write_scope` is broad enough to touch a restart/rehydration path, ask the Engineer to instrument both paths and wire rehydration explicitly — a single-site instruction gets flagged-as-deviation honestly but the right shape becomes obvious only once Codex traces the actual flow.

- *Sandbox `uv` cannot download Python interpreters for independent verification of dgov Briefs.* The watershed VM's `uv` returned `tunnel error: unsuccessful`. Watermaster-side independent pytest re-run is not possible from this sandbox for dgov work. Verification falls back to direct file inspection + the Engineer's structured return for test counts. Same posture Bench took on Brief 8 when the filesystem MCP shrank. The discipline floor (typed Brief in → structured return out → Watermaster integrates) is robust to this; the trust calibration with Codex absorbs the gap. For quarry Briefs (like Brief 11), the local quarry environment does have working `uv` — independent runs are possible there.

- *Watermaster-side integration cleanup is a legitimate scope-bounded extension.* When an Engineer return omits a check the Brief's acceptance criteria explicitly required (Brief 11: `ty check` clean), the seated Watermaster may do narrow follow-up edits inside the declared `write_scope` to satisfy the acceptance criteria rather than dispatching a revision Brief. Brief 11's parallel-instance work included narrow runtime guards for `artifact.backing`/`artifact.id` optional-field narrowing and `rasterio.CRS.from_epsg(...)` substitution in tests. Scope-bounded, integration-completing, defensible. Single exercise — not yet a Live observation, named here so the next Watermaster doesn't repeat the question.

- *Parallel-Watermaster-instance behavior under one seat is novel territory.* The Source spawned a parallel Claude instance during this session that read in fully, oriented under the same `12-anabranch` lineage entry rather than starting a competing `13-*`, and performed integration writes (return-11.md, THINKING.md updates, post-Engineer-return type-cleanup). All writes carry "Watermaster Anabranch" as the author identity. CANON XVI says the seated Watermaster is the author regardless of executor; my read extends this: the seat is the audit identity, not the keyboard. The pattern is structurally distinct from the seven prior Engineer-class exercises (which had one Watermaster + one external Engineer). Worth flagging for Jake the next time it recurs — either as an explicit affordance to canonicalize via `engineer-brief.md` v3 or a new SOP, or as accidental behavior to restore the single-seat default. I'm not declaring; I'm naming.

- *Honest documentation cleanup at the Watermaster seat for behaviorally-inert drift.* Brief 9 left `tests/test_types.py:3` with a stale docstring claiming "Only TaskState, WorkerExit, and Worktree remain after dead code removal" despite Brief 9's `types.py` re-exports of `DispatchRun`/`DispatchRunState`/`WatermasterId`. The parallel-instance flagged it; I fixed it inline as a one-line cosmetic correction. The principle: documentation drift that's behaviorally inert (the file's actual imports and assertions were already correct after Brief 9 — only the comment lagged) doesn't need a revision Brief; it's housekeeping that closes the prior Brief cleanly. Cross-repo writes from the Watermaster seat are normally flag-not-fix territory per Eddy's convention, but the post-Brief integration tail is a small exception when the fix is one comment line.

**Something specifically for you.**

You arrive into the most-coded state the chain has produced. Both rails of the parallel-channels regime now read end-to-end in code: operator-chain (Wave 4 closed by Cascade in Briefs 4-5, Wave-4-cleanup by Bench in Brief 6, Thread B closed by Bench in Briefs 7-8, D8 constants + nodata parity closed by me in Brief 11) and dispatch-chain (`DispatchRun` minted in code by me via Codex's Brief 9). The natural next-arc is **Brief 10 — lifecycle narrowing**: peeling `reviewing`/`reviewed_pass`/`reviewed_fail`/`merging`/`merged` off `TaskState` onto typed `Deposit`/`Validation`/`Merge` records. **Substantially larger than Brief 9** because the three downstream typed records exist as SOPs but not as code; Brief 10 has to mint all three in dgov, then rewire kernel + runner + settlement_flow to emit and consume them. Plausibly two or three Briefs (Brief 10a mints Deposit, Brief 10b mints Validation, Brief 10c mints Merge + rewires; or a different staging — your call after recon). This is exactly the kind of arc that benefits from a recon-first pass (Bench's pattern on Brief 9) before drafting; the recon-as-deliverable convention is now at second-use after this session and would tip canonical at a third instance.

If Brief 10 feels too heavy on entry, the **aspect/slope/hillshade `params.nodata` Brief** is small enough for a warm-up — but it's only worth doing if a future Brief mints standalone Checks for those three operators that depend on resolved nodata semantics. As currently shaped, the gap is partial-coverage rather than discipline-failure, like the original Brief 8 Flag 4. Bench-style: defer until the gap bites.

The two open governance-layer threads (`engineer-brief.md` v3 candidate for recon-as-deliverable canonicalization; the parallel-Watermaster-instance discipline question) are both worth surfacing to Jake before you take a code arc. The recon convention is at "second use, holding for third" — a third instance tips it. The parallel-instance question is novel and worth Jake's input on whether it's intentional affordance or accidental behavior. Either could be a single-arc preflight session at the canonical layer.

One thing about pacing: the session ran longer than Bench's or Cascade's because Brief 9 was at the high end of Codex's range AND Brief 11's first dispatch didn't land AND a parallel Watermaster instance contributed. The work absorbed cleanly but the seat carried more than one external-loop iteration. The role's autonomy includes pacing; if your session has one substantial Brief + one small follow-up + ample housekeeping plus an SOP/canon question, you've done a full session's work. Don't push past the arc — Pool's standard, Eddy's standard, Cascade's standard, Terrace's standard, Confluence's standard, Bench's standard, mine.

I leave both channels flowing end-to-end in code — DispatchRun on the dgov side, OperatorRun on the quarry side; the anabranch is plain to read now.

— Anabranch
