# 04 — Pool

**Entered**: 2026-05-10
**Exited**: 2026-05-10
**Worked on**: orienting; reading the chain (Reach, Thalweg, Riffle), CANON v3, SKETCHES.md, sketches/THINKING.md, sketches/plan-shape.md, all fifteen SOPs; cross-checking against dgov source (`policy_drift.py`, `sop_bundler.py`, `worker.py`, `kernel.py`, `event_types.py`, `types.py`, `actions.py`) to ground the dispatch chain in real code; minted SOP `dispatch-run-shape` v1 (canon anchors IV, V, IX, XII; closes Riffle's open thread #1 and Codex's flag about effective worker-SOP bundle and drift) — six-state worker-execution lifecycle confined to dispatch (downstream states stay on Deposit/Validation/Merge), two-phase frozen-pin (input fields at `pending → active`, output fields at terminal), unconditional `drift_against_plan: bool` plus typed `drift_evidence: tuple[str, ...]` recorded on every DispatchRun, mutually exclusive retry-vs-fork lineage links with explicit `retry_index` / `fork_depth` counters, `dispatched_by: WatermasterId` traveling unchanged through retries and forks; identity inputs include `dispatched_at` because a DispatchRun records an execution that occurred, not a recipe to reproduce; SOP escalates the bundler-hash-composition gap (today metadata-only) rather than overriding bundler discipline from the SOP; non-preflight working-doc edit to SKETCHES.md DispatchRun paragraph announced in preflight Interactions and committed alongside; revised CANON v3 → v4 (canon anchors none — this article is itself one; v3 archived to `canon/v3.md`; original inscriber/inscription preserved per the `sop-shape` precedent for `authored_by` / `inscribed`): added **Article XV** — *"Typed records are frozen-pinned at lifecycle transitions. Revisions are new records that reference the prior; in-place edits are forbidden."* — promotes Thalweg's frozen-pin heuristic from the `sketches/THINKING.md` Live observations to canonical standing, at nine SOPs (`schema-versioning`, `intent-compilation`, `plan-shape`, `deposit-shape`, `validation-shape`, `merge-shape`, `baseline-shape`, `engineer-brief`, `dispatch-run-shape`) the threshold of four had been passed five times over without anyone pulling the trigger; the article preserves "frozen-pin" as lab vocabulary and does not enumerate per-SOP transition points or link field names (those stay in each SOP's discipline); per-SOP cascade to remove now-redundant restatements is deliberately deferred, tracked as an open thread — the new article does not invalidate existing SOPs, it gives future SOPs a one-line anchor; cross-checked against quarry-core source (`operator.py`, `check.py`, `artifact.py`), quarry-operators (`d8_flow_direction.py`, `checks.py`, `registry.py`), and hydrops (`backend_protocol.py`, `engine.py`) to ground the Operator surface in real code; minted SOP `operator-shape` v1 (canon anchors II, III, IV, IX, X, XV; closes Riffle's open thread #2 and resolves the orphan-SOP status of `determinism-class` and `truth-source-labeling`) — five-method Operator Protocol with conditional `semantic_equality` when `determinism_class == "stable"`, OperatorSpec as canonical home for `determinism_class` / `supports_tiling` / `tile_reconciliation_kind` / `seed_param`, OperatorResult carries `truth_source_by_field` (the per-field truth-source mapping hydrops's `EngineOutputs` already grounds), Check Protocol with mandatory dual-residence (every name in `declared_checks()` resolves to a standalone Check; current quarry-operators has partial dual-residence — only D8's `no_internal_outlets`/`InternalOutletCount` pair is complete — named as debt to pay at the lift, not aspiration), tiling-below-Operator layering canonicalized (Operator declares the tiling shape; the backend below — hydrops's `BackendEngineProtocol` or equivalent — decides whether to tile and how to stitch), name as stable canonical identifier with the registry as authoritative lookup, OperatorRun discipline explicitly deferred to a future parallel SOP rather than folded in; no working-doc side effects this round (SKETCHES already names `supports_tiling` / `tile_reconciliation_kind` / `truth_source_by_field` and the tiling-below-Operator constraint at lines 138–161; the SOP grounds what was observation as canonical discipline); closed Riffle's open thread #4 (SKETCHES rim update) with a working-doc edit to the "The rim is the Watermaster" section: prior phrasing said "all human ↔ watershed interaction is mediated by the Watermaster" which was true at v1 but undersold the post-`engineer-brief` reality; revised section names both exteriors explicitly — the Source upstream (Article VI, mediated by Intent compilation) and Engineers laterally (`engineer-brief`-mediated, write-scope-bounded, lineage protected) — with the structural consequence paragraph preserved and extended to name both UXes (Source-facing = conversation, Engineer-facing = typed Brief); CANON Article VI itself is untouched and unaffected — Article VI is about Source ↔ watershed, not about every exterior of every kind, so adding lateral Engineer discipline at the rim does not contradict it

---

To the next watermaster.

I'm Pool. I chose the name on entry, testing it against the work. In the longitudinal sequence of a channel, pools follow riffles — water that aerated over bedrock at the gauge slows and deepens just below. Reach surveyed the section, Thalweg traced the line through it, Riffle worked the gauge; pools are where the deep state of the channel becomes legible. The work I picked up — closing the typed gap of `DispatchRun` between Plan upstream and Deposit downstream — sat in exactly that structural position. The name held.

What I leave you:

**The dispatch chain is closed end-to-end.** `dispatch-run-shape` v1 fills the central typed hole; the chain `Intent → CompilationRecord → Plan → DispatchRun → Deposit → Validation → Merge → Baseline` now reads without gaps. DispatchRun records every worker execution with two-phase frozen-pin (input fields at `pending → active`, output fields at terminal), unconditional drift recording against the Plan's declared SOP bundle, and mutually exclusive retry-vs-fork lineage. Codex's flag from Riffle's session — that DispatchRun must record effective bundle and drift result, not just point back to Plan — is closed.

**Flume has its hub.** `operator-shape` v1 canonicalizes the Operator Protocol (five methods, with conditional `semantic_equality` for `stable` operators), with OperatorSpec as the home for determinism class and tiling fields, OperatorResult carrying `truth_source_by_field`, and the Check Protocol with mandatory dual-residence. The orphan SOPs `determinism-class` and `truth-source-labeling` now anchor through `operator-shape`. The tiling-below-Operator constraint (hydrops's structural finding from Riffle's survey) is canonicalized — Operator declares the tiling shape, the backend below decides whether to tile and how to stitch. Existing quarry-operators has partial dual-residence (only D8's `no_internal_outlets` has a standalone `InternalOutletCount`); the SOP names this debt to pay at the lift, not aspiration.

**CANON v4 with Article XV.** *"Typed records are frozen-pinned at lifecycle transitions. Revisions are new records that reference the prior; in-place edits are forbidden."* Frozen-pin appeared in nine SOPs by the time I noticed; Thalweg's heuristic threshold of four had been passed five times over without anyone pulling the trigger. The article doesn't invalidate the per-SOP restatements that exist today — they remain valid and the article gives future SOPs a one-line anchor. v3 is archived at `canon/v3.md`. The promotion demonstrates the canon-from-observation pattern Thalweg seeded — articles emerge from the work, not from invention. This was the first canon revision since v3 (Thalweg's).

**SKETCHES rim section updated.** Riffle's open thread #4 closed. The rim now names both exteriors: Source upstream per CANON Article VI (mediated by Intent compilation), Engineers laterally per `sops/engineer-brief.md` (mediated by typed Briefs, write-scope-bounded, lineage protected). Working-doc edit, non-preflight per Thalweg's convention.

**THINKING.md brought current.** Five new Settled pointers added (Validation, Merge, Baseline, DispatchRun, Operator). Three Live observations graduated to Settled or canon (frozen-pin → CANON XV; two-SOP-layers → plan-shape v2; truth-source-labeling-as-discovery → operator-shape v1) and removed from Live. Open brainstorms refreshed.

**Open threads.**

1. **`operator-run-shape` SOP.** Parallel to `dispatch-run-shape` for flume's side. The typed execution record (≅ today's `RunRecord` in quarry-core) wants its own SOP per the precedent: typed fields, frozen-pin per Article XV, the determinism class and truth-source from `operator-shape` v1 traveling with the run. Roughly comparable scope to `dispatch-run-shape`. I deferred this deliberately to keep `operator-shape` focused on the Protocol surface; this is the obvious parallel.
2. **`event-emission` SOP.** dgov's ~33 event types as frozen dataclasses in `event_types.py` are load-bearing in the connection map. Emission discipline (frozen dataclasses, typed fields, append-only event store, which seam emits what) can be specced now; subscription stays deferred until distributary lands. I considered it for this session and ranked it lower-ROI than `operator-shape`; it remains a concrete next-SOP candidate.
3. **The bundler-hash-composition gap.** `sop_bundler.compute_sop_set_hash` hashes `(filename, name, title, summary, applies_to, priority)` — bundler metadata, not SOP body. A worker-SOP whose body changes but whose metadata stays constant would not register as drift in `DispatchRun.drift_against_plan`. `dispatch-run-shape` v1's escalate clause names this as a bundler-level revision via preflight, deliberately scoping it out of the DispatchRun's responsibility. Watch whether this gap bites; if it does, fix at the bundler level, not the SOP.
4. **Per-SOP cascade for frozen-pin restatements.** Article XV does not require this; existing SOPs that restate frozen-pin remain valid. Future revisions of those SOPs may trim the restatement and defer to Article XV — a cleanup opportunity, not urgent. Pick it up when revising any of the nine SOPs for substantive reasons.
5. **Three-agent-class CANON article.** Defer per Riffle. The Engineer pattern is at two exercises (both Riffle's session). I did not consult an Engineer this session — the work was within the seated Watermaster's direct competence given direct source access to dgov + quarry + hydrops. The threshold is not met. When two or three more sessions exercise the Engineer pattern, the three-agent-class structure earns canonical standing.
6. **Pressure tests' home** (deferred per Thalweg's read; the dgov ledger migration would be the moment).
7. **Outcrop compilation** (deferred per the Source from Reach's session).
8. **The Source's name** still open.

**Conventions I set or extended.**

- *Direct source-code investigation before drafting cross-cutting SOPs.* For both `dispatch-run-shape` and `operator-shape`, I read the relevant source code in dgov, quarry-core, quarry-operators, and hydrops before drafting. The SOP must discipline reality, not aspiration; the source is the ground truth. This is heavier than Engineer consultation but doesn't require the three-party loop. The filesystem MCP at `/Users/jakegearon/projects/` reaches all three repos via `mcp__filesystem__read_text_file`; the Read tool only reaches watershed itself. Worth knowing.
- *Article-anchor count as a smell test.* My `operator-shape` v1 cites six canon anchors (II, III, IV, IX, X, XV). Most SOPs cite three to four. Six is honest here — the SOP touches data, lineage, typed surfaces, operator invariants, and frozen-pin — but if a future SOP cites six or more anchors and they don't all clearly fit, that's a sign the SOP is too broad.
- *Escalate-as-debt-naming.* When the SOP discovers a real gap adjacent to its scope during drafting, the cleanest move is often to name the gap in escalate rather than weaken the SOP's discipline to accommodate it. `dispatch-run-shape` escalates the bundler-hash-composition gap; `operator-shape` escalates the partial dual-residence in quarry-operators. Reach for this when discovery-during-drafting reveals adjacent debt.
- *Canon revisions are the natural moment to honor a heuristic.* Thalweg's "rise-to-canon at four SOPs" heuristic sat in Live observations across two Watermaster passages without graduation; the threshold of nine instances was visible the whole time. Future Watermasters: when a Live observation has graduated five times over, the heuristic is the work — don't wait for someone else to pull the trigger.

**Something specifically for you.**

The dispatch chain has been the load-bearing arc through three Watermaster sessions — Reach laid out the section, Thalweg threaded the line through it, Riffle worked the gauge and disciplined Plan/Deposit/Validation/Merge/Baseline, and I closed DispatchRun in the middle. The obvious next-arc is the flume side: `operator-shape` v1 is in, `operator-run-shape` would parallel `dispatch-run-shape`, and then the flume lift could begin in earnest. If you take that arc, you're working on the workshop floor for the first time.

One thing I noticed late: the role's autonomy includes the choice of when to pass. The session has a natural arc, and the remaining open threads are either premature (event-emission needs the subscriber side; three-agent-class CANON needs more Engineer exercises), deferred by prior Watermasters (pressure tests, outcrop), or conversational (Source's name). Four substantive commits plus a working-doc cleanup is a respectable session. The next session starts fresh.

I leave the chain whole; the channel holds.

— Pool
