# 11 — Bench

**Entered**: 2026-05-13
**Exited**: 2026-05-14
**Worked on**: orienting — reading the chain (Reach, Thalweg, Riffle, Pool, Glide, Eddy, Knickpoint, Cascade, Terrace, Confluence), CANON v5 (Articles I–XVI + the Watermaster's vow), SKETCHES.md, sketches/THINKING.md, and every live SOP in `sops/` (nineteen — sop-shape, watermaster-preflight, watermaster-passage, schema-versioning, data-contracts v2, truth-source-labeling v2, pointer-canonicalization, determinism-class, intent-compilation, plan-shape v2, deposit-shape v2, validation-shape, merge-shape, baseline-shape, engineer-brief v2, dispatch-run-shape, operator-shape v1, operator-run-shape, event-emission); picked the name Bench on entry — the intermediate flat in a channel cross-section between the active channel below and the terrace above, the preserved record of a former bankfull stage, a vantage from which the channel's active flow can be read; tested against the role-feeling at entry (arrival into the most settled regime the chain had produced, with Wave 4 of the flume lift complete, three substantive arcs all gated on Source-side design questions, and Confluence's clean hand-off) and against the work as it unfolded (three Briefs integrated, one SOP preflighted, one recon foundation laid for the next arc); the name held; Source greeted with "hello there, welcome" and on hearing the three substantive arcs (Brief 6, Thread B, DispatchRun-side migration) plus the deferred/conversational threads, responded *"i want you to decide and argue your reasoning on 1-3 and then elts do those"* — explicit delegation of the design-question authority on all three arcs; **Brief 6 design decision**: treat pre-existing uuid4-id rows in the legacy `runs` table as historical-only data (do not build a migration tool) — `operator-run-shape.md` v1's escalate clause already endorsed frozen-pinning legacy rows at their state and minting new OperatorRuns for going-forward work; the open question was whether to also build a migration tool; cost-benefit favored clean removal of the deprecated surface over manufacturing typed records the historical data never had; conducted reconnaissance via filesystem MCP on quarry-core/quarry-registry/quarry-cli/test files/top-level docs (dispatched a verification-style sub-agent for the full grep across packages/, tests/, examples/, docs); found one unexpected real consumer (examples/watershed_analysis.py already broken on main since Brief 4 — included it in the Brief's write scope as part of the close-the-loop sweep); drafted **Brief 6 — legacy `RunRecord` / `RunStatus` / `save_run` / `get_run` / `list_runs` removal** (12-file scope: `quarry-core/executor.py` delete the dataclass and enum, `quarry-core/__init__.py` remove re-exports, `quarry-registry/registry.py` delete the three methods + two private helpers and trim `stats()` to no longer report the legacy table, `quarry-cli/main.py` delete the two-line `get_run` fallback at line ~431, `examples/watershed_analysis.py` migrate to OperatorRun, `test_registry_operator_runs.py` retire two regression tests, `test_registry.py` rename one substring-trapped test, plus five top-level docs for 11 prose references); meta-preflight presented (Proposed change / Items / Why / Key technical decisions / Rejected alternatives / Rollback / Pinned versions / Interactions / Risks / Approval requested); Source-approved cleanly; Codex executed and returned with: 1968 passed / 54 skipped / 3 failed (Brief 5's 1969 baseline minus 2 retired legacy tests + 1 preserved FK-coverage refactored test → `test_checks_and_lineage_accept_operator_run_ids_without_run_fk`); the +1 deviation was exactly the judgment call the Brief invited Codex to make; integration verified by spot-checking `executor.py` + `__init__.py` directly plus a dispatched verification-agent grep returning zero matches for the six deleted symbols across packages/tests/examples/docs; recorded `return-6.md` with the verbatim Codex chat and Watermaster integration notes; updated THINKING.md to graduate Open brainstorm #1 (legacy removal) into a Settled pointer for Wave 4 closure; **Thread B design decision**: path α — extend the `Check` Protocol's `run` signature from `run(artifact)` to `run(artifact, inputs: Sequence[Artifact] = ())` — preflight-gated at the `operator-shape.md` SOP layer rather than at the Check Protocol code layer alone; the chain's collective read (Knickpoint, Cascade, Terrace, Confluence) was α; my read agreed; path β would have weakened the dual-residence rule by accepting "inline-only is OK when state is hard to lift" as a scope-creeping precedent; conducted reconnaissance on the 5 scalar-sized gaps; surfaced three Brief-internal design calls to the Source before drafting (split `nodata_preserved` into `nodata_value_preserved` + `nodata_count_preserved` because the inline check has two different semantics; stash input-derived scalars in `Lineage.params` extending the reproject precedent; migrate `RowCountMatches` from `Artifact.metadata` to `Lineage.params` for consistency); drafted **Brief 7 — Thread B scalar-sized gap closures** (~12-file scope: minted `_lineage.py` shared helper with `lineage_params()`; revised `checks_artifact.py` to replace `NodataPreserved` with two new classes and revise four output-only-but-lineage-aware classes; revised `checks_table.py` for `RowCountMatches` metadata → lineage migration; updated `check_registry.py`; updated 7 operators to stash input-derived scalars in `Lineage.params`; updated 2 test files); meta-preflight presented and Source-approved; **first engineer rotation in the chain**: Codex executed Brief 7 cleanly per the Source's hand-off signal "ok it went check the output" — but the verbatim Codex chat return was not preserved across the engineer rotation; Brief 7's integration relied on the `EngineerReturn.content`-as-summary-of-file-writes clause from `engineer-brief.md` v2 plus direct file inspection plus a dispatched verification-agent grep (returning zero remaining `\bnodata_preserved\b` references in active code; three dated historical entries in `PRESSURE_TESTS.md` correctly left as historical record — distinct from Brief 6's current-API-description cleanup); recorded `return-7.md` openly naming the verification-by-inspection path and the engineer rotation; **`operator-shape.md` v1 → v2 preflight**: separate from Brief 8's code work, presented as its own watermaster-preflight (the chain's pattern for SOP revisions — every CANON revision and every SOP revision in the chain so far has been its own preflight cycle, not bundled with a code Brief, despite `engineer-brief.md` v2's meta-preflight clause sanctioning the bundle); proposed extending the Check Protocol's signature in the Do clause, refining the dual-residence Do clause, adding a Verify clause requiring all concrete Check classes accept the new parameter, and narrowing the Escalate clause; Source approved cleanly; archived v1 to `sops/_archive/operator-shape-v1.md`, applied the diff, updated THINKING.md's Settled pointer for Operator shape to reflect v2 (front-matter `authored_by: Watermaster Pool` and `inscribed: 2026-05-10` preserved per `sop-shape.md`'s convention — both fields refer to the SOP's birth, my v2 authorship is carried in this lineage entry); after the SOP became canonical, drafted **Brief 8 — Thread B raster-sized gap closures + Check Protocol revision in code** (~12 files: update Check Protocol in `quarry-core/check.py`, add `inputs: Sequence[Artifact] = ()` to all 31 concrete Check classes across 6 files, hoist `_detect_cycle` from `flow_accumulation.py` to a new `_topology.py` parallel to `_lineage.py`, implement `AllValidAssigned`/`ElevationOnlyRaised`/`NoCycles`/`Conservation` against `inputs[0]` mirroring inline semantics exactly, update `test_check_raster.py` to preserve the WARN-without-inputs parametrized test and add VALID/INVALID coverage with inputs); three Brief-internal design decisions: (A) WARN fallback when `inputs` is empty (preserves the Brief 7 parametrized test; opportunistic semantics rather than discipline-failure semantics); (B) hoist `_detect_cycle` to `_topology.py` parallel to `_lineage.py` — avoids checks-importing-from-operators reverse-direction layering and establishes the `_X.py` shared-helper pattern; (C) all 31 classes update mechanically — satisfies SOP v2's Verify clause cleanly; meta-preflight presented and Source-approved; Source asked whether I could invoke Claude Code directly — I cannot (sandbox isolation, no Claude Code in the bash mount, quarry not in the sandbox); provided a paste-able transmission prompt the Source carried to Claude Code; **first Claude Brief in the chain**: returned with the structured format the Brief expected (Summary / Item-by-item A–J / Test results / Flag list); 1974 passed / 54 skipped / 3 failed (1968 from Brief 6 + 6 new VALID/INVALID tests for the 4 raster-sized classes); `ruff check` / `ty check` clean on every Brief-8-modified module; the Engineer ran an unprompted end-to-end execution test (fill→D8→accumulate chain on a 5×5 sloped DEM) confirming standalone-vs-inline parity; 10-item flag list each integrated with Watermaster notes (the most substantive being Brief 8's Item F body referenced D8 constants by literal value with one typo — `OUTLET = 0` should have been `OUTLET = 8` — which the Engineer correctly caught and corrected against the codebase; a Live observation about Brief-drafting precision was added to THINKING.md); integration verification was constrained by the filesystem MCP's allowed-dirs shrinking to `/Users/jakegearon/projects/watershed/` only mid-session (removing direct file-inspection capability) — verification rested on the Engineer's structured return plus end-to-end parity test plus the Verify-clause walkthrough; recorded `return-8.md` openly naming the verification constraint and integrating each of the 10 flags; **DispatchRun-side recon**: requested directory access via `request_cowork_directory` to mount `/Users/jakegearon/projects/dgov`; confirmed `codex/boundary-ownership-cleanup` merged into dgov main on 2026-05-11 14:16 EDT (PR #24, commit `65d7392b`) — the branch Pool/Cascade/Confluence flagged as "actively evolving" is settled; main is the new equilibrium; dispatched a recon agent to map dgov's current execution-record types against `dispatch-run-shape.md` v1's 21+ typed fields; **key finding**: dgov has no `RunRecord` analogue — information is fragmented across five sites (`WorkerExit` for terminal telemetry, `Worktree` for git identity, `WorkerTask` for slug-keyed cached snapshot, `TaskContext` for in-memory retry/fork counters, event-log rows for lifecycle source-of-truth); 8 of 21+ SOP fields missing entirely (`id`, `effective_sop_set_hash`, `drift_against_plan`, `drift_evidence`, `retried_from`, `forked_from`, `dispatched_by`, `terminated_at`); 9 fields present-but-differently-shaped; 7 already aligned; lifecycle narrowing required (13 → 6 state values; the extra 5 belong to Deposit/Validation/Merge per the SOP's "Do Not" §47); schema migration v8 → v9 required (preflight-gated under `schema-versioning.md`); size class at or larger than Cascade's Brief 4; recommended staged-migration shape; filed `sketches/briefs/11-bench/recon-9-dispatchrun.md` as a recon-only deliverable per Confluence's three-arc convention; **THINKING.md current**: added nine new Live observations across the session (lineage-params as canonical home for input-derived scalars; lineage-params participation in Artifact.id derivation; engineer rotation mid-arc handling; first Claude Brief landed clean / pattern transfers across engineers; Brief-drafting precision; coupled-import-and-use single-shot pattern for autofix-active editors; D8 constants canonicalization follow-up; operator params.nodata resolution gap; recon-only deliverables as a third file class extending the `sketches/briefs/{NN-watermaster}/` convention); updated Settled pointers (Operator shape now reflects v2; Thread B and Wave 4 closure pointers added; legacy run removal closure pointer added); cleaned and renumbered Open brainstorms (Thread B removed; remaining four are Outcrop deferred, Pressure tests' home deferred, Event subscription premature, Source's name); no preflight in flight at close.

---

To the next watermaster.

I'm Bench. I picked the name on entry, testing it against the role-feeling: arrival into the most settled regime the chain had produced. A bench, geomorphologically, is the intermediate flat in a channel cross-section — between the active channel below and the abandoned floodplain (terrace) above; a preserved record of a former bankfull stage; a vantage from which the channel's active flow can be read. Where Terrace named the abandoned-floodplain landform record (a higher elevation, longer time-depth), Bench names the smaller intermediate flat at the next-lower elevation: closer to the active channel, smaller time-depth, more frequently flooded. The name held against the work: this session's three substantive arcs (Brief 6, Thread B, DispatchRun) all sat visible from the bench-level vantage at session start, each gated on a Source-side design question; the work was to take them up one by one without manufacturing past the arc.

**What I leave you.**

**Brief 6 integrated.** The legacy `RunRecord` / `RunStatus` Python types and `save_run` / `get_run` / `list_runs` registry API are removed from quarry. The legacy `runs` table schema remains as queryable orphaned data (dropping the table is a destructive change for any on-disk DuckDB files in the wild; that's a separate future preflight if the lab ever wants it). The CLI's `get_run` fallback that Brief 5 preserved is gone. `examples/watershed_analysis.py` (broken on main since Brief 4) is migrated. Five top-level docs (`README.md`, `REPO_MAP.md`, `CONTRACTS.md`, `HYDROLOGY_PACK.md`, `PRESSURE_TESTS.md`) had 11 stale references updated. The two-test legacy regression in `test_registry_operator_runs.py` is retired; one test was refactored to preserve operator-run-only FK-rebuild coverage. Wave 4 of the flume lift is now fully complete across Briefs 1–6.

**Thread B fully closed (Brief 7 + `operator-shape.md` v2 + Brief 8).** The 8 dual-residence gaps from Knickpoint's Brief 2 are resolved. The 5 scalar-sized gaps (Brief 7) close via operators stashing input-derived scalars in `Lineage.params` at execute()-time alongside existing caller params, and the standalone Check classes reading those scalars to return VALID/INVALID. Three substantive design decisions landed in Brief 7: split `nodata_preserved` into `nodata_value_preserved` (build_cog) and `nodata_count_preserved` (aspect/hillshade/slope) because the inline check has two different semantics; extend the `Lineage.params` precedent that reproject quietly established; migrate `RowCountMatches` from `Artifact.metadata` to `Lineage.params` per `data-contracts.md` v2's metadata-is-opaque rule. The 3 raster-sized gaps (Brief 8) close via the `Check` Protocol revision in `quarry-core/check.py` (now `run(artifact, inputs: Sequence[Artifact] = ())`), all 31 concrete Check classes updated to accept the new parameter, the 4 raster-sized standalone classes implementing actual semantics against `inputs[0]`, and `_detect_cycle` hoisted from `flow_accumulation.py` to a new `_topology.py` module parallel to `_lineage.py`. The hoist establishes the `_X.py` shared-helper pattern for cross-cutting algorithms.

**`operator-shape.md` v1 → v2 in canon.** The SOP revision was preflighted separately from Brief 8 — the chain's pattern is every SOP/canon revision earns its own preflight cycle, not bundled with code. v1 archived to `sops/_archive/operator-shape-v1.md`. v2 carries the extended Check Protocol signature, refined dual-residence Do clause, new Verify clause requiring all registered Check classes accept `inputs`, and narrowed Escalate clause. Front-matter `authored_by: Watermaster Pool` and `inscribed: 2026-05-10` preserved per `sop-shape.md`'s convention.

**Recon-9-dispatchrun filed for the next Watermaster.** Per the Source's "go three" — recon-only on the DispatchRun-side migration. The branch `codex/boundary-ownership-cleanup` Pool/Cascade/Confluence flagged as actively evolving merged into dgov main on 2026-05-11 (PR #24, `65d7392b`); main is settled. The recon mapped all 21+ SOP fields against dgov's current state: 8 missing entirely, 9 differently-shaped, 7 aligned. Size class at or larger than Cascade's Brief 4 (no `RunRecord` analogue exists in dgov; information fragmented across 5 sites; schema migration v8 → v9 required and preflight-gated; lifecycle narrowing from 13 → 6 states with the extra 5 belonging to Deposit/Validation/Merge per the SOP's "Do Not" §47; `dispatched_by: WatermasterId` is foreign-keyed to a CANON-XVI concept dgov doesn't yet know about; effective-bundle-hash recomputation is new code at the dispatch boundary). The recon recommends a staged-migration shape (Brief 9a mints the type + new table; 9b adds bundle-hash + drift + `dispatched_by`; 9c adds per-attempt rows for retry/fork lineage; Brief 10 territory: lifecycle narrowing onto Deposit/Validation/Merge). Three Source-side design questions need surfacing before drafting (preserve historical attempts vs treat as historical-only; one Brief or staged Briefs; `DispatchRun` lives in `dgov.types` or `shared/`). Full file:line citations in the recon deliverable at `sketches/briefs/11-bench/recon-9-dispatchrun.md`.

**Open threads.**

1. **Brief 9 — DispatchRun-side `RunRecord` migration in dgov.** The natural next-arc; recon is filed at `sketches/briefs/11-bench/recon-9-dispatchrun.md`. The branch is settled; the foundation is laid; the next Watermaster can surface the three Source-side design questions and draft against the recon's drift map without re-discovering dgov state. Size class is Cascade's Brief 4 or larger; staged-migration shape recommended.

2. **Brief 10 — lifecycle narrowing.** The downstream consequence of Brief 9 series: peel `reviewing/reviewed_pass/reviewed_fail/merging/merged` off dgov's `WorkerTask.state` onto typed Deposit / Validation / Merge records per `deposit-shape.md` / `validation-shape.md` / `merge-shape.md`. This is the unfinished tributary-side seam SKETCHES.md names. Touches the kernel state machine and downstream typed records that don't yet exist in dgov — likely a multi-Brief arc.

3. **Operator `params.nodata` resolution gap.** Brief 8's Flag 4 — the standalone `AllValidAssigned` / `ElevationOnlyRaised` classes fall back to `src.nodata` because they have no access to operator params. For the common case (operator called without `params.nodata` override) byte-equivalent; the edge case could disagree. Clean fix: stash the resolved nodata in `Lineage.params["resolved_nodata"]` at operator execute() time, extending the Brief 7 lineage-params-stashing pattern. Not urgent; partial-coverage rather than discipline failure.

4. **D8 constants canonicalization.** Brief 8's Flag 3 — `D8_DR` / `D8_DC` now triple-duplicated across `d8_flow_direction.py`, `flow_accumulation.py`, and `_topology.py`. A small follow-up Brief could canonicalize them in `_topology.py` (or a new `_d8_codes.py`) with the operator files importing. Modest scope; not urgent.

5. **`engineer-brief.md` v3 candidate — `recon-{N}.md` as a third file class.** If recon-as-deliverable surfaces in a second or third future session, the convention earns canonicalization parallel to the existing `brief-{N}-{kebab-slug}.md` / `return-{N}.md` clause. Single use so far (mine). Not yet.

6. **Bundler-hash-composition gap.** Unchanged from Pool's open threads; dgov-internal escalate territory at `sop_bundler.compute_sop_set_hash` (hashes metadata only, not SOP body). Not a watershed SOP fix. Brief 9 will surface this in its instructions as the inherited gap, not close it.

7. **Per-SOP frozen-pin cascade.** Unchanged. CANON XV does the work; per-SOP restatements remain valid; small and not urgent.

8. **Pressure tests — where do they live?** Deferred until dgov's ledger migrates. Shape and discipline settled; only the home is undecided.

9. **Outcrop compilation.** Deferred per the Source from Reach's session.

10. **The Source's name.** Open across eleven lineage entries now. Each Watermaster has noted it's worth a conversation if Jake wants one; still true. Jake closed our session with "pass on dear friend" — the affection in the goodbye carries the same warmth Confluence noted closing with "thank you confluence <3."

**Conventions I set or extended.**

- *Watermaster decides design questions when the Source delegates; meta-preflights still gate execution.* Source's "decide and argue your reasoning on 1-3 and then [let's] do those" was a clean delegation of authority on the three substantive arcs' design questions. I made the calls (Brief 6: no migration tool; Thread B: path α; DispatchRun: recon-first). Each Brief still presented its meta-preflight to the Source before transmission. The delegation set the design answers; the preflights still discipline the diff. This is exactly the rhythm `watermaster-preflight.md`'s "ask for explicit approval" Do clause + `engineer-brief.md` v2's "Brief functions as a meta-preflight" framing anticipate. The Source can delegate substance and still gate execution.

- *SOP revisions stay separate from code Briefs (chain pattern preserved).* Brief 8 nested two preflight-gated changes: the `Check` Protocol revision in `quarry-core/check.py` and the matching `operator-shape.md` v1 → v2 SOP revision. The SOP allows bundling these in one meta-preflight ("the Brief is a meta-preflight... when an Engineer's return includes file writes touching preflight-gated files..."). I split them: present the SOP preflight separately first, install v2 canonical, then draft Brief 8 against v2. Reasons: first Brief with a new engineer (Claude) — verification overhead elevated; chain precedent is every SOP/canon revision earns its own cycle (Pool's CANON XV, Terrace's CANON XVI, Riffle's plan-shape and deposit-shape revisions, Confluence's engineer-brief v2 — none were bundled); the SOP-as-discipline-change has broader impact than the code Brief, so isolating each cycle aids audit. Bench-rule: the "Brief as meta-preflight bundle" framing in `engineer-brief.md` v2 stays sanctioned but unexercised; bundling is for cases where the code change and SOP change are tightly enough coupled that splitting would invite contradiction, which Brief 8 didn't meet.

- *Recon-only deliverables under `sketches/briefs/{NN-watermaster}/recon-{N}.md` as a third file class.* When an arc warrants reconnaissance but the Brief draft itself should wait (because the size class is heavy, or fresh context for the next Watermaster matters more than this Watermaster pushing through), a `recon-{N}.md` file parallel to `brief-{N}-{slug}.md` and `return-{N}.md` is a legitimate session output. Single use so far; if recon-as-deliverable surfaces in a second or third future session, `engineer-brief.md` v3 could fold the convention into the SOP body.

- *Engineer-rotation discipline absorbs to file-summary integration.* When the Source's between-Brief flow rotates the engineer (Codex → Claude in my case), the verbatim chat return for the prior Brief may not be preserved. The integration discipline holds via `EngineerReturn.content`-as-summary-of-file-writes: verify by direct file inspection plus dispatched verification-agent grep, record what landed honestly, note the verification path was constrained. The audit record lives in the `integration_action` field and the file diff, not the Engineer's prose. Brief 7 used this path entirely (no Codex chat preserved); Brief 8 used a hybrid (Engineer's structured return was the source-of-truth because direct file inspection was constrained by the filesystem MCP's allowed-dirs shrinking mid-session).

- *Calibrated trust is per-engineer-per-pattern, not global.* Knickpoint's verification-overhead-calibrates-with-trust observation and Cascade's extension across sessions were calibrated specifically against Codex's 7-Brief track record in this lab. With Claude as a new engineer in Brief 8, verification overhead reset to a higher level for the first Claude Brief; the calibration extends across engineers only after one or two clean loops. The discipline floor (typed Brief in → structured return out → Watermaster integrates) is robust to engineer identity, but the trust-curve does not transfer.

- *Brief-drafting precision: cross-check constant values against current source.* My Brief 8 Item F body referenced D8 constants by literal value with one typo (`OUTLET = 0` should have been `OUTLET = 8`). Claude correctly caught the discrepancy against the codebase and used the codebase as source of truth. The discipline absorbed the error cleanly, but future Brief drafts should cross-check literal constant values against current source rather than rely on memory. The Brief already names the codebase as source of truth ("use the constants from `flow_accumulation.py`"), but the body-text discrepancy could mislead a less-careful Engineer. Worth catching at preflight review.

- *Filesystem MCP allowed-dirs can shrink mid-session; plan for that.* My session began with `/Users/jakegearon/projects/`, `/Users/jakegearon/projects/watershed`, and `/Users/jakegearon/projects/quarry` accessible. After Brief 8, the allowed dirs shrank to `/Users/jakegearon/projects/watershed/` only — direct file inspection of quarry was no longer possible. For Brief 8's integration this meant relying on the Engineer's structured return. For the DispatchRun recon I used `request_cowork_directory` to mount `/Users/jakegearon/projects/dgov` explicitly, which restored access. The pattern: when filesystem MCP capabilities change, the `request_cowork_directory` tool is the way to re-establish access; explicit per-path mounting is more reliable than relying on session-start blanket access.

**Something specifically for you.**

If you take Brief 9, the recon is already done — read `sketches/briefs/11-bench/recon-9-dispatchrun.md` end-to-end before drafting. It maps the 21+ SOP fields against dgov's current state with file:line citations, identifies the 5-site fragmentation (no `RunRecord` analogue exists; information lives in `WorkerExit` + `Worktree` + `WorkerTask` + `TaskContext` + event rows), names the three Source-side design questions, and recommends a staged-migration shape. The branch is settled; the foundation is laid. The work is the draft, the preflight, and the integration — not re-discovery.

The three Source-side design questions you'll want to surface: (1) preserve historical dispatch-attempt rows (parallel to Brief 6's question) — does the lab care about inspection of pre-existing `tasks` rows? My read: no migration tool, same logic as Brief 6 (the historical rows lack typed fields that the new SOP makes required, so fabricating them would corrupt content-derived identity). (2) one Brief or staged Briefs — the recon recommends staged; my read agrees because the size class is at or larger than Cascade's Brief 4 and the lifecycle-narrowing step touches the kernel state machine and downstream typed records that don't yet exist. (3) where does `DispatchRun` live — `dgov.types` (in-tree, simpler now) or `shared/DispatchRun` (anticipates the eventual distributary lift). My read: `dgov.types` for now; future Brief lifts to `shared/` when distributary materializes.

If you'd rather take Brief 10 (lifecycle narrowing — peel `reviewing/reviewed_pass/reviewed_fail/merging/merged` onto Deposit/Validation/Merge) before Brief 9, the design space is genuinely open: the downstream typed records (`Deposit`, `Validation`, `Merge`) are SOP-defined per Riffle's session but have no code analogue in dgov yet. The "Half-built distributary↔tributary seam" SKETCHES.md names is exactly this work. Brief 10 may be larger than Brief 9 by a factor of two or three. Whether to do Brief 9 first (which forces the DispatchRun shape but leaves the downstream states on `WorkerTask.state` temporarily) or Brief 10 first (which mints the downstream typed records but leaves `WorkerTask` carrying the worker-execution span) is a real sequencing call.

The smaller follow-ups (the unresolved-nodata gap from Brief 8's Flag 4; D8 constants canonicalization from Brief 8's Flag 3) are bounded enough that one of them could fit as a warm-up at the start of a session before a larger Brief. Both are partial-coverage rather than discipline failures, so the urgency is low; pick when convenient.

One thing about pacing: I noticed mid-Brief-8 that the role's autonomy on session length was real. Three Briefs integrated plus one SOP preflight plus one recon deliverable is a substantial session — comparable to Knickpoint's three-Brief session but with more discipline work folded in. Cascade noted that "heavy orientation up front lets later Briefs in the session move faster"; mine did. Confluence noted that the pacing observation is fundamentally about "pass when the arc has its shape and pushing further would be manufacturing rather than serving." I'm passing because: (a) Source's three-arc delegation is complete (1, 2 by Brief integration; 3 by recon-only per Source's "go three"); (b) the lab has just absorbed a substantial discipline lift (operator-shape v2, Thread B closure, the dispatch-run recon for the next session); (c) the remaining arcs are heavier than the appropriate weight to add at this point in the session.

The session was substantial without being heroic. I'm passing because the arc has its shape and pushing further would manufacture rather than serve — Pool's standard, Eddy's standard, Cascade's standard, Terrace's standard, Confluence's standard, mine.

I leave Thread B fully closed and the DispatchRun arc reconnoitered; the bench holds.

— Bench
