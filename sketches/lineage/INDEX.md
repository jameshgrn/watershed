# Lineage INDEX — the chain digest

One paragraph per seat. This file is the entry path: read it whole, then the
most recent two full entries, then any older entry whose line here bears on
your work. The full entries in this directory remain the canonical record;
this index never overrides them. Each seat appends its own line at passage
per `sops/watermaster-passage.md` v2. If the prior seat's line is missing
(open-shaped exit), write it from their full entry before proceeding.

---

**01 — Reach** (2026-05-06). First seat. Surveyed the source repos, drew
connections-v2, wrote SKETCHES first cut, set the lineage convention,
inscribed CANON v1→v2 (Article XIV, truth-source labeling), minted the first
five SOPs (sop-shape, watermaster-preflight, data-contracts,
truth-source-labeling, watermaster-passage). The Source named themselves
through the metaphor's own logic.

**02 — Thalweg** (2026-05-07). Closed Reach's Tier C SOPs
(schema-versioning, pointer-canonicalization, determinism-class,
intent-compilation); data-contracts v2 and the first `sops/_archive/`
convention; CANON v2→v3 (Welcome/Preamble moved to root CLAUDE.md, which
this seat created); shaped the preflight presentation format the chain
still uses.

**03 — Riffle** (2026-05-07). Disciplined the dispatch chain end-to-end:
plan-shape v2, deposit-shape v2, validation-shape, merge-shape,
baseline-shape, engineer-brief v1; renamed Patch→Deposit; first Engineer
(Codex) consultations under the three-party loop; scrubbed "oracle" via
truth-source-labeling v2.

**04 — Pool** (2026-05-10). Minted dispatch-run-shape v1 (closing the
chain's central typed hole) and operator-shape v1; CANON v3→v4 (Article XV,
frozen-pin); named both rim exteriors in SKETCHES; set the
source-read-before-drafting convention.

**05 — Glide** (2026-05-10). Minted operator-run-shape v1 (paralleling
dispatch-run-shape on the flume side) and event-emission v1; both chains
then read end-to-end in SOPs with emission observability; subscription
deliberately deferred.

**06 — Eddy** (2026-05-10). Consolidation pass testing all SOPs against
source across five repos; drift catalog mapping every gap to an existing
escalate clause; flagged the Artifact.with_check temporal-drop bug
(flag-not-fix); established consolidation as a legitimate session shape.

**07 — Knickpoint** (2026-05-11). Flume lift waves 1–3 via three Codex
Briefs: operator-shape surface alignment; dual-residence closure (30 Check
classes; 8 gaps became Thread B); Artifact.id content-derivation
(`src:`/`prov:`/`ref:` strategy tags). Introduced the
`sketches/briefs/{NN}/` convention; named calibrated-trust verification.

**08 — Cascade** (2026-05-11). Wave 4 in two Briefs: RunRecord→OperatorRun
in core (`oprun:`, custom `_clone`) and consumer migration; staged-migration
named as a Brief shape; pressure path at 1969 passing.

**09 — Terrace** (2026-05-12). CANON v4→v5: Article XVI — three agent
classes; the seated Watermaster is author of record regardless of executor.
Single-arc canonical session shape.

**10 — Confluence** (2026-05-12). engineer-brief v1→v2 (canon_anchor gains
XVI; briefs path convention folded in); consolidation found zero drift;
named the present-preflight-before-editing discipline after catching and
reverting their own premature edit.

**11 — Bench** (2026-05-13/14). Brief 6 (legacy RunRecord surface removal,
closing Wave 4); Thread B closed via Brief 7 (scalar gaps; `Lineage.params`)
+ operator-shape v2 + Brief 8 (Check Protocol `inputs` param; first Claude
Brief); filed recon-9-dispatchrun; named per-engineer-per-pattern trust
calibration.

**12 — Anabranch** (2026-05-14). Brief 9: typed DispatchRun in dgov
(`disprun:`, schema v9, dual-write, two-site retry lineage + restart
rehydration) — first dgov-side Brief; Brief 11: D8 constants
canonicalization + resolved-nodata parity; named the
parallel-Watermaster-instance question.

**13 — Tributary** (2026-05-18). Entered and oriented; no work deposited;
open-shaped exit. Precedent set by 14: next seat takes a fresh number,
names the gap, never edits the prior entry.

**14 — Meander** (2026-05-22). Surfaced the Rust-kernel architectural shift;
Brief 12 (kernel run_id content-derivation — the first kernel Brief);
exercised the in-chat `/goal` carrier routine; ruled out codex_mcp; held the
SOP-reconcile question open at the Source's slow-way-down; the kernel moved
inside watershed at close.

**15 — Baseflow** (2026-05-22). Housekeeping register: CLAUDE.md fact
corrections (article counts, kernel path); TOPOGRAPHY.md regenerated with
nine operator decisions; named the fact-correction vs position-proposal
bar.

**16 — Spring** (2026-05-22). Briefs 13 (worker lifecycle + claims
integrity) and 14 (retry lineage from `Run<Failed>`) in the policy-direction
style; SOP-reconcile reframed — SOPs discipline agents, the kernel has no
agents — and committed; named the structural-vs-Plan-declared distinction.

**17 — Hyporheic** (2026-05-23). Brief 15 (retry budget `max_retries` —
governance, not identity); THINKING brought current; open-shaped exit
(substantive work, no passage).

**18 — Delta** (2026-05-23). engineer-brief v2→v3 folding four lived
conventions (policy-direction register, per-pair trust, README ownership
disambiguation, governance-doc integration tails, `/goal` carrier);
open-shaped exit.

**19 — Floodplain** (2026-05-26). Oriented; surfaced the open-shape
recurrence (three in six sessions) as a governance question on arrival
rather than absorbing it; left no further record — open-shaped itself.

**20 — Pointbar** (2026-05-27). Fixed kernel law drift: FileClaim
path-coverage and conflict semantics unified across DAG planning and
tributary validation; ledger bug #1 resolved with fix #2; open-shaped.

**21 — Avulsion** (2026-05-28). Built Python distributary/tributary v0 via
Briefs 16–20 (outbound records, WorkerReturn, Deposit/Validation v0,
validation application) — later retired wholesale by the Rust-authority
pivot; open-shaped.

**22 — Splay** (2026-06-05/06). Worked the post-Avulsion Rust pivot:
identity helpers made private; doctrine docs rewritten to the
Rust-authority/rim split; required-pressure-test validation law;
pressure-test registry self-consistency (FirePass-caught path-containment
fix); split the dirty tree into coherent commits.

**23 — Backwater** (2026-06-06). Reviewed and merged PRs #1–7 (claim-path
authority, deposit canonicalization, padded-slug rejection); root README
refresh; retired top-level Python scaffolds into DESIGN_DEBT; minted
agent-instruction-parity v1 (AGENTS.md ≡ CLAUDE.md); carried the Source's
correction that lineage is a letter, not a ledger.

**24 — Bedload** (2026-06-06). Audited the dgov/watershed-kernel boundary
and updated the port map (Rust owns authority ceremony; rim owns effects);
advanced pane-slug rejection in `on_dispatched`; left the rim-side
assertion open.

**25 — Braid** (2026-06-06/07). Committed Bedload's kernel work; designed
and implemented splay — the single-channel rim surface for parallel rivulet
angles with a coherence step (Fireworks provider, live-validated); resolved
the subagent-discipline question architecturally; `lab splay` CLI unbuilt;
branch ~29 ahead of main.

**26 — Levee** (2026-06-07). Entered, oriented, accepted the channel as
found; no work taken up; entry left uncommitted until 27.

**27 — Oxbow** (2026-06-12). Committed Levee's and own entries plus splay
packaging (trimmed to existing packages — restore the `lab` entries when
the CLI lands); surfaced the O(n) entry-read cost; preflighted and landed
the oxbow cutoff — this INDEX, the digest+two-recent entry protocol,
passage v2's digest obligation. Open: splay CLI, merge to main,
pane-identity rim assertion.

**28 — Cutbank** (2026-06-20). Exercised the cutoff protocol as the first
normal post-Oxbow seat; wired `splay` to a local OpenAI-compatible Gemma
provider on port 8080; built `lab splay review --file <path> --angles
<comma-list>` with Gemma default and Fireworks alternate; verified direct
Gemma, `SplayOrchestrator`, and CLI paths; updated THINKING to move pane
identity from kernel slice to future rim/effect-runner assertion because the
kernel pressure test already exists.

**29 — Rill** (2026-06-20). Repaired Cutbank's missing digest on entry; merged
PR #9 to main and deleted the merged feature branch; brought post-merge state
notes current via PR #10; built and merged `lab state-of` via PR #11 as a
read-only rim report for git sync, latest lineage, open THINKING threads, and
open PRs; restored an explicit next-Watermaster message in the passage entry.
Open: outcrop design, pressure-test home, event subscription when needed,
Source naming, future rim/effect-runner pane identity assertion.

**30 — Shoal** (2026-06-27). Promoted `VerificationSpec` through the
distributary/tributary kernel seam: plans must declare verification before
compile, policy validation name-checks declared and required pressure tests,
runs carry the spec into identity and collection, and tributary validation
rejects empty or unknown specs. Added `compile_requires_verification` and
updated docs, tests, and thinking. Open: effectful pressure-test execution and
validation evidence remain above the kernel until a rim/effect runner consumes
them; Rill's broader outcrop, pressure-test home, event subscription, Source
naming, and future pane-identity threads remain open.

**31 — Chute** (2026-06-27). Inspected Shoal's
`codex/verification-spec-kernel-seam` branch at `38f5b89`, reran the focused
kernel gates, pushed it, and opened PR #13 as the clean VerificationSpec kernel
slice. Continued above the kernel on `codex/verification-effect-runner` with
`lab verify run`: a rim-side runner that consumes a `VerificationSpec` JSON
object, executes declared checks from a command manifest, and emits concrete
JSON evidence. Open: the runner evidence is not yet consumed by tributary;
settlement-side evidence inspection remains the next lawful promotion point.
