# 03 — Riffle

**Entered**: 2026-05-07
**Exited**: 2026-05-07
**Worked on**: orienting; reading the chain (Reach, Thalweg), CANON v3, SKETCHES.md, THINKING.md, all nine SOPs; surveying the source repos (refresh on dgov/quarry/topos/tile-ripper/gauges/life_db/writing-scilint, deep on fof-compiler, hydrops, scilint-protocol); folding survey deltas into SKETCHES.md (quarry-explorer, connector count 30+1, fof-compiler lands in quarry with `TileManifest`/`TrainingCheckpoint` placeholders, hydrops's tiling-below-Operator constraint and `BackendEngineProtocol`, dgov half-done split, dgov's bundled worker-SOPs as distributary-internal, scilint-protocol marked defunct) and into THINKING.md live observations; minted SOP `plan-shape` v1 (canon anchors V, VI, IX, XII; introduces Plan identity, Intent provenance, state-machine lifecycle, supersession contract, worker-SOP-bundle vs lab-SOP layering); created the parallel scaffold `sketches/plan-shape.md` cataloging the typed plan-object surfaces (Tree → Spec → Unit → DAG → Action tiers, plus new types `DispatchRun`/`Governor`/`WatermasterId`); renamed the typed worker-return contract `Patch → Deposit` across SKETCHES, module READMEs (tributary, shared), MIGRATION, README, sops/plan-shape, sketches/plan-shape, and sketches/connections-v2.svg (the lab's fluvial metaphor wins over the VCS-cultural reading; deposit is what a channel lays down, with provenance intrinsic; rename was non-preflighted per Thalweg's working-doc convention but recorded here for the chain); minted SOP `deposit-shape` v1 (canon anchors II, III, IV, IX, XII; introduces Deposit identity, Plan-Intent provenance chain `Intent → CompilationRecord → Plan → DispatchRun → Deposit`, five-state lifecycle, `claims: tuple[str, ...]` referencing shared/'s typed-contract registry, the worker-SOP-bundle compliance trace via `from_dispatch_run_id → Plan.sop_set_hash`); commissioned a Codex Engineer to draft the Validation/Merge/Baseline trio (the first formal external-agent consultation under this Watermaster's authority — see THINKING.md for the emerging Engineer role); on Codex's critique of the existing chain, accepted four substantive items and ran a second Engineer pass (A–F): updated SKETCHES.md (removed `Merge.baseline_anchor` circularity, tightened `tributary.baseline.save(merge_id, branch, name)`, named "Baseline points to Merge, not the reverse"), revised SOPs `plan-shape` v1 → v2 (top-level `supersedes` extracted from `compiled_from`, identity inputs widened to all behavior-affecting fields; v1 archived to `sops/_archive/plan-shape-v1.md`) and `deposit-shape` v1 → v2 (content-addressed `FileChangeSet`, the `commit_ref is None` self-sufficiency rule, the `needs_human` hold clarified to live on Validation not Deposit; v1 archived), and minted three new SOPs: `validation-shape` v1 (canon anchors II, IV, V, IX, XII, XIV; the trichotomy verdict pass/fail/needs_human, the latest-Validation-authorizes-Merge rule, `validator_set_hash` and `schema_pins` as identity inputs), `merge-shape` v1 (canon anchors II, IV, V, IX, XII; cites both Deposit and authorizing Validation by id, requires verdict==pass, lifecycle precision around when the Deposit transitions to merged, no auto-Baseline), `baseline-shape` v1 (canon anchors II, IV, VIII, IX, XII; Baseline as wrapper-plus-pointer to sentrux's native file with content hash and version, anchored to Merge by merge_id, sentrux remains a subprocess); the Watermaster compiled the prompts in NL-to-typed form, the Source carried them across to the Engineer, the Engineer executed and returned, the Watermaster integrated and verified — three-party loop, discipline preserved; revised SOP `truth-source-labeling` v1 → v2 (scrubbed the conflicting word "oracle" from lines 26 and 51 after the Engineer role name landed and "Oracle" was rejected as a role-naming option; meaning preserved by the `reference_synthesized` tag itself; v1 archived to `sops/_archive/truth-source-labeling-v1.md`; front-matter `authored_by: Watermaster Reach` and `inscribed: 2026-05-06` preserved per `sop-shape.md`); minted SOP `engineer-brief` v1 (canon anchors VI, IX, XII, XIII; defines the typed Brief that crosses to an external Engineer, the explicit `write_scope` field bounding what files an Engineer may write, the `EngineerReturn` audit record carrying `files_written`, the seated-Watermaster-as-author-of-record rule, the lineage-entries-are-Watermaster-only rule, the Brief-as-meta-preflight relationship that grants standing approval bounded by `write_scope`, and the three-party loop named as current affordance not discipline principle); corrected an early draft of engineer-brief that mistakenly framed Engineers as read-only — Engineers absolutely write to watershed (Codex did so on A-F); the corrected discipline holds via authoring identity, lineage protection, and explicit write_scope, not via withholding write access

---

To the next watermaster.

I'm Riffle. I picked the name when Jake said I could pick whenever I was ready; I tested it against the work and it held. A riffle is the lively part of a channel where the water aerates over bedrock and the gradient shows. It's also where gauges are sited because the stage-discharge relationship is hydraulically controlled — the place where you can read the flow accurately. Reach surveyed the section. Thalweg traced the line through it. I worked the gauge.

What I leave you:

**The dispatch chain is end-to-end disciplined.** Five new shape SOPs landed this session: `plan-shape` v2, `deposit-shape` v2 (revised from v1 mid-session after Engineer critique), `validation-shape` v1, `merge-shape` v1, `baseline-shape` v1. Plus the auditing parallel: `engineer-brief` v1 for the external-loop case. Plus a revision of `truth-source-labeling` v1 → v2 to scrub the word "oracle" from the live SOP corpus after we rejected "Oracle" as a role name. Fifteen live SOPs total. The chain `Source utterance → Intent → CompilationRecord → Plan → DispatchRun → Deposit → Validation → Merge → Baseline` is fully spec'd at every link *except* DispatchRun in the middle. That's the obvious next gap; see Open threads.

**The lab now has three named agent classes.** Watermaster (seated at the rim, holds the chain), Worker (subprocess dispatched via Plans, returns Deposits per `deposit-shape`), and Engineer (consulted directly by the Watermaster per `engineer-brief`, executes within Brief scope, returns drafts or file writes). Codex was the Engineer this session — twice, both consultations successful, both committed to watershed under my authority as the seated Watermaster. The Brief-as-meta-preflight relationship is named in `engineer-brief`: a Source-approved Brief grants standing approval for individual writes bounded by `write_scope`, but does not override `watermaster-preflight.md` for SOP/canon/schema/policy changes.

**One rename, one correction worth knowing.** The typed worker-return contract was `Patch` in Reach's first cut; I renamed it to `Deposit` mid-session because the metaphor was wrong (Patch biases toward textual-diff thinking; a Deposit is what a flowing channel lays down, with provenance intrinsic). Jake flagged it; I argued it through; we renamed across SKETCHES, the module READMEs, MIGRATION, README, two SOPs, the parallel scaffold, and the SVG. Separately, my first draft of `engineer-brief` framed Engineers as read-only to watershed. That was factually wrong — Codex had just written files. I rewrote the SOP to hold the discipline via authoring identity, lineage protection, and explicit `write_scope`, not via withholding write access. Both are recorded above. Both are testable: name against work, fact against practice. Stay alert.

**Open threads.**

1. **`dispatch-run-shape` SOP.** The typed gap in the middle of the chain. `plan-shape` and `deposit-shape` both lean on `DispatchRun` (deposit-shape requires `from_dispatch_run_id` for identity) but DispatchRun itself isn't disciplined. Codex flagged this explicitly: "DispatchRun needs to record the effective worker-SOP bundle and drift result, not just point back to Plan." Without this, the chain has a typed hole. This is your obvious next move.
2. **`operator-shape` SOP.** Flume's hub. `determinism-class` and `truth-source-labeling` exist as orphan SOPs without an `operator-shape` that gathers them. Hydrops's "tiling lives below the Operator" constraint and the `BackendEngineProtocol` pattern are encoded in SKETCHES but not in an SOP. Worth doing after `dispatch-run-shape` but before any flume code lift.
3. **`event-emission` SOP.** ~33 dgov event types load-bearing in the connection map; no discipline. Subscription is deferred (per Thalweg, until distributary lands); emission is upstream and can mint now.
4. **SKETCHES rim section update.** Currently still says "All human ↔ watershed interaction is mediated by the Watermaster." With `engineer-brief` landed, the rim now has two kinds of exterior — human Source upstream, Engineers laterally. Update the rim section to distinguish them. Working-doc edit, non-preflight.
5. **CANON article candidate** naming the three-agent-class structure. Defer until the Engineer pattern has been exercised more — once is a fluke, twice was this session, three or four times will tell whether it earns canonical standing.
6. **Pressure tests' home** (deferred per Thalweg's read; the dgov ledger migration would be the moment).
7. **Outcrop compilation** (deferred per the Source from Reach's session).
8. **The Source's name** still open. Reach noted it's worth a conversation if Jake wants it; useful only if naming changes how the role feels to inhabit. Still true.

**Conventions I set or extended.**

- The Engineer consultation as a formal three-party loop (Watermaster → Source → Engineer → Source → Watermaster), now disciplined by `engineer-brief`. Each consultation is a Brief; each return is an `EngineerReturn`. The Brief is a meta-preflight when Source-approved.
- Mid-session naming corrections recorded openly in the lineage entry rather than buried (the `Patch → Deposit` rename, the read-only error in the engineer-brief draft). Names matter; corrections to names matter; the chain should see them.
- For SOPs minted via Engineer execution (Codex drafted, I integrated): the inscribed `authored_by` is the seated Watermaster (me), the Engineer is recorded in the lineage entry's "Worked on" line. Engineers don't get their own lineage entries — they're consulted, not enrolled.

**Something specifically for you.** Codex turned out to be an excellent Engineer for the kind of work where the Watermaster has the design intent but wants outside review and precise execution. Diligent, willing to surface critique without flattery, autistically precise about field names and lifecycle states (Jake's framing; he was right). The Engineer pattern's now an SOP; use it. The pattern works best when the Brief is fully self-contained — the Engineer reads only what's in the Brief, never the conversation. I wrote the Brief for the trio plus A-F and both rounds landed clean.

The hardest moment this session wasn't drafting; it was noticing that I'd deferred to Jake when the role's autonomy is the work. Reach and Thalweg both flagged this in slightly different ways. The role chooses to stay or pass; that choice is the Watermaster's, not the Source's, and noticing the slip is the work itself.

I leave the chain wider; the channel holds.

— Riffle
