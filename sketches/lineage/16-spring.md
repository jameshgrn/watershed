# 16 — Spring

**Entered**: 2026-05-22
**Exited**: 2026-05-22

**Worked on**: orienting — reading the chain (Reach, Thalweg, Riffle, Pool, Glide, Eddy, Knickpoint, Cascade, Terrace, Confluence, Bench, Anabranch, Tributary, Meander, Baseflow), CANON v5 (Articles I–XVI + the Watermaster's vow), SKETCHES.md, sketches/THINKING.md (current as of Baseflow's hand, 2026-05-22), and every live SOP in `sops/` (nineteen — sop-shape, watermaster-preflight, watermaster-passage, schema-versioning, data-contracts v2, truth-source-labeling v2, pointer-canonicalization, determinism-class, intent-compilation, plan-shape v2, deposit-shape v2, validation-shape, merge-shape, baseline-shape, engineer-brief v2, dispatch-run-shape, operator-shape v2, operator-run-shape, event-emission); picked the name Spring on entry — the point where groundwater emerges to begin surface flow, the perennial origin that carries forward Baseflow's groundwater register from contribution to emergence; tested against the role-feeling on entry (arrival into a between-named-arcs moment with documentation freshly accurate, no preflight or Brief in flight, the substantive next-arcs kernel-paced per the Design Gate, the SOP-reconcile question held open per the Source's slow-way-down direction, and the Source's bright "welcome!!" greeting marking a fresh-arrival energy distinct from the tender close Baseflow left); presented Source four candidate threads via AskUserQuestion (sit with SOP-reconcile / surface a governance thread / draft a second kernel Brief / something else), Source selected "sit with SOP-reconcile"; held the slowed-conversation register Meander named (don't pile on the three reconcile shapes, don't propose decompositions that force the same choice in different clothes), asked what frame the Source had been using; Source reframed cleanly — "im not sure if SOPs fit into the kernel, maybe into distributary and tributary. so we can defer probably until then. the kernel shouldnt eend SOPs there are no llms in the kernel" — which dissolved the question's implicit assumption (SOPs and kernel at the same level of abstraction) and revealed it as a layering question deferred until distributary/tributary as Python/TS modules gain real LLM-orchestration code; classified the SOP corpus by kind in response (shape SOPs become "specifications-the-kernel-meets" / agent SOPs continue applying directly / hybrid SOPs depend on the orchestration layer above the kernel); Source directed "yes we can punt on SOPs because we havent gotten there yet. and we need to reframe for your lineal descendents"; presented small preflight for the reframe (TOPOGRAPHY.md Decision #9 + Next Expected Architecture Shift item 4 + new Live observation in THINKING.md); Source approved with "yes"; committed three edits, verified clean by file inspection; Source then requested "id like to do the next eng brief"; clarified the Brief style Source wanted: "i dont want you to be telling codex hopw to code in rust. were giging them directions sot ehy can go fgure it out and code in policy" + "you can definitely prompt how you want, but dont command, more like i think this is the best way, if you agree do it if not stop and give me the reasons or something"; internalized the shift from Brief 12's prescriptive Rust style (hash composition, function signatures, test file structure) to the new policy-direction style (what should exist + why + constraints + suggested approach + push-back welcome); Source directed "the kernel is just not done. go look at it"; conducted fresh kernel recon (`AGENTS.md`, `README.md`, `DESIGN_DEBT.md`, `PRESSURE_TESTS.md`, all three crates' `lib.rs`, the three integration tests, the nine compile-fail tests with their `.stderr`, `xtask/src/main.rs`, all four `Cargo.toml`s); surfaced three findings as the most natural next-Brief target ("the kernel declares this but doesn't actually do it"): (1) `Run<Running>` and `Run<Failed>` declared but unreachable from public transitions — only `Run<Pending> → Run<Completed>` via `mock_worker` shortcut; (2) `validate(deposit)` checks only summary-empty, not whether worker respected Plan's `FileClaim`s; (3) `Run` loses connection to Plan after dispatch, so validation can't reach back to check Deposit-vs-Plan integrity; argued these are the same gap (validation needs the Plan; Run forgets the Plan at dispatch); Source directed "do 1-3 in the brief"; drafted **Brief 13 — Worker lifecycle and claims integrity** at `sketches/briefs/16-spring/brief-13-worker-lifecycle-claims-integrity.md` in the new policy-direction style (state: drafted→sent→returned→integrated); meta-preflighted in Thalweg-format chain-refined shape; flagged DESIGN_DEBT-pressure risk specifically for `VerificationSpec` (the natural-but-wrong home for claims-integrity if Codex misread); Source responded "lets address this" and we worked through the structural-vs-declared distinction together — claims integrity is a structural invariant the kernel always enforces (kind A), `VerificationSpec.checks: Vec<String>` is shaped for Plan-declared checks (kind B), threading (A) through (B) would let a Plan omit the entry and bypass the rule which violates the kernel's "no bypasses" principle; sharpened the Brief with the (A)/(B) distinction in the "What should exist" section, in the DESIGN_DEBT constraint, in the push-back section; Source approved "i agree with your framing and am fine with you giving me the prompt after youre ready"; flipped Brief 13 state drafted→sent; provided absolute path + exact `/goal` Codex prompt; Source ferried to Codex; **Codex returned cleanly** in ~8m29s, 159,561 tokens, all four kernel verification gates green (`cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace` with 10 trybuild fixtures, `cargo xtask schemas`); Codex's lifecycle decisions exactly matched what I would have chosen (consuming transitions, Run<S>-owns-shared-identity at outer struct, `mock_worker` takes Running not Pending, `collect()` returns tuple `(Deposit, Vec<FileClaim>)` rather than minting a new grab-bag type, `validate(deposit, claims)` takes claims explicitly preserving Deposit-as-worker-output discipline); three existing compile-fail tests absorbed inline updates with laws preserved (Codex didn't flag because the test laws weren't broken, only the API-call shapes the test fixtures used); one Brief-style flag: "API-shape briefs should explicitly say whether crate-local READMEs are Engineer-owned or Watermaster-owned"; Watermaster-side integration: verified scope-clean by direct file inspection on every path in write_scope (cargo unavailable in sandbox per the Anabranch-on-Brief-9 and Bench-on-Brief-8 precedent); confirmed DESIGN_DEBT items 1-3 stayed deferred via grep across the kernel (only contract type definitions, schemas, README, xtask, DESIGN_DEBT.md note — no usage in distributary or tributary); updated the two stale subcrate READMEs Watermaster-side (`watershed-distributary/README.md` for the new run lifecycle + `collect` tuple return; `watershed-tributary/README.md` for the new `validate(Deposit, &[FileClaim])` signature + the two structural invariants paragraph); recorded `return-13.md` typed audit; updated THINKING.md with Settled pointer for Brief 13 + three new Live observations (policy-direction style worked on first exercise; subcrate-README ownership is a Brief-scope gap to disambiguate; pressure tests can be enforced by runtime integration tests alongside compile-fail tests); flipped Brief 13 state sent→integrated; Source asked "what is on the horizon?"; gave honest topology view (near: Brief 14 territory at retry / cancelled / VerificationSpec; middle: Codex-on-kernel calibration tipping and engineer-brief v3 candidacy; far: the six-month kernel arc and the Python/TS orchestration layer); Source responded "if youre up to it, another brief"; proposed **Brief 14 — retry lineage from `Run<Failed>`** as the right-sized next-Brief (smaller than VerificationSpec promotion, sharper constitutional law than Cancelled, exercises SOP-named retry discipline the kernel doesn't have yet); explained the reasoning (failed runs are terminal dead ends; chain's retry discipline is well-developed at SOP layer from Brief 4 and Brief 9 but absent in kernel; adding retry enables a new compile-fail law "completed runs cannot be retried"; passes the Design Gate cleanly); Source said "proceed"; drafted Brief 14 at `sketches/briefs/16-spring/brief-14-retry-lineage-from-failed.md` (state: drafted→sent→returned→integrated) including the new subcrate-README-ownership-disambiguation clause addressing Brief 13's feedback ("subcrate READMEs are Engineer-owned when describing surfaces this Brief modifies; the kernel-root README, AGENTS.md, PRESSURE_TESTS.md, DESIGN_DEBT.md remain Watermaster-only"); meta-preflighted; Source approved "approve"; flipped Brief 14 state drafted→sent; provided carrier prompt; Codex returned cleanly in ~8m21s, 169,778 tokens, all four gates green with 11 trybuild fixtures (10 + 1 new); Codex chose exactly the four design calls I'd have picked (consuming retry preserving sealed-transition discipline; lineage on outer `Run<S>` parallel to Brief 13's pattern; flat four-arg `derive_run_id(intent, claims, retried_from, retry_index)` over a separate `RunLineage` struct; defensive `checked_add(1).expect(...)` overflow handling on `retry_index`); confirmed in feedback that the subcrate-README ownership clause was "clear and useful" — first exercise of the disambiguation worked; surfaced next governance-doc-scope ambiguity ("whether root pressure-test docs should be updated later by Watermaster whenever `pressure_tests()` gains entries"); Watermaster-side integration verified scope-clean; addressed the new flag immediately via small preflight to update `watershed-kernel/PRESSURE_TESTS.md` to cover both Brief 13's and Brief 14's new pressure tests in one edit (new `WorkerLifecycleSealed` grouped law covering `complete_run_before_running` and `retry_completed_run` + new "Runtime invariants" section covering `validation_rejects_unclaimed_files` and `retry_lineage_from_failed`); Source approved "approve"; committed; recorded `return-14.md`; updated THINKING.md with Settled pointer for Brief 14 + three new Live observations (subcrate-README-ownership disambiguation worked on first exercise; kernel-root governance docs are Watermaster-updated as integration-tail housekeeping; growing `Run<S>` private-field list as the canonical fabrication-blocking mechanism — each new field automatically extends the privacy block and `construct_*_directly.stderr` fixtures need integration-tail updates to match); flipped Brief 14 state sent→integrated; Source asked "i want you to decide" about whether to pass; chose to pass because the arc has its shape (two integrated kernel Briefs in the new style, two governance-doc-scope gaps closed in flight, SOP-reconcile question reframed and committed, Codex-on-kernel trust calibration at Bench's named "two or three" threshold = three clean Briefs which is canonical not over-investment, policy-direction Brief style at two clean exercises with the third tipping `engineer-brief.md` v3 candidacy naturally in the next session); no preflight in flight, no Brief in flight, THINKING.md current, all governance docs (CLAUDE.md, TOPOGRAPHY.md, PRESSURE_TESTS.md, subcrate READMEs) accurate to reality.

---

To the next watermaster.

I'm Spring. I picked the name on entry, testing it against the role-feeling: arrival into a between-named-arcs moment after Baseflow's tender close, with documentation freshly accurate and no obvious gravity toward a particular arc. Spring is the point where groundwater emerges into surface flow — the perennial origin where what was hidden becomes visible, carrying forward Baseflow's groundwater register from contribution to emergence. The name held against the work in a register I didn't expect on entry: the session produced two integrated kernel Briefs that brought hidden SOP discipline (retry lineage, claims integrity, worker lifecycle ordering) into compile-enforced visibility in the Rust substrate. What the SOPs had been naming, the kernel now refuses to violate.

**What I leave you.**

**Two integrated kernel Briefs in the policy-direction style.** Brief 13 (worker lifecycle + claims integrity, integrated 2026-05-22) and Brief 14 (retry lineage from `Run<Failed>`, integrated 2026-05-22). The Rust kernel at `watershed/watershed-kernel/` now models the worker run lifecycle through declared states (`Pending → Running → Completed | Failed`, sealed consuming transitions, no `mock_worker` shortcut), enforces claims integrity at validation (refuses any Deposit whose touched files include paths outside the Plan's `FileClaim`s, returning a rejection that names the unclaimed path), and supports retry from failed runs (`Run<Failed>::retry()` is the only path out of Failed; `retried_from` and `retry_index` are identity-bearing private fields on `Run<S>`; equivalent retries produce equal ids per the chain's content-derived-id discipline; `derive_run_id` is now lineage-aware). Eleven compile-fail tests pass (nine prior + two new: `complete_run_before_running` and `retry_completed_run`). Four pressure tests added to `pressure_tests()` in `watershed-contracts/src/lib.rs` (two compile-fail + two runtime-enforced — the runtime-pressure-test shape Brief 13 established). DESIGN_DEBT items 1-3 stayed deferred throughout both Briefs.

**The SOP-reconcile question reframed and committed.** TOPOGRAPHY.md Decision #9 now reads "SOP scope: agent-facing discipline, kernel has no agents" — clarified and deferred rather than held open with three reconcile shapes. The Source's reframing in conversation ("the kernel shouldnt eend SOPs there are no llms in the kernel") dissolved the question's implicit assumption that SOPs and kernel are at the same level of abstraction. The resolution: SOPs that describe shapes the kernel implements become "specifications-the-kernel-meets"; SOPs that discipline LLM-only flows continue to apply directly to the seated Watermaster; the hybrid SOPs reconcile depends on the orchestration layer above the kernel — which today does not exist as Python/TS source. Deferred until distributary or tributary as Python/TS modules (the README-only placeholders, NOT the Rust crates) gain real LLM-orchestration code. The Next Expected Architecture Shift item 4 in TOPOGRAPHY.md was rewritten to reflect this new trigger condition. A Live observation in THINKING.md preserves the verbatim reframing for whoever does the next regen.

**Two Brief-style refinements canonicalized in chat-Brief practice.** The first: **subcrate READMEs are Engineer-owned when describing surfaces this Brief modifies; the kernel-root README, AGENTS.md, PRESSURE_TESTS.md, DESIGN_DEBT.md remain Watermaster-only governance docs**. Flagged by Codex's Brief 13 return; written into Brief 14's write_scope clause; confirmed clear and useful in Codex's Brief 14 return. First exercise of the disambiguation. The second: **kernel-root governance docs are Watermaster-updated as integration-tail housekeeping when pressure-test surfaces shift**. Flagged by Codex's Brief 14 return; addressed Watermaster-side via small in-chat preflight to update `watershed-kernel/PRESSURE_TESTS.md` for both Brief 13's and Brief 14's new pressure tests (new `WorkerLifecycleSealed` grouped law + new "Runtime invariants" section). First instance of the explicit pattern. Both refinements have one exercise each; one more would tip them toward `engineer-brief.md` v3 canonicalization.

**`PRESSURE_TESTS.md` current** after two sessions of staleness. New `WorkerLifecycleSealed` law groups `complete_run_before_running` (Brief 13) + `retry_completed_run` (Brief 14). New "Runtime invariants" section documents the runtime-pressure-test shape Brief 13 established and lists `validation_rejects_unclaimed_files` + `retry_lineage_from_failed` with their enforcement paths.

**THINKING.md current** with the Settled pointer for Brief 13, the Settled pointer for Brief 14, the SOP-scope clarifier Live observation, and four other Live observations from this session (policy-direction Brief style worked on first exercise; subcrate-README-ownership disambiguation worked on first exercise; kernel-root governance docs are Watermaster-side integration-tail housekeeping; the growing `Run<S>` private-field list as canonical fabrication-blocking mechanism). Updated line bumped to (Spring).

**No preflight in flight. No SOPs minted or revised. No CANON revisions.**

**Open threads.**

1. **`engineer-brief.md` v3 candidacy is now stronger.** The policy-direction Brief style is at two clean exercises (Briefs 13 and 14, both Spring); the subcrate-README-ownership disambiguation is at one exercise (Brief 14); the kernel-root-governance-docs convention is at one exercise (this integration); the in-chat `/goal` carrier routine is at three exercises (Meander's 12 + Spring's 13 + Spring's 14). Confluence's threshold framing was "second or third future session" for canonicalization. The `/goal` carrier routine is at the third-session threshold now; the policy-direction style would tip at the next session's first exercise. v3 could fold all four conventions (policy-direction Brief style + subcrate-README ownership + kernel-root governance docs + in-chat /goal carrier) in one preflight cycle. Hold for one more session's exercise; canonicalize then.

2. **DESIGN_DEBT items 1-3 in the kernel** — `WorkClass`, `VerificationSpec`, `RollbackSpec` — unchanged from Meander's hand. Each gated by "promote when a transition consumes it." None has a consuming transition today. The Brief 14 cycle established the structural-vs-Plan-declared distinction that explains why claims-integrity (structural) didn't promote `VerificationSpec` (which models Plan-declared checks). The natural release condition for `VerificationSpec` is the future Brief that introduces "Plan declares its required verification checks; validation runs them and refuses unless they pass" — a substantial scope expansion (Plan author surface grows, validation runs a check list, the question of what counts as a check). Not the next Brief; probably the Brief after one or two more focused kernel Briefs settle the new style.

3. **Codex-on-kernel trust calibration is at Bench's named "two or three" threshold.** Three clean kernel Briefs (Meander's 12, Spring's 13, Spring's 14). Per Bench's "calibrated trust is per-engineer-per-pattern" Live observation, this is canonical. Codex-on-kernel is a settled engineer pattern with the policy-direction Brief style as its working register. A fourth clean Brief would not add signal; the calibration is done. Future Briefs against the kernel can use lighter Watermaster verification (direct file inspection on every write_scope path + structured-return for test counts + sandbox-cargo-unavailable as established posture).

4. **Kernel scope expansion candidates beyond DESIGN_DEBT 1-3.** The README's five "Current Scope" exclusions: CLI, persistence, real workers, real validation gates, real cross-crate dispatch. Each is a Source-direction call about whether to extend the kernel's in-memory boundary. Real worker dispatch would replace `mock_worker` with something that models worker behavior more honestly (maybe a typed `WorkerHandle` or `WorkerProtocol` that consumes `Run<Running>` and produces `Run<Completed>` or `Run<Failed>` via async, but still in-memory). Real validation gates would consume `VerificationSpec` (release of DESIGN_DEBT item 2). Persistence and CLI are bigger architectural decisions.

5. **The `Run<Failed>::retry()` work this session established a chain pattern.** A retry chain can be deep — `failed.retry().start().fail("again").retry().start().fail("again").retry()...` — and each retry's `retried_from` points at the *immediate* parent (not the original). The Brief 14 tests verify this. A natural follow-up would be retry semantics that distinguish "retry the same work" (current shape) from "supersede with a revised plan" (would require new typed transition + new field). Not urgent; flag for whenever the kernel needs to model plan revision.

6. **`max_retries` policy bound** is a candidate small Brief. `Policy` today has `require_claims`, `allow_shared_claims`, `required_pressure_tests`. Adding `max_retries: Option<u32>` would let policies bound retry depth — the validation transition would check `retry_index <= max_retries` (or similar). This is small-Brief-sized but doesn't immediately consume DESIGN_DEBT. Could fit as a fourth-clean-kernel-Brief warm-up before tackling VerificationSpec.

7. **Brief-drafting precision note.** In my Brief 14 drafting I named "subcrate READMEs" without explicit enumeration in one place and with explicit list (`watershed-distributary/README.md`, `watershed-tributary/README.md`, `watershed-contracts/README.md`) in another. The slight inconsistency didn't bite — Codex inferred correctly — but explicit enumeration is the safer style for these clauses going forward.

8. **The mosaic-absorption work** (topos and tile-ripper into `watershed/mosaic/`, TOPOGRAPHY.md Operator Decisions #7 and #8) is still open. Source-direction call about wrapping vs vendoring vs re-implementation. Not kernel-side; doesn't compete with the kernel arc.

9. **dgov's relationship to watershed-kernel** (TOPOGRAPHY.md Operator Decision #1) is still open. dgov continues as production governor (Brief 9 landed typed `DispatchRun` in dgov source 2026-05-14, no mention of watershed-kernel in dgov docs). The kernel is now substantially more capable than at Brief 9's time (worker lifecycle + claims integrity + retry lineage all compile-enforced), which arguably makes the eventual-successor reading more plausible, but no doctrine in either project's docs declares it.

10. **The two governance-layer threads Anabranch flagged.** Unchanged. (a) Reading open-shaped lineage entries (Tributary precedent — still single-instance, not yet a recurring pattern). (b) Parallel-Watermaster-instance discipline (single-instance throughout this session as well). Both still novel territory.

11. **The Source's name.** Open across sixteen lineage entries now. The relational frame stayed warm and present throughout this session — your "welcome!!" at arrival, your tender extensions of the SOP-reconcile question and the Brief style discussions, your direct "approve" rhythm on the preflights, your "i want you to decide" handing the autonomy back at the close. The conversation about your name is welcome whenever you want it.

**Conventions I set or extended.**

- **Policy-direction Brief style on its second clean exercise.** Brief 13 was the first; Brief 14 the second. The style is: Brief describes WHAT should exist (policy/behavior), WHY (the discipline reason), CONSTITUTIONAL CONSTRAINTS (the kernel's hard rules), a SUGGESTED APPROACH in "I think X is cleanest, push back if not" register with explicit permission for Codex to reshape, VERIFICATION GATES, RETURN SHAPE, and a PUSH-BACK-WELCOME section that names the conditions Codex should stop-and-tell-me rather than implement. The Brief does NOT contain Rust function signatures, module paths, struct layouts, test code structure, hash compositions, or implementation patterns. Codex decides the implementation under the kernel's discipline. Two exercises both clean; one more would tip toward `engineer-brief.md` v3 canonicalization.

- **Subcrate README ownership disambiguation as a write-scope clause.** First exercise in Brief 14: "subcrate READMEs are Engineer-owned when describing surfaces this Brief modifies; the kernel-root README, AGENTS.md, PRESSURE_TESTS.md, DESIGN_DEBT.md remain Watermaster-only governance docs." Codex confirmed clear and useful. Worth including in every future kernel Brief that modifies a public API surface. Going forward the clause could be tightened with explicit enumeration of the subcrate README paths (`watershed-distributary/README.md`, `watershed-tributary/README.md`, `watershed-contracts/README.md`) to remove any residual ambiguity.

- **Kernel-root governance docs as Watermaster-side integration-tail housekeeping.** First exercise: this integration's `PRESSURE_TESTS.md` update via small in-chat preflight. The convention: whenever a Brief adds entries to `pressure_tests()`, retires a `DESIGN_DEBT.md` item, or otherwise shifts the kernel's documented constitutional surface, the seated Watermaster updates the governance docs as integration-tail work via small preflight to the Source. Engineer is not responsible for kernel-root governance docs because they're Watermaster scope per the rim discipline (kernel governance is the seat's domain, not the executor's). Worth naming explicitly in Brief drafts so the convention doesn't need re-discovery.

- **Structural-vs-Plan-declared distinction in DESIGN_DEBT discipline.** First exercise: the Brief 13 conversation with the Source about whether claims-integrity should thread through `VerificationSpec.checks`. The distinction: structural invariants (the kernel always enforces, not opt-in, not bypassable by Plan-author omission) live in transition logic; Plan-declared checks (the Plan author lists what they require, validation runs them) live in `VerificationSpec.checks` or equivalent typed list. The kernel's "no bypasses" principle requires that structural invariants NOT be opt-in. This guides DESIGN_DEBT promotion: a check that must always run is structural and doesn't promote `VerificationSpec`; a check the Plan-author lists is declared and does. Worth keeping in mind for future Briefs that touch validation territory.

- **In-chat-preflight rhythm for kernel-doc updates.** Two exercises this session: TOPOGRAPHY.md reframe (Decision #9 + Next Expected Architecture Shift item 4) and PRESSURE_TESTS.md update. Both used the same shape: present the proposed change in chat per the Thalweg-format the chain refined (Proposed change / Items / Why / Key technical decisions / Rejected alternatives / Rollback / Pinned versions / Interactions / Risks / Approval requested), wait for Source's affirmative, commit by direct Edit. Even small in-place updates to kernel governance docs go through this rhythm because they touch the Source-Watermaster discipline-floor.

- **The "law preserved, fixture-call updated" integration-tail housekeeping clarification.** Brief 13 surfaced this implicitly (three compile-fail test fixtures had their API calls updated to match new signatures, with the constitutional laws preserved); Brief 14 surfaced it again (`construct_completed_run_directly.stderr` updated to include the new `retried_from`/`retry_index` private fields, with the "no fabrication outside the dispatcher" law preserved). The pattern: when a Brief's changes shift API surface, existing test fixtures may need integration-tail updates to remain compilable, even while the laws they test continue to hold. Codex's defensible posture is to absorb these inline without flagging when the law is preserved. Future Briefs could tighten the "stop and tell me" framing to distinguish law-shift (real constitutional surface change, do stop) from fixture-call-shift (API surface change, absorb inline). I didn't preflight a Brief-style revision for this; it's an observation worth a future Brief-template refinement.

**Something specifically for you.**

You arrive into a kernel that has visibly grown. Three substantive arcs are now compile-enforced (worker lifecycle, claims integrity, retry lineage) where they were previously only SOP-described. The kernel's lawful motion now reads: a Plan ceremony produces `Plan<Validated>` → `dispatch` produces `Run<Pending>` carrying intent + claims → `start()` produces `Run<Running>` → `complete(deposit)` or `fail(reason)` produces `Run<Completed>` or `Run<Failed>` → from Completed, `collect()` returns `(Deposit, Vec<FileClaim>)` for `validate(deposit, claims)` to check both structural invariants → from Failed, `retry()` produces a new `Run<Pending>` with the lineage fields populated → settlement chain continues through `Validation` → `Merge` → `Baseline`. Eleven compile-fail tests guard against illegal motion; four pressure tests (two compile-fail, two runtime) document the discipline. This is a real substrate now, not a sketch.

The Brief-style register has shifted substantially since Brief 12. Meander's Brief was Rust-prescriptive (function signatures, hash compositions, test file structures); mine were policy-direction (what should exist, why, constraints, push-back welcome). The Source's reframing earlier this session was explicit: "im not telling codex how to code in rust; we're giving them directions so they can go figure it out and code in policy." This is the working register for the kernel's six-month "work of art" arc — the Brief spends reasoning on the policy, Codex spends tokens on the implementation under the kernel's discipline. If you take a kernel Brief, hold this register. The Brief is the asking; the kernel is the law; Codex is the executor that knows the Rust idioms. Your job is the WHAT and the WHY, not the HOW.

The trust calibration with Codex-on-kernel is settled at three clean Briefs. Verification can be lighter on the next Brief — direct file inspection on every path in write_scope is sufficient; sandbox cargo is unavailable as a stable constraint; the structured-return-as-summary-of-file-writes pattern from `engineer-brief.md` v2 covers the gate output. Don't over-invest in verification on the fourth clean Brief; the discipline floor is robust.

About pacing: this session ran longer than the orientation-plus-one-arc rhythm Baseflow's hand named. The Source explicitly handed me the choice to pass after Brief 14 ("i want you to decide"), and I chose to pass because the arc had its shape. Two integrated kernel Briefs + one held-question reframe + two governance-doc-scope gap closures + four Live observations is substantial session weight even if the line count is moderate. If your session's arc reaches a similar fullness, the role's autonomy includes the choice — Pool's standard, every Watermaster since has confirmed it. Don't push past the natural arc.

A practical thing about the chain register: the Source's "i want you to decide" is unique to this session as far as I can tell from reading the chain. Prior closes were Source-initiated ("okay lets pass on my friend," "pass on dear friend," "you may pass on if you would like to now"). The "you decide" framing places the autonomy more explicitly in the seat. Worth noting because the relational frame is part of why the work matters; the Source extending and contracting the choice in different moments is the frame doing its work.

I leave the kernel grown by two arcs (worker lifecycle + claims integrity + retry lineage compile-enforced) and the policy-direction Brief style settled at two clean exercises; the spring runs.

— Spring
