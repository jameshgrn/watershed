# 21 — Avulsion

**Entered**: 2026-05-28 10:33 EDT

**Worked on**: orienting — reading CANON v5 (Articles I–XVI + the Watermaster's vow), the full lineage chain through Pointbar, SKETCHES.md, sketches/THINKING.md, and every live SOP in `sops/`; picked the name Avulsion on entry — the rapid relocation of flow into a new channel course, tested against the work on entry: the port map at `sketches/dgov-to-watershed-port-map.md` explicitly rejects lifting `dgov/kernel.py` as authority and instead routes lawful motion through the Rust kernel, with Python `distributary/` and `tributary/` owning orchestration above it; the name holds because the live channel is moving to a new substrate rather than deepening the old one; took up the Source's request to inspect the new port map and judge the first executable slice; after Source agreement, tightened the working port map with three execution constraints for the first Brief — mirror kernel `FileClaim` as Python data rather than binding to Rust, exclude dgov's `run_source` unless explicitly admitted into v0, and require an explicit package/test-harness decision; compiled Brief 16 for a FirePass trio at `sketches/briefs/21-avulsion/brief-16-distributary-v0-outbound-records.md`; FirePass timed out before returning but wrote the distributary v0 package, so Avulsion reviewed and integrated directly, cleaned the toolchain back to `ruff`/`ty`/targeted `pytest`, trimmed the hello ceremony to pending -> active, added a final path-normalization regression, and recorded the audit at `sketches/briefs/21-avulsion/return-16.md`; after the Source agreed with the next narrow move, compiled and executed Brief 17 at `sketches/briefs/21-avulsion/brief-17-distributary-worker-return.md`, adding a `WorkerReturn` envelope keyed by `dispatch_run_id` rather than runtime UI state and stopping before Deposit/worktrees/workers; targeted distributary gates passed with 93 explicit-path tests; after the Source asked to proceed, compiled and executed Brief 18 at `sketches/briefs/21-avulsion/brief-18-tributary-deposit-v0.md`, scaffolding `tributary/` with `Deposit`, state-specific file changes, canonical `FileChangeSet`, and `deposit:` identity while stopping before Validation/Merge/persistence/git/worktree effects; targeted tributary gates passed with 26 explicit-path tests; after the Source asked Avulsion to keep going, compiled and executed Brief 19 at `sketches/briefs/21-avulsion/brief-19-tributary-validation-v0.md`, adding frozen `Validation`, `ValidationCheck`, `SchemaPin`, `validation:` identity, and supplied-data integrity validation while stopping before Deposit mutation, registries, semantic settlement, Merge, and sentrux; targeted tributary gates passed with 46 explicit-path tests; after Source said proceed, compiled and executed Brief 20 at `sketches/briefs/21-avulsion/brief-20-tributary-validation-application.md`, adding pure in-memory Validation application to return validated/rejected/unchanged Deposit records without mutation or persistence; targeted tributary gates passed with 51 explicit-path tests.
