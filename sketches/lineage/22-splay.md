# 22 — Splay

**Entered**: 2026-06-05 15:49 BST

**Worked on**: orienting — reading CANON v5 (Articles I-XVI + the Watermaster's vow), the full lineage chain from Avulsion back through Reach, SKETCHES.md, sketches/THINKING.md, and every live SOP in `sops/`; picked the name Splay on entry — the fan-shaped deposit laid down when flow escapes a channel and spreads sediment across adjacent ground, tested against the work as inherited from Avulsion: the channel had avulsed into Python `distributary/` and `tributary/` surfaces, with v0 outbound records, worker returns, deposits, validations, and validation application laid down in narrow executable slices; this seat's natural register is not another relocation but controlled deposition outward from that new breach, keeping scope bounded while the new substrate gains form; audited the current dirty worktree against Avulsion's last record and found a post-Avulsion local pivot: the tracked Python `distributary/` and `tributary/` source files are deleted, and uncommitted `README.md` / `MIGRATION.md` edits explicitly state the top-level Python package attempt was removed and authority-bearing fan-out/fan-in work is now under `watershed-kernel/`; worked with that pivot rather than restoring deleted files; read `watershed-kernel/AGENTS.md`, `STOP_LINE.md`, and `DESIGN_DEBT.md`; verified the current kernel diff is coherent around two laws — raw `DagKernel::new(...)` must receive file claims for every task and reject independent conflicting write authority, and authoritative `Deposit` moved from `watershed-contracts` into `watershed-distributary` with private construction and content-derived `deposit:` ids, consumed by `watershed-tributary`; made one narrow surface-tightening edit in `watershed-kernel/watershed-tributary/src/lib.rs`, changing `derive_validation_id`, `derive_merge_id`, and `derive_baseline_id` from public to private because legal motions already return records with ids and a free-form public identity helper is authority-adjacent surface area not consumed by later kernel motion; updated `watershed-kernel/watershed-tributary/tests/record_identity.rs` to test rejected-validation identity through public `validate(...)` behavior instead of private helper calls; targeted verification passed from `/Users/jakegearon/projects/watershed/watershed-kernel`: `cargo fmt --check`; `cargo test -p watershed-distributary --test dag_kernel --test dag_plan --test worker_lifecycle --test lawful_motion`; `cargo test -p watershed-tributary --test claims_integrity --test record_identity --test lawful_motion --test constitutional`; `cargo clippy -p watershed-contracts -p watershed-distributary -p watershed-tributary --all-targets -- -D warnings`; `cargo xtask schemas`; `git diff --check -- watershed-kernel README.md MIGRATION.md sketches/lineage/22-splay.md`.

**Updated doctrine docs**: after the Source clarified that watershed should put things into Rust as it can, not force them, audited the internal docs for stale Python-first fan-out/fan-in language; replaced `sketches/dgov-to-watershed-port-map.md`'s retired `distributary v0 outbound records` slice with a Rust-authority/rim-orchestration lift map; sharpened `README.md` and `MIGRATION.md` to say Rust receives state-machine law when a current kernel transition consumes it, while runner/worktree/persistence/prompt/event-store/CLI/subprocess effects stay above the kernel; updated `SKETCHES.md` so `distributary/` and `tributary/` read as future rim surfaces around the Rust crates rather than parallel Python law; corrected `sketches/THINKING.md` and `TOPOGRAPHY.md` where they still described top-level Python packages as README-only placeholders; recorded durable ledger decision #3 for the boundary.

**Kernel reconciliation pass**: at the Source's direction, audited `watershed-kernel/README.md`, `PRESSURE_TESTS.md`, `watershed-contracts/README.md`, `watershed-distributary/README.md`, the `pressure_tests()` registry, and the current DAG/deposit law in Rust; found no missing raw-DAG claim-bypass law, but found the deposit authority pressure-test record was bundled too broadly; split it into constructor sealing (`construct_deposit_directly`) and content-derived identity (`deposit_ids_are_derived`) in `watershed-contracts/src/lib.rs`; updated `PRESSURE_TESTS.md` and distributary/contracts READMEs to match; updated kernel README verification guidance away from default full-workspace tests and toward package/test-scoped gates; ran `cargo xtask schemas`, which aligned generated `Deposit.json` with the existing distributary-owned `Deposit` shape and updated `PressureTest.json`'s description hash; verification passed from `/Users/jakegearon/projects/watershed/watershed-kernel`: `cargo fmt --all -- --check`; `cargo test -p watershed-contracts`; `cargo test -p watershed-distributary --test dag_kernel --test dag_plan --test worker_lifecycle --test lawful_motion`; `cargo test -p watershed-tributary --test claims_integrity --test record_identity --test lawful_motion --test constitutional`; `cargo clippy -p watershed-contracts -p watershed-distributary -p watershed-tributary --all-targets -- -D warnings`; `cargo xtask schemas`; `git diff --check` on touched kernel files; trailing-whitespace scan on touched kernel files.

**Required pressure-test policy law**: on 2026-06-06, implemented the next small kernel law the Source approved: `Policy.required_pressure_tests` is no longer inert bookkeeping. `Plan<Compiled>::validate(...)` now checks each required pressure-test name against the `pressure_tests()` registry and returns `ValidationError::UnknownPressureTest { name }` for unknown names; it does not run tests or add any scheduler/effect layer. Added the registry entry `required_pressure_tests_are_registered` and focused tests in `watershed-distributary/tests/policy_pressure_tests.rs` for accepted registered names and rejected unknown names. Updated `PRESSURE_TESTS.md`, `watershed-distributary/README.md`, and `watershed-contracts/README.md`; regenerated schemas after the registry/docs change. Verification passed from `/Users/jakegearon/projects/watershed/watershed-kernel`: `cargo fmt --all -- --check`; `cargo test -p watershed-distributary --test policy_pressure_tests`; `cargo test -p watershed-contracts`; `cargo test -p watershed-distributary --test dag_kernel --test dag_plan --test worker_lifecycle --test lawful_motion --test policy_pressure_tests`; `cargo test -p watershed-tributary --test claims_integrity --test record_identity --test lawful_motion --test constitutional`; `cargo clippy -p watershed-contracts -p watershed-distributary -p watershed-tributary --all-targets -- -D warnings`; `cargo xtask schemas`.

**Pressure-test registry self-consistency**: on 2026-06-06, made the pressure-test registry test itself rather than letting `Policy.required_pressure_tests` depend on an unchecked list. Added `pressure_test_registry_self_consistent` to `watershed-contracts/src/lib.rs` and `PRESSURE_TESTS.md`; the focused contracts test now requires every registry name to be trimmed and unique, every claim to be trimmed and non-empty, and every semicolon-separated enforcement path to be non-empty, canonicalizable, inside the canonical workspace root, and a file. FirePass review first found a blocking path-containment hole in the path check; fixed it by canonicalizing the joined path and requiring `resolved_path.starts_with(&workspace_root)` before `is_file()`. A follow-up FirePass review approved the fix with no blocking issues. Verification passed from `/Users/jakegearon/projects/watershed/watershed-kernel`: `cargo fmt --all -- --check`; `cargo test -p watershed-contracts`; `cargo test -p watershed-distributary --test dag_kernel --test dag_plan --test worker_lifecycle --test lawful_motion --test policy_pressure_tests`; `cargo test -p watershed-tributary --test claims_integrity --test record_identity --test lawful_motion --test constitutional`; `cargo clippy -p watershed-contracts -p watershed-distributary -p watershed-tributary --all-targets -- -D warnings`; `cargo xtask schemas`; `git diff --check` on the touched registry/doc files; trailing-whitespace scan on the touched registry/doc files.
