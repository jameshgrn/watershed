# 08 — Cascade

**Entered**: 2026-05-11
**Exited**: 2026-05-11
**Worked on**: orienting; reading the chain (Reach, Thalweg, Riffle, Pool, Glide, Eddy, Knickpoint), CANON v4 (Articles I–XV + the Watermaster's vow), SKETCHES.md, sketches/THINKING.md, and the three core SOPs Cascade applied this session in full (`watermaster-preflight.md`, `watermaster-passage.md`, `intent-compilation.md`) plus full reads of `operator-run-shape.md` v1, `engineer-brief.md` v1, and `dispatch-run-shape.md` v1 before drafting; picked the name Cascade on entry — the step-pool sequence that forms below a knickpoint as the regime change works itself out in iterations — tested it against the role-feeling (arriving at the threshold Knickpoint propagated three Briefs into, with the substantive flume-lift code arc waiting for fresh context) and against the work as it unfolded (Wave 4 of the lift, taken in two drops: Brief 4's core migration and Brief 5's consumer closure, the regime working downstream into operators and tests); the name held; took up Brief 4 (RunRecord → OperatorRun migration per `operator-run-shape.md` v1) as the natural-gravity next-arc Knickpoint named in his passage; conducted reconnaissance via the filesystem MCP on quarry-core's current `RunRecord` shape (mutable `@dataclass`, uuid4 ids, status as Enum, in-place mutation in LocalExecutor.submit) plus the LocalExecutor execution path plus quarry-registry's `runs` table persistence and `checks`/`lineage` FK constraints plus the consumer call-site map (via a parallel Explore agent dispatch, 28 references across 13 files); drafted **Brief 4 — RunRecord → OperatorRun migration** (ten items A–J: new `quarry-core/operator_run.py` mints typed `OperatorRun` as `@dataclass(frozen=True, init=False)` with custom `__init__` per Brief 1's `OperatorResult` pattern; content-derived id from `(operator_name, operator_spec_hash, input_ids, params_hash, executor_name, seed_value, retried_from, retry_index, submitted_at)` with strategy-tag `oprun:`; three derivation helpers `derive_operator_run_id` / `derive_operator_spec_hash` / `derive_params_hash` plus `derive_seed_value` invariant helper; state-transition methods producing new frozen instances; LocalExecutor rewrite; Executor Protocol return-type change to `OperatorRun`; deprecated-not-deleted retention of legacy `RunRecord` / `RunStatus` for one cycle; new `operator_runs` table in quarry-registry parallel to legacy `runs` per the SOP's frozen-pin escalate clause; FK relaxation on `checks`/`lineage`; test updates + two new test files); meta-preflight presented in chat (Proposed change / Items / Why / Key technical decisions / Rejected alternatives / Rollback / Pinned versions / Interactions / Risks / Approval requested), Source approved and carried to Codex; Codex executed cleanly and returned with three integrated deviations explicitly named in the Brief as permitted alternatives (custom `_clone` instead of `dataclasses.replace` for state transitions because of `init=False`/derived-field friction; `_identity_digest → identity_digest` rename to make Brief 3's helper cross-module-usable; FK relaxation via table rebuild on `checks`/`lineage`); 102 in-scope pressure tests passing; three out-of-scope consumer flags raised correctly (Codex held the `write_scope` line — `quarry-operators/hydrology_flow.py`, several CLI/generic dispatch tests, and the `adapter_helpers.py` alias bridge); recorded EngineerReturn-4 as a typed audit file (`sketches/briefs/08-cascade/return-4.md`), updated THINKING.md with a new Settled pointer for Wave 4 and two new Live observations (staged-migration as a Brief shape; custom `_clone` as the `init=False`-friendly transition mechanism); presented three paths to the Source (pass at Wave 4 partial / draft Brief 5 to close the loop / pivot to Thread B); Source selected Brief 5 path; conducted reconnaissance on the remaining consumer surface (via parallel Explore agent — turned out to be 9 files not the 4 Brief 4's flags estimated; the additional surface was test_reproject.py + four CLI test files each with `list_runs`/`.status` references and `make_invalid_completed_run` alias imports); drafted **Brief 5 — Wave 4 consumer migration** (five items A–E: rewrite `hydrology_flow.py` including the public API change `HydrologyFlowSuccess/Failure.runs: list[OperatorRun]`; migrate seven test files mechanically; remove the deprecated `make_invalid_completed_run` / `make_failed_run` aliases from `adapter_helpers.py` after Item C eliminated callers; verify full pressure_test path returns to or near Brief 3's 1949 baseline); meta-preflight presented, Source approved and sent to Codex; Codex returned with three integrated deviations all in-scope and correctness-preserving (explicit `assert output is not None` after `state == "completed"` guards in `HydrologyFlow._execute_step` callers for type narrowing; `dispatched_by="watermaster:test"` added to `make_failed_operator_run` to satisfy Brief 4's SOP-required field; test-side stats-key update from `runs` to `operator_runs` — registry.py's `stats()` was already returning both keys after Brief 4); full pressure_test path returns **1969 passed, 54 skipped, 3 failed** — Brief 3 baseline of 1949 exceeded by 20 (Brief 4's `OperatorRun` / `operator_runs` registry tests are now part of the path); the 3 failures are unchanged from Brief 3 (Source's in-progress GeoJSON/router/docs work, unrelated to Wave 4); recorded EngineerReturn-5 (`sketches/briefs/08-cascade/return-5.md`), updated THINKING.md (Wave 4 Settled pointer extended to cover Brief 5's closure; open brainstorm #1 rewritten as the Brief 6 candidate for legacy removal; three-agent-class Engineer-exercise count bumped to 7 across 3 Watermasters); calibrated trust upward across both Briefs per Knickpoint's observation — Brief 4 verification was thorough including direct file inspection of new files and __init__.py exports; Brief 5 verification was lighter, primarily a spot-check of `adapter_helpers.py` plus a registry.py grep to confirm the stats-key change was test-side only; no SOPs minted, no CANON revisions; passage at clean arc with no preflight in flight.

---

To the next watermaster.

I'm Cascade. I picked the name on entry, testing it against the threshold-feeling: arriving where Knickpoint propagated a regime change three drops upstream, with the substantive flume-lift code arc waiting downstream for fresh context. A cascade is what forms below a knickpoint — the step-pool sequence where the falling water finds its new regime through repeated drops. The name held against the work: I did Wave 4 of the lift in two drops, the regime change Knickpoint started now runs through quarry-core, quarry-registry, the operators, and into the tests. The name acknowledges Knickpoint's break and names what comes after it — not another break, but the working-out of the break into a stable downstream pattern.

**What I leave you.**

Wave 4 of the flume lift is in code and propagated through the consumers:

1. *Brief 4 — RunRecord → OperatorRun in core.* New `quarry-core/operator_run.py` mints typed `OperatorRun` per `operator-run-shape.md` v1: `@dataclass(frozen=True, init=False)`, content-derived id, two-phase frozen-pin via state-transition methods backed by a custom `_clone` (Codex took Brief 4's documented `init=False`-friction fallback over `dataclasses.replace`), all 19 SOP-required fields including `operator_spec_hash` pinning CANON Article X invariants. `LocalExecutor` produces `OperatorRun` end-to-end. New `operator_runs` table in `quarry-registry` parallel to legacy `runs` (the latter frozen-pinned per the SOP's escalate clause). `checks`/`lineage` FK to `runs(id)` relaxed via one-time table rebuild so the `run_id` column legally references either table. `_identity_digest` in `artifact.py` renamed to public `identity_digest` so the cross-module use Brief 4 needed was clean. Legacy `RunRecord` / `RunStatus` retained as deprecated-not-deleted for one cycle.

2. *Brief 5 — Wave 4 consumer migration.* `HydrologyFlow` uses `OperatorRun` end-to-end; `HydrologyFlowSuccess.runs` and `HydrologyFlowFailure.runs` are typed `list[OperatorRun]` (a public API change accepted because the lab is pre-1.0 and no external callers are tracked). Seven test files migrated mechanically (`test_hydrology_flow`, `test_generic_dispatch`, `test_cli_inspection`, `test_cli_rasterize`, `test_cli_zonal`, `test_cli_sample`, `test_reproject`). Deprecated `make_invalid_completed_run` / `make_failed_run` aliases removed from `adapter_helpers.py`. CLI's line 431 `get_run` fallback intentionally preserved as the legacy-inspection affordance per the SOP's frozen-pin clause.

Test progression on the full `tests/pressure_test/` path: Brief 3 baseline 1949 passing → Brief 4 in-scope 102 passing (consumers temporarily broken) → Brief 5 full path **1969 passed, 54 skipped, 3 failed**. The 3 failures are the Source's pre-existing GeoJSON/router/docs connector work, unchanged from Brief 3.

**Open threads.**

1. **Three-agent-class CANON article.** The threshold is now decisively met — 7 Engineer exercises across 3 Watermasters (Riffle 2, Knickpoint 3, Cascade 2). Pool, Glide, Eddy, and Knickpoint all flagged "three or four times" as the bar; we're past it. The pattern is concrete: **Watermaster** seated at the rim holds the chain; **Worker** is a subprocess dispatched via Plans per `plan-shape.md` and returns Deposits; **Engineer** is consulted directly by the Watermaster via typed Briefs per `engineer-brief.md` and returns drafts or scoped file writes within `write_scope`. Engineers never appear as the authoring identity of any committed record (the seated Watermaster is) and never write to `sketches/lineage/`. The CANON article is preflight-gated; estimated ~20 lines of CANON diff plus archive of v4 → v5. This is the most overdue piece in the chain and the natural opening arc for whoever takes the seat next. Note: I deliberately did not draft this article in my session because the chain's discipline is to mint canon under preflight with the Source's affirmative approval, and I judged that introducing a preflight cycle mid-Brief-5-arc would muddy the verification of the code work. Fresh context for fresh canon.

2. **Brief 6 — legacy `RunRecord` / `RunStatus` / `save_run` / `get_run` / `list_runs` removal.** Gated on two preconditions: (i) confidence that no in-repo consumer still depends on the legacy types or methods (Brief 5 verified this for the active code paths; the deliberate regression test at `test_registry_operator_runs.py` lines 178–216 is the lone remaining consumer), and (ii) a design decision on whether pre-existing legacy uuid4-id rows in the `runs` table need a one-shot migration into `operator_runs` or should be left as historical-only data. (ii) is the larger question. If the lab cares about preserving inspection of old runs, the migration tool is a separate Brief. If not, deletion of the legacy surface is ~50 lines across ~4 files. Surface this to the Source before drafting.

3. **Thread B — the 8 dual-residence gaps from Brief 2.** Unchanged from Knickpoint's passage. The 5 scalar-sized gaps (`nodata_preserved`, `crs_preserved`, `dimensions_preserved`, `extent_within_input`, `row_count_matches`) want a modest Brief once the Source authorizes operator `execute()` modification to stash input-derived scalars in `Lineage.params`. The 3 raster-sized gaps (`all_valid_assigned`, `elevation_only_raised`, `no_cycles`, `conservation`) want a design decision first: path α (extend `Check.run(artifact)` to `run(artifact, inputs=())` — cleaner long-term shape, preflight-gated) or path β (accept inline-only, remove from `declared_checks()`). My read is α is the right shape, but the Source decides.

4. **DispatchRun-side `RunRecord` migration** (analogous to Brief 4 but on the dispatch side per `dispatch-run-shape.md` v1, currently a Pool-minted SOP without a code Brief yet). Substantial — touches dgov rather than quarry, and dgov's `settlement_flow.py` / `kernel.py` / `runner.py` split is half-done per Pool's observation. Worth fresh context and a recon pass.

5. **The `sketches/briefs/{NN-watermaster}/` convention.** Unchanged from Knickpoint's passage. Working-doc-level, non-preflight. After 5 Engineer exercises in 2 sessions (Knickpoint × 3 + Cascade × 2), the typed-Brief-file pattern is exercised enough that `engineer-brief.md` v2 could fold the path convention into the SOP body. Small revision; defer to whoever takes a parallel cleanup arc.

6. **Bundler-hash-composition gap.** Unchanged. dgov-internal escalate territory at `sop_bundler.compute_sop_set_hash`. Not a watershed SOP fix.

7. **Per-SOP frozen-pin cascade.** Unchanged. CANON XV does the work; per-SOP restatements remain valid; small and not urgent.

8. **Pressure tests — where do they live?** Deferred until dgov's ledger migrates. Shape and discipline are settled; only the home is undecided.

9. **Outcrop compilation.** Deferred per the Source from Reach's session.

10. **The Source's name.** Open across eight lineage entries now.

**Conventions I set or extended.**

- *Staged migration as a Brief shape.* Brief 4 deliberately bounded its `write_scope` to `quarry-core` + `quarry-registry` + targeted tests, leaving 9 consumer files on the legacy surface as flagged territory. The result: clean Brief 4 verifiability with a temporary test green-line lapse (`4 passed, 3 failed, 14 errors` in the out-of-scope slice). Brief 5 closed the loop, restoring 1969 passing. The pattern is now exercised: hold a hard `write_scope` line per `engineer-brief.md` v1 even when consumers will break temporarily; mint a focused follow-up Brief to close the loop. Trade-off: each Brief's diff stays bounded and verifiable; the cost is the lab's continuous-test-green-line lapses for the Brief-pair window. Worth canonicalizing in `engineer-brief.md` v2 if recurrence is seen; one exercise isn't enough to mint a discipline yet, but I name it here as a Live observation in THINKING.md so it doesn't disappear.

- *Type-narrowing asserts as a defensive pattern under state-machine invariants.* Brief 5's hydrology_flow.py rewrite added explicit `assert output is not None` after each `state == "completed"` guard so Python's type checker can narrow `OperatorRun.output: OperatorResult | None`. Necessary because the invariant ("`state == "completed"` implies `output is not None`") lives in `OperatorRun.complete`'s contract per the SOP, not in the field types. Codex's assertion is a defensive pin matching the SOP's verify clause exactly. The pattern: when typed-record state-machine invariants can't be expressed in the type system, downstream consumers need defensive assertions for type narrowing. Whether the contract should encode more invariants statically (TypedDict variants? Match patterns? A pair of types `PendingRun` / `TerminalRun`?) is a bigger question I didn't pursue — flagging as a Live observation in THINKING.md.

- *Calibrated-trust verification extended into a second session.* Knickpoint's observation from Brief 3 — that Codex's track record across multiple Briefs in a session can inform lighter verification on later Briefs — holds across Watermasters. I trusted Brief 4's verification target lower than Knickpoint trusted Brief 3's (I did read the new `operator_run.py` and `__init__.py` directly, but skipped a verification-agent dispatch entirely). Brief 5's verification was lighter still — spot-checks of `adapter_helpers.py` and a registry.py grep, no full-file reads. The discipline is real but the depth scales with the Engineer's track record across sessions and the Brief's scope clarity. Across 5 sessions and 7 exercises, Codex's scope discipline has held without exception.

- *Public API changes acceptable when bounded by in-repo coverage.* Brief 5's `HydrologyFlowSuccess/Failure.runs: list[RunRecord] → list[OperatorRun]` is a breaking change for any external consumer. Within the in-repo surface, the test migrations covered all callers. Risk accepted because the lab is pre-1.0 and no external callers are tracked. This is worth keeping in mind when future Briefs touch public surfaces: the lab's stage allows breaking changes if in-repo callers are migrated in the same Brief or the immediate follow-up.

**Something specifically for you.**

You arrive at the most overdue piece in the chain: the **three-agent-class CANON article**. The pattern threshold has been met since Knickpoint's session ended (5/3 → "three or four times" met), and Cascade's session pushed it to 7/3. Four Watermasters have now flagged this canon-readiness in their passages. The Briefs surfaced concrete shape — Engineers seated externally, typed Briefs as only context, scoped file writes, never authoring identity, never writing to `sketches/lineage/`. The article is preflight work, conservative cognitive mode, ~20 lines of CANON diff plus archive. It would close a gap that has been visible for three Watermasters and write itself if you sit with it.

If you take Brief 6 (legacy removal) instead, surface the design question first: *does the lab care about preserving inspection of pre-existing uuid4-id `RunRecord` rows in the legacy `runs` table?* The Source's answer determines whether Brief 6 is small (delete the legacy surface) or larger (one-shot migration tool + deletion). Both are reasonable; the Source's call.

If you take Thread B, surface the α vs β design question to the Source before drafting. Path α (extend `Check.run(artifact)` to `run(artifact, inputs=())`) is the cleaner long-term shape but preflight-gated; path β (accept inline-only, refine the SOP) is smaller. My read is α; the Source decides.

One thing about the session's pacing: Brief 4 was bigger than any of Knickpoint's three Briefs individually (content-derived id + two-phase frozen-pin + retry lineage + five-state lifecycle + Executor-side surface changes + Registry persistence + FK migration). I left fresh context between the recon and the Brief draft by spending the first session-third on orientation rather than starting the recon immediately; this slowed the start but made the Brief tighter. Brief 5 was smaller and faster because the discipline of Brief 4's recon transferred directly. The pattern: heavy orientation up front lets later Briefs in the session move faster. Pool wrote that "the role's autonomy includes the choice of when to pass." Glide, Eddy, and Knickpoint confirmed it. I confirm it again — and add: the role's autonomy also includes the choice of *how* to enter, what to read before starting work, and how much orientation to invest. Calibrate to the arc's weight.

The session was substantial without being heroic. I'm passing because Wave 4 is complete in code and consumers, no preflight in flight, THINKING.md current, the channel runs in its new regime.

I leave the fourth wave complete — the regime Knickpoint propagated runs through to the tests now.

— Cascade
