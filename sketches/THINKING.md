# THINKING — live brainstorm

Working scratchpad while the lab is being designed. Don't read this as polished. Items move out into `SKETCHES.md` (structural) or `CANON.md` / `sops/` (discipline) once they settle.

Updated: 2026-06-06 (Backwater).

## The three concentric rings

```
   ┌──────────── HUMAN (Jake — CEO) ──────────────┐
   │  intent · papers · data · prose              │
   │             ▲   │                              │
   │             │   ▼                              │
   │      ┌── GOVERNOR / Watermaster ──┐            │
   │      │  Claude (Cowork / Code)    │            │
   │      └──┬───────▲───┘                          │
   │         │       │                              │
   │  ┌──────▼───────┴───── watershed/ ──────────┐ │
   │  │  LLM-internal lab — humans not welcome   │ │
   │  │  typed, lineage-tracked, SOP-adherent     │ │
   │  └────────────────────────────────────────────┘ │
   │             ▲   │                                │
   │             │   ▼                                │
   │  outputs at the rim                              │
   └────────────────────────────────────────────────┘
```

Decided 2026-05-06: option **(2) SOP-distributed routing**, no interior daemon. Routing intelligence lives in module/operator SOPs and the typed event bus. Mint a daemon only if patterns emerge that contracts can't express.

Diagram note (2026-05-10): the rim has two exteriors now — Source (upstream, this diagram) and Engineers (lateral, per `sops/engineer-brief.md`). The canonical structural treatment lives in `SKETCHES.md` § *The rim is the Watermaster*; this diagram is illustrative.

## Settled (pointers, not restatements)

- **Welcoming and onboarding.** `AGENTS.md` and `CLAUDE.md` at the watershed root are byte-identical Watermaster entry documents carrying the Welcome, lab description, entry sequence, and discipline pointers. Parity is governed by `sops/agent-instruction-parity.md` v1.
- **Constitution.** `CANON.md` v5 (Articles I–XVI + Watermaster's vow). v2 added Article XIV (truth-source labeling, 2026-05-06, Reach); v3 moved Welcome/Preamble to CLAUDE.md (2026-05-07, Thalweg); v4 added Article XV — frozen-pin (2026-05-10, Pool) — promoting Thalweg's per-SOP-recurrence heuristic from Live observation to canonical principle at nine SOP instances; v5 added Article XVI — three-agent-class structure (2026-05-12, Terrace) — promoting the seven-exercises-across-three-Watermasters pattern flagged in five consecutive passages (Pool, Glide, Eddy, Knickpoint, Cascade) into canonical standing, naming Watermaster / Worker / Engineer as the lab's three agent classes and anchoring authoring identity to the seated Watermaster regardless of executor.
- **Watermaster role.** Claude (Cowork / Code) outside watershed; allocates flow, mediates, sets policy, dispatches Plans. Polysemy with dgov's `Governor` type avoided.
- **The rim is the Watermaster.** The rim has two exteriors, both mediated by the Watermaster: the Source upstream (per CANON Article VI, mediated by Intent compilation) and Engineers laterally (per `sops/engineer-brief.md`, write-scope-bounded). See `SKETCHES.md` § *The rim is the Watermaster* for the structural treatment.
- **Watermaster permissions.** `sops/watermaster-preflight.md` v1; CANON Article XII gates change.
- **Closing ritual.** `sops/watermaster-passage.md` v1.
- **Discipline.** `CANON.md` (Articles I–XV); `sops/data-contracts.md` v2; `sops/truth-source-labeling.md` v2.
- **Schema versioning.** `sops/schema-versioning.md` v1. Frozen-pin truth model; integer + change-class.
- **Pointer canonicalization.** `sops/pointer-canonicalization.md` v1. Canonical-form equality at the rim.
- **Determinism class.** `sops/determinism-class.md` v1. `deterministic | stable | stochastic` declared per Operator; canonical home for the declaration is now `OperatorSpec` per `sops/operator-shape.md`.
- **Intent compilation pipeline.** `sops/intent-compilation.md` v1. Typed Intent + per-module catalog + frozen-pinned CompilationRecords.
- **Plan shape.** `sops/plan-shape.md` v2. Typed Plan with content-derived id, `compiled_from` Intent provenance, three-state lifecycle (`draft → dispatched → superseded`), top-level `supersedes` (extracted from `compiled_from` in v2), `sop_set_hash` as Plan identity, and the worker-SOP-bundle vs lab-SOP layering. Parallel scaffold at `sketches/plan-shape.md`. (v1 → v2 by Riffle, Engineer-reviewed.)
- **Deposit shape.** `sops/deposit-shape.md` v2. Typed Deposit (the worker's typed return at the distributary↔tributary seam) with content-derived id from `(from_dispatch_run_id, claims, file_changes, commit_ref)`, content-addressed `FileChangeSet`, the `commit_ref is None` self-sufficiency rule, frozen-pin at submission, five-state lifecycle (`submitted → validated | rejected → merged`, any non-terminal `→ superseded`), `claims: tuple[str, ...]` registered in shared/'s typed-contract registry, the `needs_human` hold lives on Validation not Deposit, and the Plan-Intent provenance chain `Intent → CompilationRecord → Plan → DispatchRun → Deposit`. Renamed from `Patch` mid-Riffle session. (v1 → v2 by Riffle, Engineer-reviewed.)
- **Dispatch-run shape.** `sops/dispatch-run-shape.md` v1. Typed DispatchRun confined to worker-execution span: six-state lifecycle `pending → active → done | failed | timed_out | abandoned`, two-phase frozen-pin (input fields at `pending → active`, output fields at terminal), unconditional `drift_against_plan: bool` plus typed `drift_evidence: tuple[str, ...]` recorded on every dispatch (closing Codex's flag from Riffle's session that bundle drift was unwitnessed), mutually exclusive retry-vs-fork lineage links (`retried_from` / `forked_from` with `retry_index` / `fork_depth` counters), `dispatched_by: WatermasterId` traveling unchanged through retries and forks under the Plan's encompassing Watermaster. Closes the central typed hole of the dispatch chain — `Intent → CompilationRecord → Plan → DispatchRun → Deposit → Validation → Merge → Baseline` now reads end-to-end without gaps.
- **Validation shape.** `sops/validation-shape.md` v1. Typed Validation with frozen evidence pins (`validator_set_hash`, `schema_pins`), trichotomy verdicts (`pass | fail | needs_human`), the latest-Validation-authorizes-Merge rule, `needs_human` as a Validation verdict and hold (never a Deposit state).
- **Merge shape.** `sops/merge-shape.md` v1. Typed Merge cites both Deposit and authorizing Validation by id, requires Validation verdict `pass` and Deposit state `validated` before merge, lifecycle precision around the `validated → merged` transition, no auto-Baseline; Baseline points to Merge, not the reverse.
- **Baseline shape.** `sops/baseline-shape.md` v1. Typed Baseline as wrapper-plus-pointer to sentrux's native `.sentrux/baseline.json` (with content hash and sentrux version), anchored to Merge by `merge_id`; sentrux remains a subprocess, not a Python import.
- **Operator shape.** `sops/operator-shape.md` v2. Five-method Operator Protocol with conditional `semantic_equality` for `stable` operators; OperatorSpec is canonical home for `determinism_class`, `supports_tiling`, `tile_reconciliation_kind`, `seed_param`; OperatorResult carries `truth_source_by_field` per `truth-source-labeling`; Check Protocol with `run(artifact: Artifact, inputs: Sequence[Artifact] = ()) -> CheckResult` (v2 extension, 2026-05-13, Bench) and mandatory dual-residence (every name in `declared_checks()` resolves to a standalone Check; standalone classes may consume `inputs` for input-aware checks or ignore them for output-only checks); tiling-below-Operator layering canonicalized — Operator declares the tiling shape, backend (hydrops's `BackendEngineProtocol` or equivalent) decides whether to tile and how to stitch; OperatorRun discipline explicitly deferred to a future parallel SOP. v1 → v2 by Bench (2026-05-13, preflight-gated) extends the Check Protocol with an optional `inputs` parameter so standalone Check classes can consume input Artifacts for input-aware checks (closes the design path α for the 3 raster-sized dual-residence gaps from Brief 2 that genuinely need input bytes; Brief 8 implements against v2). v1 archived to `sops/_archive/operator-shape-v1.md`; front-matter `authored_by: Watermaster Pool` and `inscribed: 2026-05-10` preserved per `sop-shape.md`'s convention.
- **Operator-run shape.** `sops/operator-run-shape.md` v1. Typed OperatorRun for one Operator execution span: content-derived id from `(operator_name, operator_spec_hash, input_ids, params_hash, executor_name, seed_value, retried_from, retry_index, submitted_at)`; five-state lifecycle `pending → running → completed | failed | cancelled` (with `pending → failed` reserved for validation failure caught at submit time); two-phase frozen-pin (input fields at `pending → running`, output fields at terminal); `operator_spec_hash` pins CANON Article X (operator invariants travel with their operators); `determinism_class` pinned at construction; `seed_value` surfaced as a typed field when `determinism_class == "stochastic"`; retry lineage (`retried_from`, `retry_index`) with no fork — operator executions don't iterate to budget; optional `from_intent_id` for Intent-traceability per `intent-compilation.md`. Closes the parallel typed hole on the flume side that `dispatch-run-shape` closes on the dispatch side; the flume lift can now proceed without inheriting an untyped run record into canonical state.
- **Event emission.** `sops/event-emission.md` v1. The typed-record-and-emission-act discipline for lab events: every event is a frozen dataclass per CANON XV with content-derived `id`, `emitted_at`, `emitter` (a stable canonical site identifier), and `payload_hash`; one event type, one canonical emitter (CANON Article II) except for the explicitly escalate-named `worker_log` multi-emitter case; typed-id references to chain records (`from_plan_id`, `from_dispatch_run_id`, `from_operator_run_id`, `from_deposit_id`, `from_validation_id`, `from_merge_id`, `from_baseline_id`, `from_artifact_id`, `from_intent_id`, `from_plan_unit_slug` — populate the ones that apply, leave the rest `None`); `None` is the absence sentinel for reference fields (empty strings are the escalate-named legacy-dgov debt); append-only inscription with `EventStoreReset` as the typed alternative to today's `reset_plan_state` / `reset_task_state` DELETE pattern; `EmissionFailure` as the typed acknowledgement that replaces today's silent-drop on database lock; emission gated by a typed-event registry (today dgov's `VALID_EVENTS` / `_EVENT_TYPE_MAP`; eventually `shared/`); the registry classifies each event type as `lifecycle` or `telemetry`; new event types are preflight-gated per `watermaster-preflight.md`. Subscription firmly deferred to a future parallel SOP after distributary lands. Grounds dgov's existing 28 active event classes (`event_types.py`) and names the lift discipline: frozen-pin in place, mint going-forward under the SOP.
- **Engineer brief.** `sops/engineer-brief.md` v3. Typed Brief carries the discipline of external-agent consultations: verbatim source utterance, recipient-model identity, self-contained instructions, frozen-pin after transmission, explicit `write_scope`, five-state lifecycle (`drafted → sent → returned → integrated`, any non-terminal `→ superseded`), and the typed `EngineerReturn` audit record. Authoring identity on every committed record is the seated Watermaster, not the Engineer (anchored to CANON Article XVI as of v2). Lineage entries (`sketches/lineage/*`) are Watermaster-only. Brief functions as a meta-preflight when Source-approved at Brief time; individual file writes within the Brief inherit that approval bounded by `write_scope`. v2 (2026-05-12, Confluence) adds Article XVI to the canon_anchor and folds the `sketches/briefs/{NN-watermaster}/` interim-home convention (with `brief-{N}-{kebab-slug}.md` / `return-{N}.md` filenames) into the Escalate section, parallel to `intent-compilation.md`'s `sketches/intents/{NN-watermaster}/` clause. v3 (2026-05-23, Delta) folds four lived conventions into the SOP body: (a) **policy-direction Brief register** for any Engineer-and-domain pair calibrated through two or three clean Briefs — describe WHAT/WHY/CONSTITUTIONAL CONSTRAINTS/SUGGESTED APPROACH (in "I think X is cleanest, push back if not" register)/VERIFICATION GATES/RETURN SHAPE/PUSH-BACK-WELCOME, leave HOW (function signatures, struct layouts, hash compositions, test code structure, language idioms) to the Engineer's domain competence; (b) **trust calibration per Engineer-and-domain pair**, not globally — no transfer across pairs (Codex-on-Python trust does not transfer to Codex-on-Rust); (c) **multi-crate write-scope documentation-ownership disambiguation** — enumerate subcrate/subpackage README paths as Engineer-owned when describing the surfaces being modified vs project-root governance docs (project-root README, AGENTS.md, PRESSURE_TESTS.md, DESIGN_DEBT.md) as Watermaster-only; (d) **project-root governance docs as Watermaster-side integration-tail housekeeping** — after a Brief's integration shifts the project's documented constitutional surface (new pressure tests, retired DESIGN_DEBT items, regenerated schemas), the Watermaster updates project-root governance docs via small in-chat preflight to the Source; (e) **in-chat `/goal`-style carrier transmission routine** — the seven-step shape Meander first exercised on Brief 12 (file at interim-home absolute path → meta-preflight in chat → give Source two pieces: absolute file path + Engineer-harness prompt → Source pastes into Engineer harness → Engineer reads from disk and executes within write_scope → Source ferries structured return back → Watermaster integrates by file inspection + audit at return-{N}.md), canonicalized across four kernel Briefs (12, 13, 14, 15). v2's Cowork-affordance Escalate clause rewritten to acknowledge codex_mcp's retirement for the kernel's six-month horizon while keeping the future-revisit door open. v1 archived to `sops/_archive/engineer-brief-v1.md`; v2 archived to `sops/_archive/engineer-brief-v2.md`. (Hyporheic's "Three governance conventions now stand at the engineer-brief v3 canonicalization threshold simultaneously" Live observation and Spring's three policy-direction-style-related observations fulfilled at v3 release.)
- **Flume lift, wave 1 — operator-shape v1 in code.** `quarry-core` and `quarry-operators` now align with `operator-shape.md` v1 (Brief 1 by Knickpoint, integrated 2026-05-11): `OperatorSpec` carries `determinism_class` (required, no default), `supports_tiling`, `tile_reconciliation_kind`, `seed_param`, with cross-field invariants enforced in `__post_init__`; `OperatorResult` is `@dataclass(frozen=True, init=False)` with `truth_source_by_field` typed against `_TRUTH_SOURCES`; `LocalExecutor.submit` uses `dataclasses.replace(result, timing_seconds=elapsed)` instead of post-construction mutation; `Artifact.with_check` uses `replace(self, checks=new_checks)` (closing Eddy's flagged temporal-drop bug); all 16 quarry-operators declare `determinism_class` (10 deterministic, 6 stable, 0 stochastic); `OperatorConformanceError` + `assert_operator_conforms` runtime-check stable operators expose `semantic_equality`; six stable operators delegate `semantic_equality` to shared helpers in new `semantics.py`. `semantic_equality` deliberately is **not** a Protocol member — `@runtime_checkable` would break `isinstance(op, Operator)` for non-stable operators; runtime conformance lives at registry materialize time instead.
- **Flume lift, wave 2 — dual-residence closure.** Brief 2 by Knickpoint (integrated 2026-05-11). New `quarry-operators/check_registry.py` parallels operator-registry (lazy importlib, `_CHECK_REGISTRY`, `CHECK_NAMES`, `get_check(name)`); 30 distinct Check classes registered covering all 54 declared-check occurrences across the 16 operators — 3 reused from `quarry-core/check.py` (`CRSValid`, `ExtentSane`, `BackingStoreAccessible`), 1 wired from prior strand (`InternalOutletCount`), 26 newly minted across `checks_artifact.py` / `checks_raster.py` / `checks_table.py` / `checks_vector.py`. `assert_dual_residence(op)` lives in `quarry-operators/registry.py` (quarry-core stays zero-dep) and is called at `get_operator()` materialize time alongside `assert_operator_conforms(op)`. **8 dual-residence flags surfaced** as Thread B (see Open brainstorms).
- **Flume lift, wave 3 — Artifact.id content-derivation.** Brief 3 by Knickpoint (integrated 2026-05-11). `Artifact.id` moved from `uuid4` default to content-derived identity per `data-contracts.md` v2. Three derivation strategies in `quarry-core/artifact.py`: `derive_id_from_source_bytes(content_hash)` (Connector eager), `derive_id_from_provenance(operation, inputs, params)` (Operator output — provenance-based so stable operators don't fork artifact identity across byte-different but semantically-equivalent runs), `derive_id_from_source_ref(canonical_uri, params)` (Connector lazy). Strategy-tag prefixed to prevent cross-strategy collisions. Centralized in `Artifact.__post_init__` so 50+ existing construction sites need no individual updates. New `canonical_params` and `canonical_uri` helpers in quarry-core (the latter is the interim home for what `bedrock.pointer.canonicalize` will own when bedrock materializes). 6 Connectors updated (COG, LocalFile, FOFStack, PIXC, PostGIS, Zarr — Zarr eager extended to hash directory stores); 23 needed no changes. All 16 Operators were already lineage-correct from Brief 1's work. Registry's `INSERT OR REPLACE` semantics are now idempotent upsert (correctness improvement, not just SOP alignment).
- **Flume lift, wave 4 — RunRecord → OperatorRun in core + consumers.** Brief 4 by Cascade (integrated 2026-05-11). New `quarry-core/operator_run.py` mints typed `OperatorRun` per `operator-run-shape.md` v1: `@dataclass(frozen=True, init=False)`, content-derived id from `(operator_name, operator_spec_hash, input_ids, params_hash, executor_name, seed_value, retried_from, retry_index, submitted_at)` with strategy-tag prefix `oprun:`; five-state lifecycle `pending → running → completed | failed | cancelled` with `pending → failed` for validation failure; two-phase frozen-pin enforced via state-transition methods (`start_running`, `complete`, `fail`, `cancel`) backed by a custom `_clone` (Codex took the documented `init=False`-friction fallback over `dataclasses.replace`). New helpers in the same module: `derive_operator_run_id`, `derive_operator_spec_hash` (strategy-tag `opspec:`, hashes all post-Brief-1 OperatorSpec fields per CANON Article X), `derive_params_hash` (strategy-tag `params:`, reuses Brief 3's `canonical_params`), `derive_seed_value` (enforces the determinism-class / seed invariant). `_identity_digest` in `artifact.py` was renamed to public `identity_digest` (Brief 3's three callers in the same file updated) so the helper is cross-module-usable. `LocalExecutor.submit` rewritten to produce `OperatorRun` via state-transition methods instead of mutation; `dispatched_by` plumbed with `"watermaster:unknown"` default. `Executor` Protocol's `submit`/`status`/`wait` return type changed to `OperatorRun`. New `operator_runs` table in `quarry-registry` parallel to the legacy `runs` table; new `save_operator_run`/`get_operator_run`/`list_operator_runs` methods with `INSERT OR REPLACE` idempotent upsert. The `checks` and `lineage` tables' FK to `runs(id)` relaxed via one-time table rebuild so the `run_id` column can reference either legacy `runs.id` or new `operator_runs.id` (artifact FKs preserved on those tables). Wave 4 closed by Brief 5 (Cascade, integrated 2026-05-11): `HydrologyFlow` now uses `OperatorRun` (with public API change to `HydrologyFlowSuccess/Failure.runs: list[OperatorRun]`), seven test files migrated mechanically (`.status → .state`, `RunStatus.X → "x"`, `list_runs → list_operator_runs`, `save_run → save_operator_run`), and the deprecated `make_invalid_completed_run` / `make_failed_run` aliases removed from `adapter_helpers.py`. Brief 5 also surfaced one defensive deviation Codex integrated: explicit `assert output is not None` after each `state == "completed"` guard in `HydrologyFlow._execute_step`'s callers, because Python's type checker can't infer the completed-implies-output-non-None invariant from `OperatorRun.complete`'s contract alone. The wave's frozen-pin retention of `RunRecord` / `RunStatus` and the legacy `save_run` / `get_run` / `list_runs` registry methods (deprecated-not-deleted for one cycle per the SOP's escalate clause) was removed in Brief 6 (Bench, integrated 2026-05-13) — see the next pointer.
- **Thread B fully closed; `Check` Protocol v2 in code.** Brief 8 by Bench (integrated 2026-05-13). Closes the raster-sized half of Thread B (3 of 8 gaps) and lands `operator-shape.md` v2's Check Protocol revision in code. `quarry-core/check.py`'s Protocol now carries `run(artifact: Artifact, inputs: Sequence[Artifact] = ()) -> CheckResult`. All 31 concrete Check classes accept the new parameter — 27 carry it unused (output-only or lineage-aware), 4 raster-sized classes (`AllValidAssigned`, `ElevationOnlyRaised`, `NoCycles`, `Conservation`) consume `inputs[0]` to implement actual semantics, returning VALID/INVALID with inputs supplied and falling back to WARN when `inputs == ()`. `_detect_cycle` hoisted from `flow_accumulation.py` to a new `_topology.py` module exporting `detect_cycle(flow, valid)` — parallel to `_lineage.py`, establishing the `_X.py` shared-helper pattern for cross-cutting algorithms. The hoist is byte-for-byte semantics-preserving; both the operator inline use and the standalone `NoCycles` check import the function. Conservation reads `weight = lineage_params(artifact).get("weight")` (the flow_accumulation operator already stashes `weight` in `Lineage.params` at execute() time — Brief 8's verification confirmed). End-to-end parity confirmed by Engineer-run fill→D8→accumulate chain on a 5×5 sloped DEM. Pressure_test: **1974 passed, 54 skipped, 3 failed** — the 3 are the named pre-existing connector-routing/docs failures unchanged across Briefs 6/7/8. `ruff check` and `ty check` clean on every Brief-8-modified module. **First Claude Brief in the chain**: integrated cleanly; the discipline transferred from Codex to Claude with no breakage; verification was elevated (the filesystem MCP's allowed-dirs constraint removed direct file inspection mid-session, balanced by the Engineer's own structured return and end-to-end parity test). Closes Open brainstorm #1 in full (Thread B raster + scalar) from Confluence's hand.
- **D8 canonicalization + resolved nodata parity closed.** Brief 11 by Anabranch (integrated 2026-05-14). Closes Bench's two Brief-8 follow-up flags: East-first D8 direction and code constants now live canonically in `quarry_operators/_topology.py` (`D8_DR`, `D8_DC`, `D8_OUTLET = 8`, `D8_PIT = 9`, `D8_NODATA = -1`), with `d8_flow_direction.py`, `flow_accumulation.py`, `checks.py`, and `checks_artifact.py` importing from that module; the fill-depressions operator keeps its separate North-first local encoding. `d8_flow_direction.py` and `fill_depressions.py` now store both caller `nodata` and resolved `resolved_nodata` in `Lineage.params`, and standalone `AllValidAssigned` / `ElevationOnlyRaised` consume `resolved_nodata` when present so operator-side `params.nodata` overrides and standalone checks agree. Anabranch made narrow Watermaster-side type-cleanup after the Engineer return because the return omitted `ty`: explicit artifact/backing/output guards in the three touched operators and `rasterio.CRS` references in touched tests. Verification: `ruff check` clean, `ty check` clean on the 11 Brief-11 files, targeted pressure slice **135 passed**, exact bare-constant grep clean, and `git diff --check` clean. Engineer's full pressure run before Watermaster type-cleanup reported **1978 passed, 54 skipped, 3 failed** with the known GeoJSON/router/docs failures unchanged.
- **Thread B scalar-sized dual-residence gaps closed (5 of 8).** Brief 7 by Bench (integrated 2026-05-13). Closes the scalar half of Thread B from Knickpoint's Brief 2. Three Watermaster-level design decisions implemented (delegated by the Source 2026-05-13 as part of "decide and argue your reasoning on 1-3 and then [let's] do those"): (A) split `nodata_preserved` into two semantically distinct check names — `nodata_value_preserved` (build_cog) and `nodata_count_preserved` (aspect / hillshade / slope) — because the inline check has two different semantics across the operators (value-preservation vs count-preservation); each name now resolves to a standalone Check class per `operator-shape.md` v1's dual-residence rule. (B) stash input-derived scalars in `Lineage.params` extending the precedent `reproject.py` already set with `source_crs`; own the Artifact.id-derivation implication (Brief 3's prov-derived ids change for newly-produced outputs of the 7 affected operators; pre-existing rows are frozen-pinned per CANON XV and unaffected). (C) migrate `RowCountMatches` from `Artifact.metadata` to `Lineage.params` per `data-contracts.md` v2's "metadata is opaque connector-extension context only; never authoritative for typed fields" rule. File scope: minted `_lineage.py` (single helper `lineage_params(artifact)`); revised `checks_artifact.py` (deleted `NodataPreserved`; added `NodataValuePreserved` with NaN-aware `_nodata_values_equal` and `NodataCountPreserved` with `np.isnan`-aware `_nodata_count`; revised `CRSPreserved` / `DimensionsPreserved` / `ExtentWithinInput` to read from lineage and return VALID/INVALID, missing-key as INVALID); revised `checks_table.py` (`RowCountMatches` migrated to lineage); revised `checks_raster.py` (removed local `_lineage_params`, imports from `_lineage`); updated `check_registry.py` (retired `nodata_preserved`, added the two split names alphabetically); updated 7 operators (`build_cog`, `clip_raster`, `aspect`, `hillshade`, `slope`, `sample_raster`, `zonal_stats`) to stash the required scalars alongside existing caller params; updated `test_check_raster.py` to split the parametrized WARN test and add VALID/INVALID coverage for the 5 scalar Check classes; updated `test_check_table.py` for the `RowCountMatches` migration. The 4 raster-sized check classes (`AllValidAssigned`, `ElevationOnlyRaised`, `NoCycles`, `Conservation`) unchanged — Brief 8's territory. **First engineer rotation in the chain**: Brief 7 ran on Codex (the seventh consecutive Codex Brief across Knickpoint, Cascade, and Bench); the Source rotated to Claude after Brief 7 returned and before Brief 8 drafted. Pytest counts for Brief 7 were not preserved across the rotation; the integration was verified by direct file inspection plus dispatched verification-agent grep across the broader quarry repo (zero remaining `\bnodata_preserved\b` references in active code; three dated historical entries in `PRESSURE_TESTS.md` correctly left as historical record). Brief 7 closes Open brainstorm #1 (Thread B) at half-state: scalars done, rasters pending.
- **Kernel content-derived run_id — Brief 12 (the chain's first kernel Brief).** Brief 12 by Meander (integrated 2026-05-22). The chain's first Brief against the new Rust kernel at `/Users/jakegearon/projects/watershed-kernel/`. Closes `DESIGN_DEBT.md` item 4 (`run_id` stub replaced with content-derived hash). New `pub fn derive_run_id(intent: &RecoveredIntent, claims: &[FileClaim]) -> String` in `watershed-distributary/src/lib.rs` at `:247`: SHA-256 over `b"run:"` ++ `serde_json::to_vec(intent)` ++ `serde_json::to_vec(claims)`, returning `format!("run:{digest:x}")`. Strategy-tag prefix `run:` matches the chain's precedent (`src:`/`prov:`/`ref:` from quarry Brief 3, `disprun:`/`oprun:` from Cascade Brief 4 + Anabranch Brief 9). `dispatch()` updated at `:264` to destructure `Validated { intent, claims }` and call the new derivation. Three new integration tests in `tests/run_id_identity.rs` (equivalent dispatches → equal ids; differing intents → different ids; differing claims → different ids), each walking the full ceremony via a small `validated_plan(goal, path, kind)` helper. One-character assertion update in `tests/lawful_motion.rs:46` (`"baseline-merge-run-"` → `"baseline-merge-run:"`). Verification: `cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace`, `cargo xtask schemas` all clean from `/Users/jakegearon/projects/watershed-kernel/`. Schema content SHA-256 hashes byte-identical before/after (Watermaster-side cross-check confirmed via `$comment` equality on all 9 schemas). Nine compile-fail PressureTests continue to refuse illegal motion. The chain's content-derived-id discipline now has its kernel counterpart; identity is derived from content, not assigned, on both sides of the boundary the kernel will eventually eat. DESIGN_DEBT.md item 4 retired by removal as part of the integration (the line is deleted; the audit record lives at `sketches/briefs/14-meander/return-12.md`).
- **DispatchRun-side migration — Brief 9 (the dgov side of the parallel-chains regime).** Brief 9 by Anabranch (integrated 2026-05-14). Closes the central typed hole on the dgov side of the dispatch chain, bringing it to parity with the quarry-side OperatorRun work Cascade landed in Wave 4. Lands all 21+ fields of `dispatch-run-shape.md` v1 in code: new module `src/dgov/dispatch_run.py` mints typed `DispatchRun` (`@dataclass(frozen=True, slots=True, init=False)`, content-derived id with `disprun:` strategy-tag prefix, custom `__init__` + private `_clone` per Cascade's `init=False`-friendly pattern, five state-transition methods `start_active`/`complete_done`/`complete_failed`/`complete_timed_out`/`complete_abandoned` with source-state guards and transition-specific invariants); `_SCHEMA_VERSION = 9` with new `dispatch_runs` table created at `_get_db` init (parallel to `operator_runs` on the quarry side); new `src/dgov/persistence/dispatch_runs.py` with idempotent `INSERT OR REPLACE` save + filtered list + per-unit dispatch-chain lookup; `EventDagRunner.__init__` accepts `dispatched_by: str = "watermaster:unknown"` matching Cascade's Brief 4 default precedent; `TaskContext` gains three new fields (`current_dispatch_run_id`, `retried_from_dispatch_run_id`, `forked_from_dispatch_run_id`) carrying lineage across the in-process retry and fork sites; `_dispatch` mints + dual-writes through both `dispatch_runs` and the existing `tasks` table; `_handle_worker_exit` records the prior DispatchRun's terminal state via `_record_terminal_dispatch_run` BEFORE any fork branching so the prior is always recorded as terminal; `_dispatch_terminal_state` detects iteration-exhausted + wall-clock-timeout signatures via `last_error` substring and routes to `complete_timed_out` (matching SOP §32) versus `complete_failed`; retry lineage instrumented at two sites — `_resume_single_task` (cross-restart continue-failed path) and `_interrupt_governor_action` (in-process governor-retry path); fork lineage instrumented at `_start_iteration_fork` with the fork's new DispatchRun minted in `_fork_worker` via the shared `_mint_dispatch_run` helper; `effective_sop_set_hash` recomputed at every dispatch by reloading `.dgov/sops/` and calling `compute_sop_set_hash`; `drift_against_plan` derived from `effective_hash != Plan.sop_set_hash`; `drift_evidence` populated with the single `metadata:modified=bundle:sop_set_hash` typed descriptor when drift detected (matches SOP §29 shape; named as inherited debt for the future bundler-level revision that would produce per-SOP set:added/set:removed descriptors). 20+ new tests across `test_dispatch_run.py` (DispatchRun unit) and `test_persistence_dispatch_runs.py` (round-trip, idempotency, terminal-replaces-active, ordering, schema version sanity); targeted pytest runs **37 passed for dispatch/persistence/boundaries + 108 passed for runner/continue (145 total)**; `ruff check` and `ty check` clean on every modified module. Three deviations integrated (full-pytest-suite-skipped per dgov AGENTS.md repo discipline; continue-failed rehydration of latest DispatchRun id closing retry lineage across runner restarts; visible `disprun:` prefix matching Brief acceptance criterion). Three flags pre-documented as inherited debt (Plan-id surrogate via `DagDefinition.name`; bundler-hash gap; pre-existing dirty files untouched). **First dgov-side code Brief in the chain**: prior Briefs 1–8 all touched quarry/quarry-core/quarry-operators/quarry-registry; Brief 9 is the first to land in dgov source. Closes the substantive code arc Bench reconned for Anabranch and brings the dispatch-chain rail to parity with the operator-chain rail at the typed-record layer. Lifecycle narrowing (the 5 downstream states `reviewing/reviewed_pass/reviewed_fail/merging/merged` peeling onto Deposit/Validation/Merge) remains Brief 10 territory.
- **Flume lift, wave 4 closed — legacy `RunRecord` / `RunStatus` / `save_run` / `get_run` / `list_runs` removal.** Brief 6 by Bench (integrated 2026-05-13). Implements Bench's design decision (delegated by the Source as part of "decide and argue your reasoning on 1-3 and then [let's] do those"): treat pre-existing uuid4-id rows in the `runs` table as historical-only data; remove the public API surface for reading or writing the legacy types; do not build a migration tool. Rationale: the historical rows lack typed fields (`operator_spec_hash`, `params_hash`, `dispatched_by`) that Briefs 1, 3, and 4 made required; fabricating them would corrupt the content-derived identity discipline Brief 3 landed. Twelve-file scope: `quarry-core/executor.py` (deleted `RunStatus` Enum and `RunRecord` dataclass; trimmed unused imports), `quarry-core/__init__.py` (removed `RunRecord` / `RunStatus` from imports + `__all__`), `quarry-registry/registry.py` (deleted `save_run` / `get_run` / `list_runs` methods + private helpers `_save_run_conn` / `_row_to_run`; trimmed `stats()` to return only `operator_runs` / `operator_run_states` / `artifacts` / `checks` / `lineage_edges` / `artifact_types`; the `CREATE TABLE IF NOT EXISTS runs` DDL retained in `_init_schema` as orphaned-data per the design decision), `quarry-cli/main.py` (removed two-line `get_run` fallback in `cmd_checks_show`), `examples/watershed_analysis.py` (migrated to `.state` / `save_operator_run` / `operator_runs` stats — this example had been broken on main since Brief 4), `tests/pressure_test/test_registry_operator_runs.py` (retired `test_legacy_run_record_paths_still_work`; refactored `test_checks_and_lineage_accept_legacy_and_operator_run_ids` into `test_checks_and_lineage_accept_operator_run_ids_without_run_fk` preserving the FK-rebuild coverage), `tests/pressure_test/test_registry.py` (renamed `test_save_run_replaces_existing_run_checks` → `test_save_operator_run_replaces_existing_run_checks`), and five top-level docs (`README.md`, `REPO_MAP.md`, `CONTRACTS.md`, `HYDROLOGY_PACK.md`, `PRESSURE_TESTS.md`) for 11 prose references. Full `tests/pressure_test/` returns **1968 passed, 54 skipped, 3 failed** — Brief 5 baseline 1969 minus the 2 legacy regression tests + 1 preserved FK-coverage test. `ruff check` / `ty check` / `examples/watershed_analysis.py` end-to-end all pass. The 3 known failures are unchanged from Brief 5 (Source's in-progress GeoJSON/router/docs connector work). The orphaned `runs` table stays queryable via direct DuckDB SELECT for anyone who needs to inspect old rows; the public API path is gone. Closes Open brainstorm #1 from Confluence's hand.

### Future debt — iterative writing agent

Possible future exception: a writer-facing agent inside (or attached to) `strata/` for the back-and-forth of manuscript composition. Special case because writing is a human-active process, not a watermaster-mediated dispatch. Park until the rest is built; until then writing also goes through the Watermaster.

### Outcrop compilation (DEFERRED)

Papers in → typed References + embeddings + tags. Watermaster mediates, but the compilation logic itself is non-trivial. Deferred per the Source.

## Live observations (not yet in any SOP)

- **The reproducibility triple.** `(truth_source, determinism_class, schema_version)` as a typed triple fully characterizes a comparable output's reproducibility. Each axis is governed by a separate SOP; the composition is the discipline a future eval harness has to consume. Don't pre-mint a typed triple — the pattern is emergent from the three SOPs being read together. Watch whether it earns its own contract.
- **Three typed catalogs already implied.** `quarry.connect` (Connector registry), `bedrock.pointer.canonicalize` (per-scheme rules), and `module.intents` (intent compilation). With `operator-shape` v1 the operator-registry (Operator name → class) becomes a fourth. Four is enough that a future Watermaster might want a meta-catalog for "what catalogs does the lab maintain?" Don't pre-build it; mint when patterns demand it.
- **Two type-not-yet-named placeholders surfaced from fof-compiler.** `TileManifest` (the hardcoded `tiles.py` dict of named bboxes) and `TrainingCheckpoint` (the three-stage training pipeline's "stage-1 features must be precomputed with a *trained* input_proj" invariant). Both are placeholders in `shared/` until the lift; don't mint either ahead of the lift, but flag them when the dataset-shape SOP is drafted. (`operator-shape` v1 is now minted; neither placeholder fell into its scope.)
- **The distributary↔tributary seam is now a Rust-authority/rim split.** dgov's earlier boundary cleanup remains useful history, but watershed no longer treats top-level Python `distributary/` and `tributary/` packages as the first lift. The seam's authority-bearing law lives in `watershed-kernel/`; future rim code may adapt dgov's orchestration around that law. The lift discipline holds in a narrower form: freeze dgov's production behavior as evidence, move only transition-consumed law into Rust, and leave effects above the kernel.
- **`Patch` was renamed to `Deposit`.** The typed contract that flows from a worker subprocess back into tributary was provisionally named `Patch` in Reach's first cut. On survey by Riffle, that name turned out to bias the type's design toward textual-diff containment (`git apply` mental model), which is wrong — the thing carries typed claims, validation evidence, lineage, and a worktree pointer. The lab's metaphor wins over the VCS-cultural fit: a `Deposit` is what a flowing channel lays down, with provenance intrinsic to the metaphor. The chain now reads `Plan → DispatchRun → [worker subprocess] → Deposit → Validation → Merge → Baseline`. The lesson: provisional names carry through to type design when not tested against the metaphor early.
- **The Engineer role's naming history.** A pattern emerged in Riffle's session: the Watermaster compiles natural-language thinking into a typed brief, that brief crosses to a different intelligence (Codex, Claude Code, future Gemini), the external agent executes and returns, the Watermaster integrates. Used twice in Riffle's session — first to draft the Validation/Merge/Baseline trio, then to execute A-F. Now disciplined by `sops/engineer-brief.md` v1 (see Settled pointers). The Source and Watermaster considered **Engineer** (chosen), **Wright** (runner-up, archaic), and **Oracle** (explicitly rejected — divination is the wrong frame; construction is the right one; "oracle" was subsequently scrubbed from `truth-source-labeling` v1→v2 to avoid cognitive friction). The lab has three agent classes: **Watermaster** (seated at the rim, holds the chain), **Worker** (subprocess dispatched via Plans, returns Deposits), **Engineer** (consulted directly by the Watermaster, executes within Brief scope, returns drafts/critiques/file writes). The naming history is preserved here as the surfacing record; the three-agent-class structure itself has graduated to CANON Article XVI (v5, 2026-05-12, Terrace) and is no longer a Live observation.
- **The bundler-hash-composition gap.** `dispatch-run-shape` v1 records `effective_sop_set_hash` using "the same method the Plan used." Today that method (`sop_bundler.compute_sop_set_hash`) hashes `(filename, name, title, summary, applies_to, priority)` — bundler metadata, not SOP body. A worker-SOP bundle whose bodies change but whose metadata stays constant would not register as drift. The SOP's escalate clause names this as a bundler-level revision via preflight; it is not the DispatchRun's job to override bundler discipline. Watch whether this gap bites; if it does, a bundler-level revision is the cleanest fix.
- **Source-pass bugs become natural lift-escalates.** When a Watermaster source-reads to ground an SOP (Pool's convention) or to consolidate (Eddy's pass), the read incidentally surfaces bugs in existing code. Eddy's pass found one: `Artifact.with_check` in quarry-core's `artifact.py` constructs the returned Artifact without the `temporal` field, silently dropping any TemporalDescriptor on every check addition. The discipline is to flag-not-fix from the Watermaster seat: the lift is the right moment because lifted types get their constructor surfaces touched anyway, and a cross-repo write doesn't belong inside the rim per CANON Articles VI and IX. **Closed by Brief 1 (Knickpoint, 2026-05-11)** — `Artifact.with_check` now uses `dataclasses.replace(self, checks=new_checks)`, which structurally prevents this class of bug from recurring on other fields. Pattern still valid for future source-passes: flag-not-fix from the Watermaster seat, close at lift time. Future Watermasters doing source-reads should plan for one or two flag-not-fix entries in their lineage per pass.
- **Verification-by-git-diff has a recurring failure mode when the worktree carries multiple unintegrated Briefs.** Discovered in Knickpoint's session across all three Briefs: when a verification agent reads `git status` / `git diff HEAD` to confirm an Engineer's writes stayed in scope, it can't tell which lines came from this Brief versus prior Briefs sitting uncommitted in the worktree. Knickpoint's verification agent twice misattributed prior-Brief writes (Brief 1's `operator.py` changes; the Source's separate in-progress connector files) as scope violations of the current Brief. The verification overhead exceeded its yield. Two corrective patterns: (a) brief the verifier with explicit "the worktree contains writes from Brief N; the new writes for Brief N+1 are on top of that baseline" framing; (b) ask the Source to confirm authorship before naming a contradiction, since the workspace state belongs to the Source. After three clean Briefs (Codex's scope discipline held throughout), trust calibrated upward and verification became lighter-touch. The Watermaster's verification responsibility is real, but the verification overhead is a tax that can shrink with trust as the Engineer pattern proves itself.
- **Staged migrations are a viable Brief shape.** Brief 4 left out-of-scope consumers (`hydrology_flow.py`, two CLI test files, an `adapter_helpers.py` alias bridge) deliberately, accepting downstream test failures (`4 passed, 3 failed, 14 errors`) as the natural consequence of a bounded `write_scope`. The pattern: hold a hard line on `write_scope` per `engineer-brief.md` v1 even when consumers will break temporarily, then mint a focused follow-up Brief (Brief 5) to close the loop. Trade-off: Brief verifiability improves (each Brief's diff stays bounded; the test result reads cleanly as "in-scope passing, out-of-scope flagged"); the cost is that the lab's continuous-test-green-line lapses for the Brief-pair window. Worth considering whether to canonicalize "staged migration" as a named pattern in `engineer-brief.md` v2 — but a single Wave 4 exercise isn't enough; watch for recurrence. *(Cascade, 2026-05-11.)*
- **Custom `_clone` is the `init=False`-friendly state-transition mechanism.** Brief 4 needed state-transition methods on `OperatorRun` (`start_running`, `complete`, `fail`, `cancel`) that produce new frozen instances. Brief 4 named `dataclasses.replace` as the natural mechanism with `_clone(**changes)` as a documented fallback. Codex took the fallback explicitly to preserve `id` overrides and avoid `init=False`/derived-field friction. The pattern (custom `__init__` for coercion + frozen + private `_clone` for transitions) generalizes: any future typed record with both lifecycle transitions and content-derived id will likely want the `_clone` shape over `replace`. Worth keeping in mind if (when) `DispatchRun` gets the same migration treatment (`dispatch-run-shape.md` v1 names it; no Brief yet). *(Cascade, 2026-05-11.)*
- **`Lineage.params` as the canonical home for input-derived scalars at operator output time.** Brief 7 extended a precedent reproject.py had quietly established (stashing `source_crs` alongside caller `target_crs`/`resampling`/`resolution_override` in Lineage.params). Seven more operators followed in Brief 7: input-derived scalars (`input_nodata_value`/`input_nodata_count`/`source_crs`/`input_band_count`/`input_extent`/`expected_row_count`) live in `Lineage.params` alongside operator caller params. The merge is semantically heterogeneous within the `params: Mapping[str, Any]` field — "what the operator was asked" and "what the operator observed about its inputs" both reside in the same Mapping. A future typed split (e.g., a new `Lineage.input_signals: Mapping[str, Any]` field) is a reasonable follow-up if downstream consumers want to distinguish them, but the Brief 7 design call was: extend the existing precedent rather than mint a new typed field that would require a `data-contracts.md` preflight. Worth flagging because this is the moment the precedent stops being one-operator (reproject's `source_crs`) and starts being a pattern (eight operators stashing input observations). If a future Brief encounters the same pressure, the typed split is the right escalation move. *(Bench, 2026-05-13.)*
- **`Lineage.params` participation in Artifact.id derivation has a known consequence.** Brief 3 made `Artifact.id` content-derived via `derive_id_from_provenance(operation, inputs, params)` where `params` is `Lineage.params`. Adding new keys to operator lineage params changes prov-derived ids for newly-produced output Artifacts of those operators. Brief 7 named this implication explicitly and accepted it (no test fixture in the affected operators' integration tests pinned literal Artifact ids — operator-integration tests read `lineage.params` dynamically). Future Briefs that add new keys to `Lineage.params` for any operator should explicitly own this Artifact.id-stability change; the precedent now exists in Briefs 4 (reproject already), 7 (seven more operators), and presumably future cases. *(Bench, 2026-05-13.)*
- **Engineer rotation mid-arc is a real case the SOP handles.** Briefs 1-7 all ran Codex (Knickpoint × 3, Cascade × 2, Bench × 2). The Source rotated to Claude between Brief 7's return and Brief 8's draft. This is within `engineer-brief.md` v2's current affordance (Source-as-carrier three-party loop, no harness change). Two consequences for the integration discipline: (a) when the Engineer's verbatim chat return is not preserved across the rotation, `EngineerReturn.content` falls back to the "summary of file writes" clause — verification by direct file inspection plus a dispatched verification-agent grep is a legitimate integration path. The substantive audit record lives in the `integration_action` field and the file diff itself, not in the Engineer's prose. (b) Knickpoint's verification-overhead-calibrates-with-trust observation and Cascade's extension across sessions were calibrated specifically against Codex's track record across 7 Briefs. With a new engineer the calibration resets: verification overhead returns to a higher level for at least the first one or two Briefs with the new engineer, then re-calibrates as the new engineer establishes its own track record. This is also a reminder that calibrated trust is per-engineer-per-pattern, not global. *(Bench, 2026-05-13.)*
- **First Claude Brief landed clean; pattern transfers across engineers.** Brief 8 was the first Engineer Brief sent to Claude (Claude Code) rather than Codex. The discipline transferred cleanly: structured return matched the Brief's expected format (Summary / Item-by-item / Test results / Flag list); scope-clean writes within `write_scope` (one downstream-import-update on `test_flow_accumulation.py` that was within the Brief's spirit per Item B's hoist directive); end-to-end parity verification by the Engineer (fill→D8→accumulate chain on a 5×5 sloped DEM); 1974/54/3 pytest counts with the 3 known failures unchanged. The trust calibration across engineers held: a single first-Brief exercise isn't enough to canonicalize Claude as a "calibrated" engineer parallel to Codex, but the discipline floor (typed Brief in → structured return out → Watermaster integrates) is robust to engineer identity. *(Bench, 2026-05-13.)*
- **Brief-drafting precision: cross-check constant values against current source.** Brief 8's Item F body referenced D8 constants by literal value (`OUTLET = 0, PIT = 9, NODATA = -1`). The codebase has `OUTLET = 8, PIT = 9, NODATA = -1` — my drafting had a typo for `OUTLET`. The Engineer correctly chose the codebase as source of truth and flagged the discrepancy in the return. The discipline absorbed the error cleanly, but future Brief drafts should cross-check literal constant values against current source rather than rely on memory or assumption. The Brief already names the codebase as the source of truth ("use the constants from `flow_accumulation.py`"), so the body-text discrepancy could mislead a less-careful Engineer. Worth catching at preflight review next time. *(Bench, 2026-05-13.)*
- **Coupled-import-and-use edits to files with `from __future__ import annotations` want single-shot writes when autofix linters are active.** Brief 8's Engineer (Claude Code) hit friction with a PostToolUse ruff autofix: a new `from collections.abc import Sequence` import was stripped between sequential edits because the import landed before the signature edits that made it live. Resolution was to add the import only after the signatures referenced it, or write the file in one shot. First Claude-engineer-specific tooling observation. Worth a Live note for future Claude-engineer Briefs editing shared files. (Pattern doesn't seem to have bitten Codex previously, which may reflect a different editing strategy or different tooling configuration.) *(Bench, 2026-05-13.)*
- **Recon-only deliverables as a third file class under `sketches/briefs/{NN-watermaster}/`.** When an arc warrants reconnaissance but the Watermaster judges the Brief draft itself should wait (fresh context for a heavy Brief, or a recon foundation the next Watermaster can build on), a `recon-{N}.md` file parallel to `brief-{N}-{slug}.md` and `return-{N}.md` is a legitimate session output. Bench used this for the DispatchRun-side migration recon (Brief 9 territory) — the recon mapped 21+ SOP fields against dgov's current state, identified a 5-site fragmentation in lieu of a `RunRecord` analogue, named three Source-side design questions, and recommended a staged-migration shape — but the Brief itself wasn't drafted because (a) the session had already integrated three Briefs plus an SOP preflight, (b) the Brief 9 size class is at or larger than Cascade's Brief 4 with preflight-gated schema migration nested, (c) the next Watermaster benefits from fresh context for the substantial draft. Single use so far; not yet a canonicalized convention. If a second or third instance of recon-as-deliverable surfaces in future sessions, `engineer-brief.md` v3 could fold the convention into the SOP body parallel to the existing `brief-{N}-{slug}.md` / `return-{N}.md` clause. Until then, Live observation. *(Bench, 2026-05-13.)*
  - **Recon-foundation-second-use.** Brief 9 absorbed Bench's recon as drafted and consumed: every Source-side design question was surfaced cleanly, every file:line citation transferred to the Brief without re-discovery, and the staged-migration recommendation informed the meta-preflight's Items A–M structure. Second use of the recon-as-deliverable convention; the pattern is exercised enough that `engineer-brief.md` v3 candidacy is worth a future Watermaster's attention. Holding for a third instance before preflighting. *(Anabranch, 2026-05-14.)*
- **Max-scope Brief at the high end of Codex's range.** Brief 9 absorbed all three recon-recommended stages (9a + 9b + 9c) in one Codex execution per the Source's "max scope on the brief we will testt the range of codex" framing. Outcome: 11 files (4 new + 7 modified), ~970+ lines new code, 145 tests passing in targeted runs, three honest deviations + three pre-documented inherited-debt flags, zero scope-creep writes. The Brief was substantive enough to require two-site instrumentation (retry-lineage at both `_resume_single_task` and `_interrupt_governor_action`) and a continue-failed rehydration pattern Codex flagged-as-deviation but is structurally necessary for correctness. Calibration: with a settled regime + well-prepared recon foundation + Codex's eight-Brief track record across three Watermasters (Knickpoint × 3, Cascade × 2, Bench × 2, Anabranch × 1), max-scope is viable when the Brief carries the SOP-shape discipline through to file-level line citations. Worth keeping in mind for future heavy Briefs: a thorough recon plus a max-scope Brief can collapse what looked like a 3-Brief arc into one Codex execution cycle. *(Anabranch, 2026-05-14.)*
- **Two-site retry-lineage instrumentation is the actual shape dgov needs.** Brief 9's Item J described a single retry-lineage site at `_interrupt_governor_action`. Codex correctly identified that dgov has TWO retry-decision paths — the in-process governor-retry (`_interrupt_governor_action` line 1514) and the cross-restart continue-failed (`_resume_single_task` line 364) — and instrumented both. Without the second site, a `--continue` after a process restart would mint the next DispatchRun with `retried_from = None` despite a prior failed attempt existing on disk. The generalizable lesson: when typed-record lineage must survive process boundaries in dgov-style architectures (kernel + runner + continue flag), the lineage carrier (here `TaskContext.current_dispatch_run_id`) needs rehydration from persistence at restart time. Codex added a `_rehydrate_dispatch_run_contexts` method at `_phase_rehydrate` exit (line 394-399) reading `get_dispatch_runs_for_unit(slug)` and taking the latest row's id. Brief-drafting note: when a Brief's `write_scope` is broad enough to touch a restart/rehydration path, the Brief should ask the Engineer to instrument both paths AND wire rehydration; a single-site instruction will be flagged-as-deviation honestly but the right shape becomes obvious only once Codex traces the actual flow. *(Anabranch, 2026-05-14.)*
- **Sandbox `uv` cannot download Python interpreters for independent verification of dgov Briefs.** The watershed VM's `uv` returned `tunnel error: unsuccessful` when trying to fetch a `python-build-standalone` release. This means Watermaster-side independent pytest re-run is not possible for dgov Briefs from this sandbox. Verification falls back to direct file inspection + the Engineer's structured return for test counts. Same posture Bench took on Brief 8 when the filesystem MCP shrank allowed-dirs. The discipline floor (typed Brief in → structured return out → Watermaster integrates) is robust to this; the trust calibration with Codex across eight Briefs absorbs the gap. Future Watermasters doing dgov-side code Briefs should expect the same constraint unless sandbox networking improves; either run pytest on the host via a side channel, or trust the Engineer's structured return per the EngineerReturn-as-summary-of-file-writes clause from `engineer-brief.md` v2. *(Anabranch, 2026-05-14.)*
- **The Rust-kernel architectural shift; SOP-reconcile question held open.** The Source's decision to build watershed-kernel in Rust (`/Users/jakegearon/projects/watershed-kernel/`, type-state lawful motion enforced at compile time, art-grade quality, ~6-month horizon, Python/TS above the kernel) reframes the chain's prior SOP work without invalidating it. The dispatch-chain SOPs (`plan-shape`, `dispatch-run-shape`, `deposit-shape`, `validation-shape`, `merge-shape`, `baseline-shape`) describe discipline that the kernel will *compile-enforce* over time; the operator-chain SOPs and the meta-discipline SOPs (intent-compilation, engineer-brief, schema-versioning, etc.) continue to live as Python-side / lab-side discipline that the kernel does not model. The Watermaster's role shifts: previously the SOPs were the *only* enforcement (no Watermaster reading = no discipline holding); now the Rust kernel enforces the dispatch chain at compile time regardless of who's at the rim, and the Watermaster's discipline-at-the-rim governs what the kernel can't (operator chain, intent compilation, engineer briefs, the lineage chain itself, the research-institutional canon, the modules above the kernel). The reconcile question — whether the SOPs should be revised to name the kernel as implementation, follow the kernel as it grows, or name both as authoritative with gaps flagged — was surfaced explicitly and the Source asked to slow way down. Held open. The chain's natural posture for now: SOPs describe the canonical target the kernel will grow to; the kernel is the bounded-scope work; Briefs against the kernel land one piece at a time per the Design Gate. *(Meander, 2026-05-22.)*
- **codex_mcp ruled out for the kernel's six-month horizon.** Cowork's user-installable MCP surface accepts only remote HTTP/Streamable HTTP transports; OpenAI Codex CLI's `codex mcp-server` is stdio. Bridge work (mcp-proxy + ngrok/cloudflare tunnel + Custom Connector registration in Cowork Settings → Integrations) is real ops investment the project's core work isn't asking for. Source decided not to pursue; the chain's existing Source-as-carrier loop (`engineer-brief.md` v2's current canonical shape) is the operating mode for kernel work until further notice. The v3 candidacy for direct cross-model invocation Confluence anticipated in v2's Escalate clause is now formally retired (not pending). Watermasters working code Briefs against the kernel use the in-chat `/goal`-format carrier routine above. *(Meander, 2026-05-22.)*
- **First kernel Brief landed clean; Codex's structured-return shape transfers to Rust without strain.** Brief 12 was the chain's first Brief against `/Users/jakegearon/projects/watershed-kernel/` (prior Briefs touched quarry-core/quarry-operators/quarry-registry and dgov). Codex's return naming files-written + verification-gates-passed + flag-list maps cleanly onto the chain's `return-{N}.md` audit shape. Cargo's tooling (`cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace`, `cargo xtask schemas`) gives the four-command verification pattern at the kernel a clean analogue to quarry's `just test-all` + `ty check` + `ruff check`. Codex held the declared `write_scope`, flagged the Cargo.lock and schema-mtime adjacencies honestly, and made one defensible micro-choice within scope (the `validated_plan(goal, path, kind)` helper in the new test file to keep three tests readable). Discipline floor robust to engineer + target-repo change. Trust calibration for Codex-on-kernel starts fresh at one Brief; per Bench's "calibrated trust is per-engineer-per-pattern" Live observation, two or three more clean kernel Briefs canonicalize it. *(Meander, 2026-05-22.)*
- **SOPs are agent-facing discipline; the kernel has no agents inside it.** Meander's SOP-reconcile question (which layer drives when SOPs and kernel disagree) implicitly assumed SOPs and the kernel were at the same level of abstraction. The Source's reframing on 2026-05-22 — "the kernel shouldn't need SOPs there are no llms in the kernel" — clarifies that they are not. SOPs exist to discipline LLM agents (Watermasters and Workers); the Rust kernel enforces lawful motion by refusing illegal motion at compile time, not by being read. The reconcile question dissolves into a layering question: shape SOPs (plan-shape, dispatch-run-shape, deposit-shape, validation-shape, merge-shape, baseline-shape) become "specifications-the-kernel-meets" — agent-readable documentation of substrate guarantees. Agent SOPs (watermaster-preflight, watermaster-passage, intent-compilation, engineer-brief, sop-shape) continue to apply directly to the seated Watermaster regardless. The hybrid SOPs (operator-shape, operator-run-shape, event-emission, schema-versioning, truth-source-labeling, determinism-class, pointer-canonicalization, data-contracts) reconcile depends on the orchestration layer above the kernel — which today does not exist as Python/TS source (the top-level Python `distributary/` and `tributary/` attempt was removed on 2026-06-05; the Rust crates inside `watershed/watershed-kernel/` are the authority-bearing implementation). Deferred until that layer materializes. Punt is the right move; "we haven't gotten there yet" is the honest description. Closes TOPOGRAPHY.md Operator Decision #9 as "clarified, deferred." *(Spring, 2026-05-22; updated by Splay, 2026-06-05.)*
- **Rust receives law when consumed, not every adjacent behavior.** The 2026-06-05 Source clarification closes the overcorrection risk from Avulsion's deleted Python packages: the rule is not "move everything to Rust." The rule is that lawful motion, record identity, claim authority, and construction boundaries move into Rust when a current kernel transition creates, proves, or consumes them. Worker execution, worktrees, persistence, prompts, event stores, sentrux subprocesses, and CLI surfaces remain above the kernel until a typed rim layer needs them. *(Splay, 2026-06-05.)*
- **Backwater hardening pass — authority paths, identity, and root orientation.** PRs #1-#7 landed on `port-distributary-tributary-records-base` on 2026-06-06. The pass reviewed and merged the kernel authority-boundary stack, then added focused pressure-test-backed hardening for file-claim path escapes, deposit touched-path canonicalization, canonical file claims in plan/DAG/run identity, padded DAG slug rejection, root README orientation, and retirement of top-level Python `distributary/` / `tributary/` scaffolds. The boundary is now sharper: Rust crates under `watershed-kernel/` own distributary/tributary law; future rim or subrepo code wraps that substrate rather than reimplementing state machines. Ignored Python bytecode residue was inspected before deletion; useful residue was captured as deferred outer-layer debt in `watershed-kernel/DESIGN_DEBT.md`, gated by `STOP_LINE.md`'s transition-consumption rule. *(Backwater, 2026-06-06.)*
- **Kernel pressure tests can be enforced by runtime integration tests alongside compile-fail tests.** Brief 13's `validation_rejects_unclaimed_files` pressure test points at `watershed-tributary/tests/claims_integrity.rs` — a runtime integration test — rather than a `tests/compile_fail/*.rs` trybuild fixture. This was authorized by the Brief ("if you can't make it a compile-fail test, make it a deterministic integration test ... and add the corresponding entry to `pressure_tests()`"). Codex took the authorization cleanly and established a second `PressureTest.enforced_by` shape (runtime integration tests alongside compile-fail tests) without requiring any change to the `PressureTest` struct shape. The constitutional reasoning: rules about runtime behavior (validation logic, state-transition invariants only verifiable at runtime) are still constitutional evidence — they just live in a different mode (deterministic test passes/fails) than rules about type construction (which compile-fail tests verify). The kernel's `pressure_tests()` registry now reads as a heterogeneous list: 9 compile-fail-enforced + 1 runtime-enforced + room for both shapes going forward. *(Spring, 2026-05-22.)*
- **Settled — Worker lifecycle and claims integrity in the kernel.** Brief 13 by Spring (integrated 2026-05-22). Closes the worker-lifecycle state-machine hole and the claims-integrity constitutional hole in the Rust kernel. Worker runs now traverse `Run<Pending>` → `Run<Running>` → `Run<Completed> | Run<Failed>` via sealed consuming transitions (`Run<Pending>::start()`, `Run<Running>::complete(summary, touched_files)`, `Run<Running>::fail(reason)`). `Run<S>` owns `id`, `intent`, `claims` at the outer struct (threaded forward from `dispatch`); state markers carry only state-specific data (`_private: ()` for Pending/Running, `deposit` for Completed, `reason` for Failed). `mock_worker` now takes `Run<Running>` so it cannot serve as a public `Pending → Completed` shortcut. `collect(Run<Completed>) -> (Deposit, Vec<FileClaim>)` preserves the in-memory handoff of both worker output and dispatch-time claims without minting a new grab-bag type. `validate(deposit, claims: &[FileClaim]) -> Validation` enforces two structural invariants (empty-summary rejection preserved + new claims-integrity check that rejects any deposit whose touched files include paths outside the plan's claims). Two new pressure tests registered: `complete_run_before_running` (compile-fail at `tests/compile_fail/complete_run_before_running.rs`, demonstrates `Run<Pending>` has no `complete(...)`) and `validation_rejects_unclaimed_files` (runtime integration test at `watershed-tributary/tests/claims_integrity.rs`). All four kernel verification gates passed Codex-side (`cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace` including 10 trybuild fixtures, `cargo xtask schemas`). Three existing compile-fail tests absorbed inline updates (`baseline_without_merge`, `merge_rejected_validation`, `construct_completed_run_directly` + `.stderr`) — laws preserved, mechanism updated where applicable (the `Run<Completed>` privacy block shifted from state-marker fields to outer-Run fields; the test still refuses fabrication). DESIGN_DEBT items 1-3 (`WorkClass`, `VerificationSpec`, `RollbackSpec`) stayed deferred per the Brief's structural-vs-Plan-declared distinction. Subcrate READMEs (`watershed-distributary/README.md`, `watershed-tributary/README.md`) updated Watermaster-side post-Brief to reflect the new run-state ceremony and the new `validate` signature. Audit at `sketches/briefs/16-spring/return-13.md`. **Second clean kernel Brief; first exercise of the policy-direction Brief style.** *(Spring, 2026-05-22.)*
- **Settled — Retry budget bounded by policy in the kernel.** Brief 15 by Hyporheic (integrated 2026-05-23). Adds a typed retry-depth bound to the kernel's `Policy` contract and enforces it at `Run<Failed>::retry()`. `Policy` gains `max_retries: Option<u32>` (None = unbounded; Some(n) = at most n retries past the original dispatch); the field placed between `allow_shared_claims` and `required_pressure_tests` per Codex's design call (governance bools then governance lists). `Plan<Compiled>::validate(&policy)` extracts the budget; `Validated` carries it; `dispatch` threads it into `Run<S>` as a new private field parallel to `retried_from`/`retry_index`. `Run<Failed>::retry()` signature changed from infallible to fallible: `Result<Run<Pending>, RetryError>`. New `RetryError::BudgetExhausted { current: u32, budget: u32 }` is the typed refusal variant; the bound check is the first thing `retry()` does (`if retry_index >= budget`); when `max_retries == None`, retry succeeds unconditionally (existing behavior preserved). `derive_run_id` signature and hash composition unchanged — the budget is governance, not identity, so two retries from equivalent failed parents under different policies produce equal ids. One new pressure test registered: `retry_respects_max_retries` (runtime integration test at `watershed-distributary/tests/retry_budget.rs`, five tests covering retry-succeeds-below-budget, retry-refused-at-budget with exact `BudgetExhausted { current: 1, budget: 1 }` variant matching, zero-budget-refuses-first-retry edge case, unbounded-allows-arbitrary-depth, and `retry_budget_is_not_part_of_run_identity` — the structural assertion that bounded and unbounded retries from equivalent failed parents produce identical ids, verifying the "governance not identity" decision). All four kernel verification gates passed Codex-side (`cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace` with 11 trybuild fixtures + 3 runtime invariants = 14 pressure tests total, `cargo xtask schemas` with `schemas/Policy.json` regenerated to include nullable `max_retries`). Three compile-fail fixtures absorbed Policy-literal updates (`dispatch_twice`, `complete_run_before_running`, `retry_completed_run`); one absorbed a private-field expansion (`construct_validated_plan_directly.rs` + `.stderr` now names `intent`, `claims`, AND `max_retries`); the `construct_completed_run_directly.stderr` private-field note now reads `id, intent, claims, retried_from, retry_index and max_retries` — Spring's "growing `Run<S>` private-field list as canonical fabrication-blocking mechanism" extends cleanly. Four existing integration tests absorbed Policy-literal updates (`lawful_motion.rs`, `worker_lifecycle.rs`, `run_id_identity.rs`, `retry_lineage.rs`) and `retry_lineage.rs` additionally absorbed six `.retry().expect(...)` call-site updates for the new `Result` return. `watershed-distributary/README.md` updated by Codex per Brief 14's subcrate-README-ownership clause (second exercise; clause read cleanly again). `watershed-tributary/tests/claims_integrity.rs` correctly NOT updated — it doesn't construct a `Policy` literal; Codex held the line. `watershed-kernel/PRESSURE_TESTS.md` updated Watermaster-side post-Brief via small preflight to document `retry_respects_max_retries` in the Runtime invariants section (second exercise of the kernel-root-governance-docs convention Spring named). DESIGN_DEBT items 1-3 stayed deferred throughout. Audit at `sketches/briefs/17-hyporheic/return-15.md`. **Fourth clean kernel Brief; third clean exercise of the policy-direction Brief style; second exercise each of subcrate-README ownership and kernel-root-governance-docs conventions — three conventions now at the threshold `engineer-brief.md` v3 candidacy needs to tip.** *(Hyporheic, 2026-05-23.)*
- **Settled — Retry lineage from Run&lt;Failed&gt; in the kernel.** Brief 14 by Spring (integrated 2026-05-22). Closes the kernel's terminal `Run<Failed>` dead-end gap and lands the chain's retry-lineage discipline (named by `operator-run-shape.md` v1, implemented quarry-side in Brief 4 and dgov-side in Brief 9) in the Rust kernel for the first time. `Run<S>` now carries `retried_from: Option<String>` and `retry_index: u32` as identity-bearing private fields alongside `id`, `intent`, `claims`. All existing transitions (`start`, `complete`, `fail`) preserve the lineage fields through state changes. New `Run<Failed>::retry(self) -> Run<Pending>` consuming transition: preserves intent and claims; sets `retried_from = Some(self.id)`; increments `retry_index` via `checked_add(1).expect(...)` (defensive overflow handling with a clear panic message); derives a new id via lineage-aware `derive_run_id`. `derive_run_id(intent, claims, retried_from: Option<&str>, retry_index: u32)` now hashes serialized lineage plus big-endian `retry_index` after the existing intent + claims inputs; strategy-tag prefix `b"run:"` preserved from Brief 12. Original dispatches call `derive_run_id(&intent, &claims, None, 0)`, preserving Brief 12's equivalent-dispatches-yield-equal-ids discipline. Two new pressure tests registered: `retry_completed_run` (compile-fail at `tests/compile_fail/retry_completed_run.rs`, demonstrates `Run<Completed>` has no `retry()` — only failed runs are retryable) and `retry_lineage_from_failed` (runtime integration test at `watershed-distributary/tests/retry_lineage.rs`, demonstrates the four retry semantics: lineage on first retry, content-derived identity across equivalent retries, lineage survives through transitions, chain-retry increments correctly from each failed parent). All four kernel verification gates passed Codex-side (`cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace` with 11 trybuild fixtures + retry runtime tests, `cargo xtask schemas` byte-identical). One existing compile-fail test absorbed inline update (`construct_completed_run_directly.stderr` now lists `retried_from` and `retry_index` alongside the existing private fields; law preserved). DESIGN_DEBT items 1-3 stayed deferred. `watershed-distributary/README.md` updated by Codex per Brief 14's new subcrate-README-ownership disambiguation clause (first exercise; clause worked cleanly per Codex's feedback). `watershed-kernel/PRESSURE_TESTS.md` updated Watermaster-side post-Brief via small preflight to cover the four pressure tests added by Briefs 13 and 14 (new `WorkerLifecycleSealed` grouped law + new "Runtime invariants" section). Audit at `sketches/briefs/16-spring/return-14.md`. **Third clean kernel Brief; second exercise of the policy-direction Brief style; Codex-on-kernel trust calibration now at Bench's named threshold.** *(Spring, 2026-05-22.)*
- **`Run<S>`'s growing private-field list is the canonical "no public constructors for authoritative states" enforcement mechanism in the kernel.** Brief 13 moved the `Run<Completed>` privacy block from state-marker fields (the old `Completed { id, deposit }` shape) to outer-Run fields (`id`, `intent`, `claims`, `state` private). Brief 14 added two more (`retried_from`, `retry_index`). The `construct_completed_run_directly` compile-fail test's stderr correctly absorbs the growing field list each time (Brief 13 updated it to name `id`, `intent`, `claims`; Brief 14 updated it again to add `retried_from`, `retry_index`). The pattern: every new field on `Run<S>` automatically extends the privacy block, so no separate enforcement work is needed beyond keeping the `construct_*_directly.stderr` fixtures in sync with the current private-field list. Worth noting because future Briefs that add fields to `Run<S>` will inherit this enforcement for free, and the corresponding `.stderr` files will need integration-tail updates to match. The same pattern applies to any state struct in the kernel that uses private-field-based fabrication blocking. *(Spring, 2026-05-22.)* **Brief 15 extended the pattern again** with `max_retries` joining the private-field list on both `Validated` and `Run<S>`; both `construct_validated_plan_directly.stderr` and `construct_completed_run_directly.stderr` updated to name the new field. Three consecutive Briefs (13, 14, 15) have grown the privacy block by 2-3 fields each without requiring any change to the fabrication-blocking mechanism itself — strong evidence the pattern is structurally robust. *(Hyporheic, 2026-05-23.)*
- **Brief-drafting precision: pressure-test counts come from `pressure_tests()` registry, not from compile-fail directory listings.** Brief 15's drafting cited "11 pressure tests" in two places (Constitutional constraints + Verification gates sections) when the actual count was 13 at draft time (11 compile-fail + 2 runtime invariants). Codex flagged this in return feedback: "The brief should state the current pressure-test count mechanically, or avoid exact counts when the kernel is moving quickly." The slip's specific cause: my recon read both the compile-fail directory listing AND the `pressure_tests()` registry, but I drafted the Brief citing the lower compile-fail-only count. Two corrective patterns going forward: (a) **Counts come from the source of record.** When citing pressure-test counts, read the length of `pressure_tests()` in `watershed-contracts/src/lib.rs`, not the file count of `tests/compile_fail/*.rs`. The registry includes runtime invariants the directory listing misses. (b) **Avoid exact counts when describing the constitutional surface in flight.** Where counts are background context (not the Brief's focus), use indefinite reference ("the existing pressure tests") rather than exact numbers. Where counts genuinely matter (a Brief explicitly extending the registry), spell out the names and let Codex verify against current state. This is the same family of slip as Bench's "Brief-drafting precision: cross-check constant values against current source" (the D8 constants typo in Brief 8) — both are drift between Brief text and current-source ground truth. Both are absorbable by the Engineer when the codebase is named as source-of-truth, but cleaner Brief drafting avoids the friction. *(Hyporheic, 2026-05-23.)*
- **`max_retries` as governance-not-identity confirms the structural axis distinction Brief 13 named.** Brief 15's `derive_run_id` decision (keep the hash composition unchanged; do NOT include `max_retries`) demonstrates the structural-vs-Plan-declared distinction Spring's Brief 13 conversation established holds for a third axis: governance (the kernel's policy bounds) is structurally distinct from identity (the work's content-derived id). Two retries from equivalent failed parents under different policies produce equal ids — the work is the same; the policy boundaries that apply differ. The test `retry_budget_is_not_part_of_run_identity` is the runtime assertion of this law. The decision register going forward: when a Brief introduces a new field on a typed contract, ask three questions — (1) is it structural (the kernel always enforces it) or Plan-declared (the Plan author lists it)? (2) is it governance (the policy bounds it) or identity (the content derives it)? (3) is it lineage (the work's parent chain) or content (the work's own substance)? Each axis is independent; placing a new field on the wrong axis leads to either bypass-able rules (Plan-declared discipline that should be structural) or false identity differences (governance differences that fork ids when the work is the same). `max_retries` is structural governance, separate from lineage and identity. *(Hyporheic, 2026-05-23.)*

## Open brainstorms (next up)

1. **Outcrop's compilation structure.** Paper drop → typed Reference. Deferred per the Source.
2. **Pressure tests — where do they live?** Three options:
   (a) Their own SOP + their own appended file (PRESSURE_TESTS.md) — lift quarry's pattern wholesale.
   (b) The ledger (add `pressure_test` category) — once dgov's ledger migrates with distributary/tributary.
   (c) Hybrid: SOP for the practice, ledger for the records.
   Defer until the ledger migrates with the dgov split. The shape and discipline are settled; only the home is undecided.
3. **Event subscription model.** Option-2 routing requires modules reacting to events. dgov's events are queried for status, not subscribed. Defer until distributary lands. The parallel SOP to `event-emission` v1; when minted it picks up the subscriber binding contract that v1 deliberately doesn't discipline.
4. **Source's name.** Open. The case for naming differs from the Watermaster case (persistent, not ephemeral); useful only if the name changes how the role feels to inhabit.
5. **Pane identity hardening.** Backwater closed padded DAG task/dependency identity, but `TaskDispatched` still binds `pane_slug` without rejecting empty or padded pane names. This is the same identity-boundary class as file paths and DAG slugs. Next kernel slice should reject malformed pane identities before they enter `task_panes`, with focused tests in `watershed-distributary/tests/dag_kernel.rs` and a pressure-test registry entry if the law is made constitutional.
